---
wave: 1
depends_on: []
files_modified:
  - flext-core/pyproject.toml
  - flext-cli/pyproject.toml
  - flext-ldif/pyproject.toml
  - flext-ldap/pyproject.toml
  - client-a-oud-mig/pyproject.toml
autonomous: true
---

# 01-01: Foundation Dependencies

Install missing runtime and development dependencies across all 5 critical projects.

<tasks>
  <task id="1" status="pending">
    <objective>Install returns and beartype in flext-core</objective>
    <command>cd flext-core && poetry add returns beartype</command>
  </task>
  <task id="2" status="pending">
    <objective>Install returns and beartype in flext-cli</objective>
    <command>cd flext-cli && poetry add returns beartype</command>
  </task>
  <task id="3" status="pending">
    <objective>Install returns and beartype in flext-ldif</objective>
    <command>cd flext-ldif && poetry add returns beartype</command>
  </task>
  <task id="4" status="pending">
    <objective>Install returns and beartype in flext-ldap</objective>
    <command>cd flext-ldap && poetry add returns beartype</command>
  </task>
  <task id="5" status="pending">
    <objective>Install returns and beartype in client-a-oud-mig</objective>
    <command>cd client-a-oud-mig && poetry add returns beartype</command>
  </task>
</tasks>

<verification>
  <step>Verify all projects have returns and beartype in their pyproject.toml</step>
  <step>Run `poetry lock --no-update` in each project to ensure lock file is synced</step>
</verification>

<must_haves>
  - All 5 projects have `returns` and `beartype` installed.
  - No poetry lock conflicts.
</must_haves>
