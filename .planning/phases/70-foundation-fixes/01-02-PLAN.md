---
wave: 2
depends_on: ["01-01-PLAN.md"]
files_modified:
  - flext-ldif/src/flext_ldif/constants.py
  - flext-ldif/src/flext_ldif/models.py
autonomous: true
---

# 01-02: Fix LDIF Max Line Length

The test suite expects a default max line length of 199, but it's currently 100.

<tasks>
  <task id="1" status="pending">
    <objective>Locate and update ldif_max_line_length default to 199</objective>
    <command>grep -r "ldif_max_line_length" flext-ldif/src/</command>
  </task>
</tasks>

<verification>
  <step>Verify the default value is 199 in the code.</step>
</verification>

<must_haves>
  - `ldif_max_line_length` default value is 199.
</must_haves>
