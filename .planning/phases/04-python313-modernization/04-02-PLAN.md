---
phase: 04-python313-modernization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "*/src/**/*.py (StrEnum classes missing @unique)"
  - "*/src/**/*.py (Literal unions convertible to StrEnum)"
autonomous: true
requirements: [MOD-03, MOD-04]

must_haves:
  truths:
    - "Every StrEnum subclass in src/ is decorated with @unique"
    - "All convertible Literal[str,...] unions are replaced with @unique StrEnum classes"
  artifacts:
    - path: "all StrEnum definitions"
      provides: "@unique enforcement"
      contains: "@unique"
    - path: "converted Literal files"
      provides: "StrEnum classes replacing Literal unions"
      contains: "class.*StrEnum"
  key_links:
    - from: "Pydantic model fields"
      to: "StrEnum types"
      via: "type annotation"
      pattern: "StrEnum"
---

<objective>
Add @unique to all StrEnum classes and convert eligible Literal[str,...] unions to @unique StrEnum classes.

Purpose: Enforce enum uniqueness at class definition time and use proper enum types instead of string literal unions.
Output: All StrEnum have @unique; ~60-70 Literal unions converted to StrEnum.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-python313-modernization/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @unique to all StrEnum classes missing it</name>
  <read_first>
    flext-core/src/flext_core/_constants/
  </read_first>
  <files>All files with StrEnum classes missing @unique decorator</files>
  <action>
    1. Find all StrEnum class definitions WITHOUT @unique decorator.
       Use ast-grep or Grep to find `class X(StrEnum):` patterns, then check preceding line for `@unique`.

       Research found ~5 StrEnum classes missing @unique out of 297 total.

    2. For each missing @unique:
       ```python
       # Before
       class Status(StrEnum):
           ACTIVE = "active"

       # After
       from enum import StrEnum, unique

       @unique
       class Status(StrEnum):
           ACTIVE = "active"
       ```

    3. Ensure `unique` is imported from `enum` in each file. Add to existing `from enum import StrEnum` line.

    4. Run `make lint` on each affected project after changes.

    Per MOD-03: 147+ StrEnum classes decorated with @unique (research found 297, ~292 already have it).
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && .venv/bin/python -c "
import subprocess, re, sys

# Count StrEnum classes
r1 = subprocess.run(['grep', '-rn', 'class.*StrEnum', '--include=*.py', '-c', '.'], capture_output=True, text=True)
# Count @unique decorators before StrEnum
r2 = subprocess.run(['grep', '-rn', '@unique', '--include=*.py', '-c', '.'], capture_output=True, text=True)

# Better check: find StrEnum without @unique on previous line
r3 = subprocess.run(['grep', '-rn', 'class.*StrEnum', '--include=*.py', '.'], capture_output=True, text=True)
strenum_count = len([l for l in r3.stdout.strip().split('\n') if l and '/src/' in l and 'test' not in l.lower()])
print(f'StrEnum classes in src/: {strenum_count}')
print('Run ast-grep verification for precise count')
"</automated>
  </verify>
  <acceptance_criteria>
    - Every `class X(StrEnum):` in `*/src/**/*.py` has `@unique` on the preceding line
    - `from enum import StrEnum, unique` (or equivalent) present in all affected files
    - `make lint` passes on affected projects
    - No duplicate enum values (would cause @unique to raise ValueError at import time)
  </acceptance_criteria>
  <done>All StrEnum classes in src/ decorated with @unique; linters pass; imports clean.</done>
</task>

<task type="auto">
  <name>Task 2: Convert eligible Literal[str,...] unions to @unique StrEnum</name>
  <read_first>
    flext-core/src/flext_core/_typings/
    flext-cli/src/flext_cli/
    flext-infra/src/flext_infra/
  </read_first>
  <files>Files containing Literal[str,...] type aliases convertible to StrEnum</files>
  <action>
    1. Find all Literal[str,...] occurrences in `*/src/**/*.py`:
       Use Grep for `Literal\[` patterns. Research found 108 occurrences across 44 files.

    2. TRIAGE each occurrence. Convert ONLY if:
       - It represents a fixed set of domain values (e.g., OutputFormat, LogLevel, Status)
       - It's a type alias (`type X = Literal["a", "b"]`) or standalone annotation
       - It has 2+ string values

       Do NOT convert:
       - Single-value Literals used for overloading (`Literal["str"]`)
       - Type discriminators in function signatures (`mode: Literal["sync", "async"]` used for overload dispatch)
       - Literals inside `Annotated[...]` or complex union types where StrEnum breaks typing
       - Literals in frozen `_utilities/*` directory

    3. For each convertible Literal:
       ```python
       # Before (in typings.py or local file)
       type OutputFormatLiteral = Literal["json", "yaml", "csv"]

       # After (in appropriate constants or models file)
       from enum import StrEnum, unique

       @unique
       class OutputFormat(StrEnum):
           JSON = "json"
           YAML = "yaml"
           CSV = "csv"
       ```

    4. Update ALL references to the old Literal type alias to use the new StrEnum class.
       Pydantic fields typed as the old Literal will accept StrEnum values since StrEnum inherits from str.

    5. Run `.venv/bin/pytest tests/ -x --no-header -q` on each affected project to verify Pydantic serialization still works.

    6. Run `make lint` and `.venv/bin/pyrefly check` on affected projects.

    Per MOD-04: 70 Literal[str,...] unions convertible to StrEnum converted.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && make lint</automated>
  </verify>
  <acceptance_criteria>
    - Convertible Literal[str,...] unions replaced with @unique StrEnum classes
    - All references updated (no dangling type alias references)
    - Pydantic models accepting the new StrEnum types validate correctly
    - Non-convertible Literals (overloads, discriminators, single-value) left as-is with brief comment if ambiguous
    - `make lint` passes workspace-wide
  </acceptance_criteria>
  <done>Eligible Literal unions converted to StrEnum; all references updated; tests and linters pass.</done>
</task>

</tasks>

<verification>
- `make lint` passes workspace-wide
- Every StrEnum in src/ has @unique
- Converted Literal→StrEnum types work with Pydantic serialization
- No import errors from removed type aliases
</verification>

<success_criteria>
MOD-03 and MOD-04 satisfied: all StrEnum have @unique, convertible Literal unions are now StrEnum classes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-python313-modernization/04-02-SUMMARY.md`
</output>
