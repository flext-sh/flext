---
phase: 02-architecture-solid
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - "flext-core/src/flext_core/_utilities/*.py"
  - "flext-core/src/flext_core/_models/*.py"
  - "flext-cli/src/flext_cli/*.py"
  - "flext-auth/src/flext_auth/*.py"
  - "flext-infra/src/flext_infra/*.py"
autonomous: true
requirements:
  - ARCH-06

must_haves:
  truths:
    - "~100 inline TypeAdapter() instantiations are cached as ClassVar on owning class"
    - "~40 already-cached instances are unchanged"
    - "5 identified dynamic instances are accepted (cannot be cached)"
  artifacts:
    - path: "flext-core/src/flext_core/_utilities/*.py"
      provides: "Cached TypeAdapter instances as ClassVar"
  key_links:
    - from: "hot-path methods"
      to: "ClassVar[TypeAdapter[T]]"
      via: "class-level cache attribute"
      pattern: "ClassVar\\[TypeAdapter"
---

<objective>
Cache ~100 inline TypeAdapter() instantiations as ClassVar on owning classes.

Purpose: ARCH-06 eliminates repeated TypeAdapter construction in hot paths. The sisyphus plan audited 451 TypeAdapter matches: ~100 inline (hot), ~40 already cached (good), 5 dynamic (accepted).
Output: Zero inline TypeAdapter() in hot-path functions, all cached as ClassVar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-architecture-solid/02-CONTEXT.md
@.planning/phases/02-architecture-solid/02-RESEARCH.md
@.sisyphus/plans/pydantic-v2-advanced-modernization.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cache TypeAdapter instances in flext-core</name>
  <files>
    flext-core/src/flext_core/_utilities/*.py,
    flext-core/src/flext_core/_models/*.py,
    flext-core/src/flext_core/service.py,
    flext-core/src/flext_core/dispatcher.py
  </files>
  <read_first>
    .sisyphus/plans/pydantic-v2-advanced-modernization.md (TypeAdapter audit: 451 matches, inline vs cached breakdown),
    .planning/phases/02-architecture-solid/02-CONTEXT.md (D-09 decision),
    flext-core/src/flext_core/_utilities/ (list directory, then read files with TypeAdapter usage)
  </read_first>
  <action>
    Per D-09/ARCH-06: Find all inline `TypeAdapter()` instantiations in flext-core and cache them as ClassVar on the owning class.

    Pattern:
    ```python
    # BEFORE (inline in method)
    def validate(self, data):
        adapter = TypeAdapter(dict[str, str])
        return adapter.validate_python(data)

    # AFTER (cached as ClassVar)
    _str_dict_adapter: ClassVar[TypeAdapter[dict[str, str]]] = TypeAdapter(dict[str, str])

    def validate(self, data):
        return self._str_dict_adapter.validate_python(data)
    ```

    Most repeated: `TypeAdapter(dict[str, object])` — 11 instances across 6 files. Deduplicate where possible.

    Leave ~40 already-cached instances untouched. Accept 5 dynamic instances that cannot be cached (forward references not resolved at class definition time — per D-09 and Research pitfall 5).

    Add `from typing import ClassVar` import where needed.

    After changes: `make check PROJECT=flext-core && make test PROJECT=flext-core`.
  </action>
  <verify>
    <automated>make check PROJECT=flext-core && make test PROJECT=flext-core</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "TypeAdapter(" flext-core/src/ | grep -v "ClassVar" | grep -v "^#"` returns only the ~5 accepted dynamic instances
    - New ClassVar[TypeAdapter[...]] declarations exist for each cached instance
    - `make check PROJECT=flext-core` exits 0
  </acceptance_criteria>
  <done>All cacheable TypeAdapter instances in flext-core cached as ClassVar</done>
</task>

<task type="auto">
  <name>Task 2: Cache TypeAdapter instances in consumer projects</name>
  <files>
    flext-cli/src/flext_cli/*.py,
    flext-auth/src/flext_auth/*.py,
    flext-infra/src/flext_infra/*.py,
    */src/**/*.py (any other consumers with inline TypeAdapter)
  </files>
  <read_first>
    .sisyphus/plans/pydantic-v2-advanced-modernization.md (consumer TypeAdapter counts: flext-cli 8, flext-auth, flext-infra),
    .planning/phases/02-architecture-solid/02-CONTEXT.md (D-09)
  </read_first>
  <action>
    Per D-09/ARCH-06: Find and cache all remaining inline TypeAdapter() instantiations in consumer projects.

    Use grep to identify all inline TypeAdapter() usages:
    ```bash
    grep -rn "TypeAdapter(" */src/ | grep -v "ClassVar" | grep -v ".venv" | grep -v "__init__"
    ```

    For each: apply same ClassVar caching pattern as Task 1. The sisyphus plan identifies flext-cli (8 inline), flext-auth, and flext-infra as primary targets.

    Validate each affected project: `make check PROJECT=<name>`.
  </action>
  <verify>
    <automated>make check PROJECT=flext-cli && make check PROJECT=flext-auth && make check PROJECT=flext-infra</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "TypeAdapter(" */src/ | grep -v "ClassVar" | grep -v ".venv" | grep -v "__init__" | grep -v "^#"` returns only accepted dynamic instances (~5 total)
    - All affected consumer projects pass `make check`
  </acceptance_criteria>
  <done>All cacheable TypeAdapter instances across consumer projects cached as ClassVar</done>
</task>

</tasks>

<verification>
- `grep -rn "TypeAdapter(" */src/ | grep -v "ClassVar" | grep -v ".venv"` -> ~5 dynamic instances only
- All affected projects pass `make check`
</verification>

<success_criteria>
- ~100 inline TypeAdapter() cached as ClassVar
- ~40 already-cached unchanged
- ~5 dynamic accepted
- All projects pass make check
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-solid/02-04-SUMMARY.md`
</output>
