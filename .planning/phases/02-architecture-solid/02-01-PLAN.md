---
phase: 02-architecture-solid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flext-core/src/flext_core/_utilities/guards.py
  - flext-core/src/flext_core/_utilities/introspection.py
  - flext-core/src/flext_core/_models/entity.py
  - flext-core/src/flext_core/container.py
  - flext-core/src/flext_core/registry.py
  - flext-core/src/flext_core/service.py
autonomous: true
requirements:
  - ARCH-04
  - ARCH-05

must_haves:
  truths:
    - "All 6 issubclass() calls in flext-core that reference ABCs are refactored to Protocol-safe patterns"
    - "6 pure ABCs are converted to @runtime_checkable Protocol"
    - "8 template ABCs have Protocol interface extracted to protocols.py while concrete base is retained"
    - "make check PROJECT=flext-core passes with 0 errors"
  artifacts:
    - path: "flext-core/src/flext_core/_protocols/*.py"
      provides: "New Protocol interfaces extracted from template ABCs"
    - path: "flext-core/src/flext_core/_utilities/guards.py"
      provides: "issubclass() calls refactored"
  key_links:
    - from: "flext-core/src/flext_core/_protocols/"
      to: "flext-core/src/flext_core/protocols.py"
      via: "make codegen regenerates __init__.py exports"
      pattern: "@runtime_checkable"
---

<objective>
Refactor issubclass() calls and convert ABCs to Protocols in flext-core.

Purpose: issubclass() on ABCs blocks Protocol conversion (D-04). This plan clears that prerequisite and then performs all ABC->Protocol work in flext-core, enabling DIP enforcement in Plan 02.
Output: flext-core with Protocol-based interfaces, no issubclass() on ABCs, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-architecture-solid/02-CONTEXT.md
@.planning/phases/02-architecture-solid/02-RESEARCH.md
@.sisyphus/plans/protocol-solid-standardization.md
@.sisyphus/plans/pydantic-v2-advanced-modernization.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor issubclass() calls and convert 6 pure ABCs to Protocol</name>
  <files>
    flext-core/src/flext_core/_utilities/guards.py,
    flext-core/src/flext_core/_utilities/introspection.py,
    flext-core/src/flext_core/_models/entity.py,
    flext-core/src/flext_core/container.py,
    flext-core/src/flext_core/registry.py,
    flext-core/src/flext_core/service.py
  </files>
  <read_first>
    .sisyphus/plans/pydantic-v2-advanced-modernization.md (ABC audit with all 6 issubclass sites and 6 pure ABCs listed),
    .sisyphus/plans/protocol-solid-standardization.md (protocol mapping table),
    .planning/phases/02-architecture-solid/02-CONTEXT.md (D-04, D-07, D-08 decisions),
    flext-core/src/flext_core/_utilities/guards.py,
    flext-core/src/flext_core/_utilities/introspection.py,
    flext-core/src/flext_core/_models/entity.py,
    flext-core/src/flext_core/container.py,
    flext-core/src/flext_core/registry.py,
    flext-core/src/flext_core/service.py
  </read_first>
  <action>
    Per D-04: Refactor all 6 issubclass() calls in flext-core identified in the sisyphus plan to Protocol-safe patterns. Replace `issubclass(cls, SomeABC)` with either `isinstance(instance, SomeProtocol)` or hasattr-based checks as appropriate for each site.

    Per D-07: Convert 6 pure ABCs (abstract-only, no concrete methods) to `@runtime_checkable` Protocol classes. For each:
    1. Remove `ABC` base class and `@abstractmethod` decorators
    2. Add `Protocol` base class and `@runtime_checkable` decorator
    3. Keep method signatures as `...` body stubs
    4. Update imports (remove `from abc import ABC, abstractmethod`, add `from typing import Protocol, runtime_checkable`)

    Per D-08: Do NOT modify `p.Base`, `p.HasModelDump`, or `p.Handler` — they are FROZEN.

    After all changes: run `make codegen` to regenerate `__init__.py` exports.
    Validate: `make check PROJECT=flext-core` must exit 0.
  </action>
  <verify>
    <automated>make check PROJECT=flext-core && make test PROJECT=flext-core</automated>
  </verify>
  <acceptance_criteria>
    - Zero `issubclass(` calls that reference any of the 6 converted ABCs in flext-core/src/
    - 6 new `@runtime_checkable` Protocol classes exist (replacing the 6 pure ABCs)
    - `p.Base`, `p.HasModelDump`, `p.Handler` are unchanged (FROZEN per D-08)
    - `make codegen` has been run (no stale __init__.py exports)
    - `make check PROJECT=flext-core` exits 0
  </acceptance_criteria>
  <done>6 issubclass() sites refactored, 6 pure ABCs converted to @runtime_checkable Protocol, flext-core passes check+test</done>
</task>

<task type="auto">
  <name>Task 2: Extract Protocol interfaces from 8 template ABCs</name>
  <files>
    flext-core/src/flext_core/_protocols/*.py,
    flext-core/src/flext_core/_models/*.py,
    flext-core/src/flext_core/protocols.py
  </files>
  <read_first>
    .sisyphus/plans/pydantic-v2-advanced-modernization.md (identifies all 8 template ABCs),
    .planning/phases/02-architecture-solid/02-CONTEXT.md (D-07 decision),
    flext-core/src/flext_core/_protocols/ (existing protocol files — list directory first),
    flext-core/src/flext_core/protocols.py
  </read_first>
  <action>
    Per D-07: For each of 8 template ABCs (ABCs with both abstract methods AND concrete implementations):
    1. Extract the abstract method signatures into a new `@runtime_checkable` Protocol in the appropriate `_protocols/` file
    2. Keep the concrete base class with its implementations, but remove `ABC` inheritance
    3. The concrete base should raise `NotImplementedError` instead of using `@abstractmethod`
    4. Ensure the Protocol is exported via the MRO namespace chain (`FlextProtocols` facade)

    Per D-08: Do NOT modify `p.Base`, `p.HasModelDump`, `p.Handler`.

    After all changes: run `make codegen` to regenerate exports.
    Validate: `make check PROJECT=flext-core && make test PROJECT=flext-core` must exit 0.
  </action>
  <verify>
    <automated>make check PROJECT=flext-core && make test PROJECT=flext-core</automated>
  </verify>
  <acceptance_criteria>
    - 8 new Protocol interfaces exist in flext-core/src/flext_core/_protocols/ files
    - Each Protocol has `@runtime_checkable` decorator
    - Original concrete base classes retain their implementation methods
    - Original concrete base classes no longer inherit from `ABC`
    - `make codegen` has been run
    - `make check PROJECT=flext-core` exits 0
  </acceptance_criteria>
  <done>8 template ABCs have Protocol interfaces extracted, concrete bases retained, flext-core passes check+test</done>
</task>

</tasks>

<verification>
- `make check PROJECT=flext-core` exits 0
- `make test PROJECT=flext-core` exits 0
- grep for `from abc import ABC` in flext-core/src/ shows reduction of 14 (6 pure + 8 template)
- grep for `@runtime_checkable` in flext-core/src/ shows increase of 14 new Protocols
</verification>

<success_criteria>
- 6 issubclass() calls refactored to Protocol-safe patterns
- 6 pure ABCs fully converted to @runtime_checkable Protocol
- 8 template ABCs have Protocol interface extracted + concrete base retained
- p.Base, p.HasModelDump, p.Handler untouched (FROZEN per D-08)
- flext-core passes make check + make test
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-solid/02-01-SUMMARY.md`
</output>
