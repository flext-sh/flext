---
phase: 02-architecture-solid
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - flext-core/src/flext_core/service.py
  - flext-core/src/flext_core/container.py
  - flext-core/src/flext_core/context.py
  - flext-core/src/flext_core/dispatcher.py
  - flext-plugin/src/flext_plugin/*.py
  - flext-target-ldap/src/flext_target_ldap/*.py
  - flext-ldap/src/flext_ldap/*.py
  - flext-quality/src/flext_quality/*.py
  - flext-tests/src/flext_tests/*.py
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "All public API type annotations use protocol types (p.Context, p.DI, p.Config, p.StructlogLogger) not concrete types"
    - "Factory methods that must return concrete types keep concrete return types"
    - "__init__.py exports are unchanged (they export concrete classes correctly)"
  artifacts:
    - path: "flext-core/src/flext_core/service.py"
      provides: "DIP-compliant public API signatures"
    - path: "flext-core/src/flext_core/container.py"
      provides: "DIP-compliant public API signatures"
  key_links:
    - from: "consumer projects"
      to: "flext-core protocols"
      via: "p.Context, p.DI, p.Config, p.StructlogLogger imports"
      pattern: "p\\.Context|p\\.DI|p\\.Config|p\\.StructlogLogger"
---

<objective>
Replace all concrete type annotations in public APIs with protocol types across all 33 projects.

Purpose: Enforces Dependency Inversion Principle (ARCH-01). Public APIs must depend on abstractions (protocols), not concretions. The sisyphus plan identified 12 DIP violations in flext-core and 23 in consumers.
Output: Zero concrete type leakage in public API annotations (except factory methods, __init__.py exports, and internal implementation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-architecture-solid/02-CONTEXT.md
@.planning/phases/02-architecture-solid/02-RESEARCH.md
@.planning/phases/02-architecture-solid/02-01-SUMMARY.md
@.sisyphus/plans/protocol-solid-standardization.md
</context>

<interfaces>
Protocol mapping (from sisyphus plan):

| Concrete | Protocol | Usage |
|----------|----------|-------|
| `FlextContext` | `p.Context` | Parameter and return types |
| `FlextContainer` | `p.DI` | Parameter types (not factory returns) |
| `FlextSettings` | `p.Config` | Parameter types |
| `FlextLogger` | `p.StructlogLogger` | Parameter types |
| `FlextDispatcher` | `p.CommandBus` | Parameter types |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DIP enforcement in flext-core public APIs</name>
  <files>
    flext-core/src/flext_core/service.py,
    flext-core/src/flext_core/container.py,
    flext-core/src/flext_core/context.py,
    flext-core/src/flext_core/dispatcher.py
  </files>
  <read_first>
    .sisyphus/plans/protocol-solid-standardization.md (12 DIP violations in flext-core with exact locations),
    flext-core/src/flext_core/service.py,
    flext-core/src/flext_core/container.py,
    flext-core/src/flext_core/context.py,
    flext-core/src/flext_core/dispatcher.py
  </read_first>
  <action>
    Per D-01/ARCH-01: Replace concrete type annotations with protocol types in all public method signatures in flext-core. Use ast-grep for mechanical replacement:

    - `FlextContext` -> `p.Context` in parameters and non-factory return types
    - `FlextContainer` -> `p.DI` in parameters
    - `FlextSettings` -> `p.Config` in parameters
    - `FlextLogger` -> `p.StructlogLogger` in parameters
    - `FlextDispatcher` -> `p.CommandBus` in parameters

    Exceptions (do NOT change):
    - Factory methods: `FlextContext.create()` keeps concrete `FlextContext` return type
    - `__init__.py` exports (export concrete classes — correct)
    - Internal implementation within method bodies (only signatures change)
    - isinstance() checks inside method bodies (these use concrete types correctly)

    After changes: `make codegen` then `make check PROJECT=flext-core`.
  </action>
  <verify>
    <automated>make check PROJECT=flext-core && make test PROJECT=flext-core</automated>
  </verify>
  <acceptance_criteria>
    - Zero `: FlextContext` in public method parameter annotations in service.py, container.py, context.py, dispatcher.py (except factory return types)
    - Zero `: FlextContainer` in public method parameter annotations
    - Zero `: FlextSettings` in public method parameter annotations
    - Zero `: FlextLogger` in public method parameter annotations
    - `make check PROJECT=flext-core` exits 0
  </acceptance_criteria>
  <done>12 DIP violations in flext-core resolved, all public APIs use protocol types</done>
</task>

<task type="auto">
  <name>Task 2: DIP enforcement across consumer projects</name>
  <files>
    flext-plugin/src/flext_plugin/*.py,
    flext-target-ldap/src/flext_target_ldap/*.py,
    flext-ldap/src/flext_ldap/*.py,
    flext-quality/src/flext_quality/*.py,
    flext-tests/src/flext_tests/*.py
  </files>
  <read_first>
    .sisyphus/plans/protocol-solid-standardization.md (23 DIP violations in consumers with exact locations),
    .planning/phases/02-architecture-solid/02-CONTEXT.md
  </read_first>
  <action>
    Per ARCH-01: Use ast-grep workspace-wide to replace concrete type annotations with protocol types in all consumer project public APIs. The sisyphus plan identifies 23 concrete type annotations across 5 consumer projects (flext-plugin, flext-target-ldap, flext-ldap, flext-quality, flext-tests).

    Apply same protocol mapping as Task 1. Use ast-grep patterns:
    ```bash
    sg --pattern '$NAME: FlextContext' --rewrite '$NAME: p.Context' --lang python
    sg --pattern '$NAME: FlextContainer' --rewrite '$NAME: p.DI' --lang python
    sg --pattern '$NAME: FlextSettings' --rewrite '$NAME: p.Config' --lang python
    sg --pattern '$NAME: FlextLogger' --rewrite '$NAME: p.StructlogLogger' --lang python
    ```

    For each affected project: ensure `p` is imported from the project's own namespace root (e.g., `from flext_plugin import p`), not from `flext_core`.

    After changes: run `make codegen` then `make check PROJECT=<name>` for each affected project.
  </action>
  <verify>
    <automated>make check PROJECT=flext-plugin && make check PROJECT=flext-target-ldap && make check PROJECT=flext-ldap && make check PROJECT=flext-quality && make check PROJECT=flext-tests</automated>
  </verify>
  <acceptance_criteria>
    - Zero `: FlextContext` in public API annotations across all consumer src/ dirs (excluding __init__.py)
    - Zero `: FlextContainer` in public API annotations across all consumer src/ dirs
    - Zero `: FlextSettings` in public API annotations across all consumer src/ dirs
    - Zero `: FlextLogger` in public API annotations across all consumer src/ dirs
    - All 5 consumer projects pass `make check`
  </acceptance_criteria>
  <done>23 DIP violations in consumer projects resolved, all pass make check</done>
</task>

</tasks>

<verification>
- `grep -rn ": FlextContext\|: FlextContainer\|: FlextSettings\|: FlextLogger" */src/ | grep -v __init__ | grep -v "# factory"` returns 0 matches
- All affected projects pass `make check`
</verification>

<success_criteria>
- Zero concrete type leakage in public API annotations across all 33 projects
- Factory methods retain concrete return types
- __init__.py exports unchanged
- All affected projects pass make check
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-solid/02-02-SUMMARY.md`
</output>
