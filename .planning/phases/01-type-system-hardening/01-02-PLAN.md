---
phase: 01-type-system-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - flext-core/src/flext_infra/**/*.py
  - flext-core/src/flext_tests/**/*.py
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - TYPE-04
  - TYPE-05
  - TYPE-06

must_haves:
  truths:
    - "make check PROJECT=flext-infra CHECK_GATES=pyrefly,pyright exits 0"
    - "make check PROJECT=flext-tests CHECK_GATES=pyrefly,pyright exits 0"
    - "Zero cast() calls in flext-infra and flext-tests src/"
    - "Zero __class__ is / __class__ not in comparisons in both projects"
    - "Zero bare object/Any annotations in both projects"
    - "Zero # type: ignore in both projects"
  artifacts:
    - path: "flext-core/src/flext_infra/"
      provides: "Type-clean infrastructure library"
    - path: "flext-core/src/flext_tests/"
      provides: "Type-clean test utilities"
  key_links:
    - from: "flext-core/src/flext_infra/"
      to: "flext-core/src/flext_core/"
      via: "imports from clean flext-core foundation (Wave 1 output)"
      pattern: "from flext_core import"
    - from: "flext-core/src/flext_tests/"
      to: "flext-core/src/flext_core/"
      via: "imports from clean flext-core foundation"
      pattern: "from flext_core import"
---

<objective>
Wave 2: Eliminate ALL pyrefly errors, pyright errors, and typing shortcuts in flext-infra and flext-tests.

Purpose: flext-infra and flext-tests sit between flext-core (now clean from Wave 1) and all consumer projects. They must be clean before consumers can be fixed, as consumers import from both.

Output: Both projects pass `make check PROJECT=X CHECK_GATES=pyrefly,pyright` with 0 errors. Zero typing shortcuts remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-type-system-hardening/01-CONTEXT.md
@.planning/phases/01-type-system-hardening/01-RESEARCH.md
@.planning/phases/01-type-system-hardening/01-01-SUMMARY.md

Source references (per D-03):
@.sisyphus/plans/strict-typing-execution-plan.md
@.sisyphus/plans/bare-object-elimination.md
@.sisyphus/plans/pyright-zero-errors.md
@.sisyphus/evidence/NARROWING-SCAN-SUMMARY.md

Skills:
@.claude/skills/flext-strict-typing/SKILL.md
@.claude/skills/flext-pyrefly-typecheck-fix/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure current error counts for flext-infra and flext-tests</name>
  <files>flext-core/src/flext_infra/, flext-core/src/flext_tests/</files>
  <read_first>
    - .planning/phases/01-type-system-hardening/01-01-SUMMARY.md (Wave 1 outcomes — what changed in flext-core)
    - .sisyphus/evidence/NARROWING-SCAN-SUMMARY.md (violation inventory for infra/tests)
    - .sisyphus/plans/bare-object-elimination.md (object annotation replacement map)
  </read_first>
  <action>
**Step 1: Pull Wave 1 changes.**
Ensure the working tree includes all Wave 1 flext-core fixes. If Wave 1 was committed on the same branch, this is automatic. Otherwise `make status` to verify.

**Step 2: Capture per-project baselines.**
Run for each project:
```
make check PROJECT=flext-infra CHECK_GATES=pyrefly,pyright
make check PROJECT=flext-tests CHECK_GATES=pyrefly,pyright
```
Record error counts. Also scan for typing shortcuts:
- `sg --pattern 'cast($$$)' --lang python flext-core/src/flext_infra/`
- `sg --pattern '$X.__class__ is $Y' --lang python flext-core/src/flext_infra/`
- `sg --pattern 'cast($$$)' --lang python flext-core/src/flext_tests/`
- `sg --pattern '$X.__class__ is $Y' --lang python flext-core/src/flext_tests/`

**Step 3: Identify cascade impacts.**
Wave 1 may have changed flext-core signatures. Check if flext-infra/flext-tests have new errors introduced by the Wave 1 type tightening. These are priority fixes — address cascade errors before pre-existing errors.
  </action>
  <verify>
    <automated>make check PROJECT=flext-infra CHECK_GATES=pyrefly 2>&1 | tail -5 && make check PROJECT=flext-tests CHECK_GATES=pyrefly 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Error counts for both projects recorded (exact numbers captured in executor output)
    - Cascade impact from Wave 1 identified (new errors vs pre-existing)
    - Typing shortcut scan results captured for both projects
  </acceptance_criteria>
  <done>Baselines for flext-infra and flext-tests captured. Cascade impact from Wave 1 identified.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate all type errors and typing shortcuts in flext-infra and flext-tests</name>
  <files>flext-core/src/flext_infra/**/*.py, flext-core/src/flext_tests/**/*.py</files>
  <read_first>
    - flext-core/src/flext_infra/ (list all .py files — understand module structure)
    - flext-core/src/flext_tests/ (list all .py files)
    - .sisyphus/plans/strict-typing-execution-plan.md (ast-grep rules for __class__ and cast)
    - .sisyphus/plans/bare-object-elimination.md (object replacement map)
    - .sisyphus/plans/pyright-zero-errors.md (root causes affecting infra/tests)
    - flext-core/src/flext_core/typings.py (available t.* contracts from Wave 1)
    - flext-core/src/flext_core/_utilities/guards.py (available TypeGuard functions from Wave 1)
  </read_first>
  <action>
Per D-02, fix ALL error categories for both projects in a single pass. Apply the same category order as Wave 1:

**Category A: __class__ rewrites (TYPE-06)**
Apply ast-grep rules A1-A4 to `flext-core/src/flext_infra/` and `flext-core/src/flext_tests/`. Same exclusion rules as Wave 1 for domain.py Option C sites (unlikely in infra/tests but check). Use `same_type()` utility from Wave 1 if needed.

**Category B: cast() removal (TYPE-05)**
Remove all `cast()` calls. Replace with isinstance narrowing or TypeGuard functions from `u.Guards.*`. Refer to NARROWING-SCAN-SUMMARY.md for specific violations in these projects.

**Category C: bare object/Any/type:ignore elimination (TYPE-02, TYPE-03, TYPE-04)**
For each `object` annotation: replace with specific `t.*` contract.
For each `Any` import/annotation: replace with specific `t.*` contract.
For each `# type: ignore`: remove and fix the underlying issue.

**Category D: pyright errors**
Fix cascade errors from Wave 1 tightening first, then pre-existing pyright issues:
- Missing type annotations (add explicit types)
- Incompatible method overrides (fix signatures)
- Argument type mismatches (widen parameter types or narrow call sites)

**Category E: pyrefly errors (remaining)**
Fix any remaining pyrefly errors after categories A-D. Apply patterns from flext-pyrefly-typecheck-fix skill:
- Logger typing drift -> `p.Logger`
- RootModel coercion -> `.root`
- Mapping mutation -> `MutableMapping` or `dict()` copy

**After all fixes:**
- Run `make gen` if any facade exports changed.
- Run `make test PROJECT=flext-infra` and `make test PROJECT=flext-tests` for non-regression.
- Run `make pyre` to verify no global regression from these changes.
  </action>
  <verify>
    <automated>make check PROJECT=flext-infra CHECK_GATES=pyrefly,pyright 2>&1 | tail -5 && make check PROJECT=flext-tests CHECK_GATES=pyrefly,pyright 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `make check PROJECT=flext-infra CHECK_GATES=pyrefly,pyright` exits 0
    - `make check PROJECT=flext-tests CHECK_GATES=pyrefly,pyright` exits 0
    - `sg --pattern 'cast($$$)' --lang python flext-core/src/flext_infra/` returns 0 matches
    - `sg --pattern 'cast($$$)' --lang python flext-core/src/flext_tests/` returns 0 matches
    - `sg --pattern '$X.__class__ is $Y' --lang python flext-core/src/flext_infra/` returns 0 matches
    - `sg --pattern '$X.__class__ is $Y' --lang python flext-core/src/flext_tests/` returns 0 matches
    - `make pol` exits 0 (no policy violations introduced)
    - `make test PROJECT=flext-infra` passes
    - `make test PROJECT=flext-tests` passes
    - `make pyre` error count is less than or equal to post-Wave-1 count (no regression)
  </acceptance_criteria>
  <done>flext-infra and flext-tests have zero pyrefly errors, zero pyright errors, zero typing shortcuts. All tests pass. Infrastructure layer is clean for consumer waves.</done>
</task>

</tasks>

<verification>
1. `make check PROJECT=flext-infra CHECK_GATES=pyrefly,pyright` exits 0
2. `make check PROJECT=flext-tests CHECK_GATES=pyrefly,pyright` exits 0
3. `make pol` exits 0
4. `make test PROJECT=flext-infra` passes
5. `make test PROJECT=flext-tests` passes
6. `make pyre` error count shows no regression (only reduction)
</verification>

<success_criteria>
- Both flext-infra and flext-tests are type-clean: 0 pyrefly errors, 0 pyright errors
- No typing shortcuts remain in either project
- All tests pass without behavioral regressions
- Infrastructure layer is clean for Wave 3 (flext-cli) and Wave 4 (consumers)
</success_criteria>

<output>
After completion, create `.planning/phases/01-type-system-hardening/01-02-SUMMARY.md`
</output>
