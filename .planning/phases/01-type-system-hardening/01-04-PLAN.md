---
phase: 01-type-system-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - flext-quality/src/**/*.py
  - flext-observability/src/**/*.py
  - flext-ldif/src/**/*.py
  - flext-meltano/src/**/*.py
  - gruponos-meltano-native/src/**/*.py
  - flext-api/src/**/*.py
  - flext-auth/src/**/*.py
  - flext-web/src/**/*.py
  - flext-ldap/src/**/*.py
  - flext-db-oracle/src/**/*.py
  - flext-dbt-oracle/src/**/*.py
  - flext-dbt-trino/src/**/*.py
  - flext-grpc/src/**/*.py
  - flext-oracle-oic/src/**/*.py
  - flext-oracle-wms/src/**/*.py
  - flext-tap-oracle/src/**/*.py
  - flext-target-oracle/src/**/*.py
  - flext-target-mssql/src/**/*.py
  - flext-target-trino/src/**/*.py
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - TYPE-04
  - TYPE-05
  - TYPE-06

must_haves:
  truths:
    - "make pyre exits 0 with 0 errors across ALL projects (excluding algar-oud-mig)"
    - "make check CHECK_GATES=pyright exits 0 for every consumer project"
    - "Zero cast() calls outside flext-core/result.py across entire repo"
    - "Zero __class__ is / __class__ not in comparisons across entire repo"
    - "Zero bare object/Any annotations across entire repo"
    - "Zero # type: ignore across entire repo"
    - "make pol exits 0"
  artifacts:
    - path: "all consumer project src/ directories"
      provides: "Type-clean consumer projects"
  key_links:
    - from: "consumer projects"
      to: "flext-core, flext-infra, flext-tests"
      via: "imports from clean foundation (Waves 1-3)"
      pattern: "from flext_core import|from flext_infra import"
---

<objective>
Wave 4: Eliminate ALL pyrefly errors, pyright errors, and typing shortcuts in the remaining ~27 consumer projects (excluding algar-oud-mig).

Purpose: With flext-core, flext-infra, flext-tests, and flext-cli all clean from Waves 1-3, the remaining consumers can now be fixed. Many of these are leaf nodes with low error counts that can be processed efficiently.

Output: `make pyre` exits 0 for the entire repo. All consumer projects pass pyright. Zero typing shortcuts remain anywhere. This completes the bulk of TYPE-01 through TYPE-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-type-system-hardening/01-CONTEXT.md
@.planning/phases/01-type-system-hardening/01-RESEARCH.md
@.planning/phases/01-type-system-hardening/01-01-SUMMARY.md
@.planning/phases/01-type-system-hardening/01-02-SUMMARY.md
@.planning/phases/01-type-system-hardening/01-03-SUMMARY.md

Source references (per D-03):
@.sisyphus/plans/strict-typing-execution-plan.md
@.sisyphus/plans/bare-object-elimination.md
@.sisyphus/plans/pyright-zero-errors.md
@.sisyphus/evidence/NARROWING-SCAN-SUMMARY.md

Skills:
@.claude/skills/flext-strict-typing/SKILL.md
@.claude/skills/flext-pyrefly-typecheck-fix/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch-fix high-error consumers (flext-quality, flext-observability, flext-ldif, flext-meltano, gruponos-meltano-native, flext-api)</name>
  <files>
    flext-quality/src/**/*.py,
    flext-observability/src/**/*.py,
    flext-ldif/src/**/*.py,
    flext-meltano/src/**/*.py,
    gruponos-meltano-native/src/**/*.py,
    flext-api/src/**/*.py
  </files>
  <read_first>
    - .planning/phases/01-type-system-hardening/01-01-SUMMARY.md (Wave 1 — t.* contracts created)
    - .planning/phases/01-type-system-hardening/01-02-SUMMARY.md (Wave 2 — infra patterns)
    - .planning/phases/01-type-system-hardening/01-03-SUMMARY.md (Wave 3 — cli patterns)
    - .sisyphus/evidence/NARROWING-SCAN-SUMMARY.md (per-project violation counts):
      * flext-quality: 298 pyrefly errors, 15 cast violations
      * flext-observability: 280 pyrefly errors
      * flext-ldif: 28 __class__ violations, 6 cast
      * flext-meltano: 18 cast violations
      * gruponos-meltano-native: 21 __class__, 3 cast
      * flext-api: 3 cast, 2 __class__, 2 isinstance(value, t.Primitives) misuse
    - flext-core/src/flext_core/typings.py (t.* contracts and t.PRIMITIVES_TYPES constant)
    - flext-core/src/flext_core/_utilities/guards.py (TypeGuard functions)
  </read_first>
  <action>
Process the 6 highest-error consumer projects. Recommended order (by error count, decreasing):

**1. flext-quality (298 errors, 15 cast)**
- Run `make check PROJECT=flext-quality CHECK_GATES=pyrefly,pyright` for baseline.
- Apply ast-grep __class__ rules A1-A4, then cast removal, then object/Any elimination.
- Fix pyrefly/pyright errors iteratively.
- `make test PROJECT=flext-quality` for non-regression.

**2. flext-observability (280 errors)**
- Same process. This project likely has many missing annotations.
- Focus on `reportUnknownVariableType` (add explicit annotations).

**3. flext-ldif (28 __class__, 6 cast)**
- High __class__ count — ast-grep rules will handle most mechanically.
- Verify subclass behavior after isinstance conversion.

**4. flext-meltano (18 cast)**
- Focus on cast removal — replace with TypeGuard or isinstance narrowing.

**5. gruponos-meltano-native (21 __class__, 3 cast)**
- High __class__ count — mechanical rewrite.

**6. flext-api (3 cast, 2 __class__, isinstance alias misuse)**
- CRITICAL: Fix the 2 `isinstance(value, t.Primitives)` sites:
  * `flext-api/webhook.py:139` -> `isinstance(value, t.PRIMITIVES_TYPES)`
  * `flext-api/utilities.py:130` -> `isinstance(value, t.PRIMITIVES_TYPES)`
  These would CRASH at runtime because `t.Primitives` is a `TypeAliasType`.

**For each project, the fix pattern is identical:**
1. Mechanical ast-grep sweep (__class__ rules A1-A4, cast removal)
2. Object/Any annotation replacement with specific t.* contracts
3. Remove # type: ignore comments and fix underlying issues
4. Fix remaining pyrefly/pyright errors
5. Run `make check PROJECT=X CHECK_GATES=pyrefly,pyright` to confirm 0
6. Run `make test PROJECT=X` for non-regression

**IMPORTANT:** Skip `algar-oud-mig` entirely — EXCLUDED per CONTEXT.md.
  </action>
  <verify>
    <automated>for proj in flext-quality flext-observability flext-ldif flext-meltano gruponos-meltano-native flext-api; do echo "=== $proj ===" && make check PROJECT=$proj CHECK_GATES=pyrefly,pyright 2>&1 | tail -3; done</automated>
  </verify>
  <acceptance_criteria>
    - `make check PROJECT=flext-quality CHECK_GATES=pyrefly,pyright` exits 0
    - `make check PROJECT=flext-observability CHECK_GATES=pyrefly,pyright` exits 0
    - `make check PROJECT=flext-ldif CHECK_GATES=pyrefly,pyright` exits 0
    - `make check PROJECT=flext-meltano CHECK_GATES=pyrefly,pyright` exits 0
    - `make check PROJECT=gruponos-meltano-native CHECK_GATES=pyrefly,pyright` exits 0
    - `make check PROJECT=flext-api CHECK_GATES=pyrefly,pyright` exits 0
    - flext-api/webhook.py does NOT contain `isinstance(value, t.Primitives)` (uses t.PRIMITIVES_TYPES instead)
    - flext-api/utilities.py does NOT contain `isinstance(value, t.Primitives)` (uses t.PRIMITIVES_TYPES instead)
    - `make test` passes for all 6 projects
  </acceptance_criteria>
  <done>6 highest-error consumers are type-clean. All pass pyrefly + pyright with 0 errors. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Batch-fix remaining consumers and achieve repo-wide zero errors</name>
  <files>
    flext-auth/src/**/*.py,
    flext-web/src/**/*.py,
    flext-ldap/src/**/*.py,
    flext-db-oracle/src/**/*.py,
    flext-dbt-oracle/src/**/*.py,
    flext-dbt-trino/src/**/*.py,
    flext-grpc/src/**/*.py,
    flext-oracle-oic/src/**/*.py,
    flext-oracle-wms/src/**/*.py,
    flext-tap-oracle/src/**/*.py,
    flext-target-oracle/src/**/*.py,
    flext-target-mssql/src/**/*.py,
    flext-target-trino/src/**/*.py
  </files>
  <read_first>
    - .sisyphus/evidence/NARROWING-SCAN-SUMMARY.md (per-project violations for remaining consumers)
    - flext-core/src/flext_core/typings.py (available t.* contracts)
    - flext-core/src/flext_core/_utilities/guards.py (TypeGuard functions)
  </read_first>
  <action>
Process ALL remaining consumer projects (everything not yet handled in Waves 1-3 or Task 1). These are generally lower error count projects.

**Approach: Efficient batch processing.**

1. Get the full list of remaining projects by listing project directories and excluding already-handled ones (flext-core, flext-infra, flext-tests, flext-cli, flext-quality, flext-observability, flext-ldif, flext-meltano, gruponos-meltano-native, flext-api) and the EXCLUDED project (algar-oud-mig).

2. For each remaining project, run `make check PROJECT=X CHECK_GATES=pyrefly,pyright`. Skip any that already pass (0 errors).

3. For projects with errors, apply the standard fix pattern:
   - ast-grep __class__ rules A1-A4
   - cast() removal
   - object/Any annotation replacement
   - # type: ignore removal
   - Remaining pyrefly/pyright error fixes
   - `make test PROJECT=X` for non-regression

4. Process projects in decreasing error count order for best ROI.

**Parallelization note:** All `flext-tap-*` and `flext-target-*` projects are leaf nodes with no imports from each other. Process them efficiently — most likely have few errors.

**After all projects done:**
- Run `make pyre` — MUST exit 0 with 0 errors (excluding algar-oud-mig if it's in the scan scope; if so, verify the exclusion).
- Run `make pol` — MUST exit 0.
- This achieves TYPE-01 (repo-wide 0 pyrefly errors) for the first time.
  </action>
  <verify>
    <automated>make pyre 2>&1 | tail -10 && make pol 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `make pyre` exits 0 with 0 errors (or only algar-oud-mig errors if it cannot be excluded from scan)
    - `make pol` exits 0
    - `sg --pattern 'cast($$$)' --lang python` across entire repo shows matches ONLY in `flext-core/src/flext_core/result.py` (and possibly algar-oud-mig which is excluded)
    - `sg --pattern '$X.__class__ is $Y' --lang python` across entire repo returns 0 matches (excluding algar-oud-mig)
    - Every consumer project passes `make check PROJECT=X CHECK_GATES=pyrefly,pyright`
    - `make test` passes for all modified projects
  </acceptance_criteria>
  <done>ALL consumer projects are type-clean. make pyre exits 0 for the entire repo. Zero typing shortcuts remain anywhere. TYPE-01 through TYPE-06 are achieved for all projects.</done>
</task>

</tasks>

<verification>
1. `make pyre` exits 0 with 0 errors across entire repo
2. `make pol` exits 0
3. Every project passes `make check PROJECT=X CHECK_GATES=pyrefly,pyright`
4. Zero cast() outside result.py across entire repo
5. Zero __class__ comparisons across entire repo
6. Zero Any/object/type:ignore across entire repo
7. All tests pass
</verification>

<success_criteria>
- TYPE-01 achieved: `make pyre` exits 0 with 0 errors
- TYPE-02 achieved: Zero # type: ignore
- TYPE-03 achieved: Zero Any imports/annotations
- TYPE-04 achieved: Zero object annotations
- TYPE-05 achieved: Zero cast() outside result.py
- TYPE-06 achieved: Zero __class__ comparisons
- All projects pass both pyrefly and pyright
- All tests pass without behavioral regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-type-system-hardening/01-04-SUMMARY.md`
</output>
