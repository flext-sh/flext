---
phase: 01-type-system-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - flext-cli/src/flext_cli/**/*.py
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - TYPE-04
  - TYPE-05
  - TYPE-06

must_haves:
  truths:
    - "make check PROJECT=flext-cli CHECK_GATES=pyrefly,pyright exits 0"
    - "Zero cast() calls in flext-cli src/"
    - "Zero __class__ is / __class__ not in comparisons in flext-cli"
    - "Zero bare object/Any annotations in flext-cli src/"
    - "Zero # type: ignore in flext-cli"
  artifacts:
    - path: "flext-cli/src/flext_cli/"
      provides: "Type-clean CLI project (largest consumer by error count)"
  key_links:
    - from: "flext-cli/src/flext_cli/"
      to: "flext-core/src/flext_core/"
      via: "imports from clean foundation (Wave 1)"
      pattern: "from flext_core import"
    - from: "flext-cli/src/flext_cli/"
      to: "flext-core/src/flext_infra/"
      via: "imports from clean infrastructure (Wave 2)"
      pattern: "from flext_infra import"
---

<objective>
Wave 3: Eliminate ALL pyrefly errors (~1,419 estimated), pyright errors, and typing shortcuts in flext-cli.

Purpose: flext-cli is the largest consumer by error count (D-04). It gets a dedicated solo plan between the infrastructure wave and the remaining consumers wave. Placing it after infrastructure ensures the dependency foundation is clean.

Output: flext-cli passes both `make check PROJECT=flext-cli CHECK_GATES=pyrefly,pyright` with 0 errors. Zero typing shortcuts remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-type-system-hardening/01-CONTEXT.md
@.planning/phases/01-type-system-hardening/01-RESEARCH.md
@.planning/phases/01-type-system-hardening/01-01-SUMMARY.md
@.planning/phases/01-type-system-hardening/01-02-SUMMARY.md

Source references (per D-03):
@.sisyphus/plans/strict-typing-execution-plan.md
@.sisyphus/plans/bare-object-elimination.md
@.sisyphus/plans/pyright-zero-errors.md
@.sisyphus/evidence/NARROWING-SCAN-SUMMARY.md

Skills:
@.claude/skills/flext-strict-typing/SKILL.md
@.claude/skills/flext-pyrefly-typecheck-fix/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure baseline and categorize flext-cli errors</name>
  <files>flext-cli/src/flext_cli/</files>
  <read_first>
    - .planning/phases/01-type-system-hardening/01-01-SUMMARY.md (Wave 1 changes to flext-core)
    - .planning/phases/01-type-system-hardening/01-02-SUMMARY.md (Wave 2 changes to infra/tests)
    - .sisyphus/evidence/NARROWING-SCAN-SUMMARY.md (violation inventory for flext-cli)
    - flext-cli/src/flext_cli/ (list all .py files to understand module structure)
  </read_first>
  <action>
**Step 1: Capture flext-cli baseline after Waves 1-2.**
Run:
```
make check PROJECT=flext-cli CHECK_GATES=pyrefly,pyright
```
Record error counts. The ~1,419 estimate is from pre-Wave 0 — actual count may differ after Wave 1+2 foundation improvements.

**Step 2: Scan for typing shortcuts.**
```
sg --pattern 'cast($$$)' --lang python flext-cli/src/
sg --pattern '$X.__class__ is $Y' --lang python flext-cli/src/
sg --pattern '$X.__class__ is not $Y' --lang python flext-cli/src/
sg --pattern '$X.__class__ in {$$$Y}' --lang python flext-cli/src/
sg --pattern '$X.__class__ not in {$$$Y}' --lang python flext-cli/src/
```

**Step 3: Identify cascade vs pre-existing errors.**
Waves 1-2 may have changed upstream signatures. Cascade errors (new errors from tighter upstream types) are priority — they indicate call sites passing wider types than now accepted.

**Step 4: Categorize errors by type** to plan the fix order:
- pyrefly errors by error code/category
- pyright errors by rule (reportArgumentType, reportUnknownVariableType, etc.)
- Typing shortcut count (cast, __class__, object, Any, type:ignore)
  </action>
  <verify>
    <automated>make check PROJECT=flext-cli CHECK_GATES=pyrefly 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - flext-cli pyrefly error count captured (exact number in executor output)
    - flext-cli pyright error count captured
    - Typing shortcut counts captured (cast, __class__, object, Any, type:ignore)
    - Cascade vs pre-existing errors categorized
  </acceptance_criteria>
  <done>flext-cli error baseline captured with categorization. Fix strategy clear.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate all type errors and typing shortcuts in flext-cli</name>
  <files>flext-cli/src/flext_cli/**/*.py</files>
  <read_first>
    - flext-cli/src/flext_cli/ (all .py files — focus on files with highest error counts)
    - .sisyphus/plans/strict-typing-execution-plan.md (rewrite rules)
    - .sisyphus/plans/bare-object-elimination.md (object replacement map)
    - .sisyphus/plans/pyright-zero-errors.md (root causes)
    - flext-core/src/flext_core/typings.py (available t.* contracts)
    - flext-core/src/flext_core/_utilities/guards.py (available TypeGuard functions)
    - flext-core/src/flext_core/_utilities/domain.py (same_type utility from Wave 1)
  </read_first>
  <action>
Per D-02, fix ALL error categories for flext-cli in a single pass. This is the largest consumer (~1,419 pyrefly errors estimated). Work systematically:

**Strategy: Start with highest-impact mechanical fixes, then manual fixes.**

**Phase 1: Mechanical fixes (ast-grep batch)**
1. Apply __class__ rewrite rules A1-A4 across all flext-cli/src/ files.
   - Dry-run first to verify scope.
   - Exclude any Option C sites (exact-type identity comparisons).
2. Apply cast() removal — replace with isinstance narrowing or TypeGuard.
3. Run `make check PROJECT=flext-cli CHECK_GATES=pyrefly` after mechanical pass — capture intermediate count.

**Phase 2: Type annotation fixes**
1. Replace all `object` annotations with specific `t.*` contracts.
2. Replace all `Any` imports/annotations with specific `t.*` contracts.
3. Remove all `# type: ignore` comments and fix underlying issues.
4. Add missing type annotations (address `reportUnknownVariableType`).

**Phase 3: Semantic fixes**
1. Fix pyright `reportArgumentType` — narrow arguments or widen parameter types at call sites.
2. Fix incompatible method overrides — match parent signatures.
3. Fix pyrefly-specific issues:
   - Logger typing: annotate as `p.Logger`
   - RootModel coercion: use `.root`
   - Mapping mutation: use `MutableMapping` or `dict()` copy
   - `r.ok(None)` patterns: fix to correct type

**Phase 4: Iteration**
Run both checkers after each phase. Fix remaining errors iteratively until both hit 0.

**After all fixes:**
- Run `make gen` if any facade exports changed.
- Run `make test PROJECT=flext-cli` for non-regression.
- Run `make pyre` to verify no global regression.

**IMPORTANT context budget note:** flext-cli has ~1,419 errors. If context usage is approaching 50%, prioritize:
1. Mechanical ast-grep fixes first (highest ROI)
2. Then systematic file-by-file passes on highest-error files
3. Use `make check PROJECT=flext-cli CHECK_GATES=pyrefly 2>&1 | head -50` to identify top error files
  </action>
  <verify>
    <automated>make check PROJECT=flext-cli CHECK_GATES=pyrefly,pyright 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `make check PROJECT=flext-cli CHECK_GATES=pyrefly,pyright` exits 0
    - `sg --pattern 'cast($$$)' --lang python flext-cli/src/` returns 0 matches
    - `sg --pattern '$X.__class__ is $Y' --lang python flext-cli/src/` returns 0 matches
    - `sg --pattern '$X.__class__ is not $Y' --lang python flext-cli/src/` returns 0 matches
    - `make pol` exits 0
    - `make test PROJECT=flext-cli` passes
    - `make pyre` shows reduction in total errors (no regression from Wave 2 count)
  </acceptance_criteria>
  <done>flext-cli has zero pyrefly errors, zero pyright errors, zero typing shortcuts. The largest consumer is clean. All tests pass.</done>
</task>

</tasks>

<verification>
1. `make check PROJECT=flext-cli CHECK_GATES=pyrefly,pyright` exits 0
2. `make pol` exits 0
3. `make test PROJECT=flext-cli` passes
4. `make pyre` total error count reduced by flext-cli's contribution
5. No typing shortcuts remain in flext-cli
</verification>

<success_criteria>
- flext-cli is type-clean: 0 pyrefly errors, 0 pyright errors
- No typing shortcuts remain (0 Any, 0 object, 0 cast, 0 __class__, 0 type:ignore)
- All tests pass without behavioral regressions
- Largest consumer is clean, clearing the way for Wave 4 remaining consumers
</success_criteria>

<output>
After completion, create `.planning/phases/01-type-system-hardening/01-03-SUMMARY.md`
</output>
