---
phase: 01-type-system-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - Makefile
  - flext-core/src/flext_core/_utilities/guards.py
  - flext-core/src/flext_core/_utilities/domain.py
  - flext-core/src/flext_core/_utilities/introspection.py
  - flext-core/src/flext_core/_utilities/mapper.py
  - flext-core/src/flext_core/_utilities/transformation.py
  - flext-core/src/flext_core/_models/domain.py
  - flext-core/src/flext_core/loggings.py
  - flext-core/src/flext_core/result.py
  - flext-core/src/flext_core/typings.py
  - flext-core/src/flext_core/protocols.py
  - flext-core/src/flext_core/container.py
  - flext-core/src/flext_core/registry.py
  - flext-core/src/flext_core/service.py
  - flext-core/src/flext_core/dispatcher.py
  - flext-core/src/flext_core/context.py
  - flext-core/src/flext_core/runtime.py
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - TYPE-04
  - TYPE-05
  - TYPE-06

must_haves:
  truths:
    - "make pyre exits 0 with 0 pyrefly errors for flext-core scope"
    - "make check PROJECT=flext-core CHECK_GATES=pyright exits 0"
    - "Zero cast() calls in flext-core outside result.py"
    - "Zero __class__ is / __class__ not in comparisons in flext-core"
    - "Zero bare object annotations in flext-core src/"
    - "Zero Any imports or annotations in flext-core src/"
    - "Zero # type: ignore in flext-core"
  artifacts:
    - path: "flext-core/src/flext_core/_utilities/domain.py"
      provides: "same_type() utility for domain.py:49,75 Option C fix"
      contains: "def same_type"
    - path: ".reports/pyrefly/"
      provides: "Fresh baseline pyrefly report"
  key_links:
    - from: "flext-core/src/flext_core/_utilities/guards.py"
      to: "flext-core/src/flext_core/_models/domain.py"
      via: "TypeGuard functions replacing __class__ patterns"
      pattern: "isinstance.*|u\\.Guards\\.is_"
    - from: "flext-core/src/flext_core/typings.py"
      to: "all flext-core modules"
      via: "t.* type contracts replacing object/Any annotations"
      pattern: "t\\."
---

<objective>
Wave 1: Fix the pyrefly entrypoint, establish a fresh error baseline, then eliminate ALL pyrefly errors, pyright errors, and typing shortcuts (Any, object, cast, __class__) in flext-core.

Purpose: flext-core is the foundation for all 32 downstream projects. Its types cascade everywhere. Cleaning the foundation first prevents cascading breakage in later waves. This plan also fixes the broken `make pyre` entrypoint (D-07) which is a prerequisite for all subsequent measurement.

Output: flext-core passes both `make pyre` (0 errors) and `make check PROJECT=flext-core CHECK_GATES=pyright` (0 errors). Fresh baseline captured for tracking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-type-system-hardening/01-CONTEXT.md
@.planning/phases/01-type-system-hardening/01-RESEARCH.md

Source references (per D-03 — MUST read sisyphus plans):
@.sisyphus/plans/strict-typing-execution-plan.md
@.sisyphus/plans/bare-object-elimination.md
@.sisyphus/plans/pyright-zero-errors.md
@.sisyphus/plans/pyrefly-repo-hardening.md
@.sisyphus/evidence/CORRECTED_BASELINE_2026-03-12.md

Skills:
@.claude/skills/flext-strict-typing/SKILL.md
@.claude/skills/flext-type-system/SKILL.md
@.claude/skills/flext-pyrefly-typecheck-fix/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix make pyre entrypoint and establish fresh baseline</name>
  <files>pyproject.toml, Makefile</files>
  <read_first>
    - pyproject.toml (lines 117-185 — [tool.pyrefly] section)
    - Makefile (search for "pyre" target around line 793)
    - .sisyphus/plans/pyrefly-repo-hardening.md
    - .sisyphus/evidence/pyrefly-toolchain.txt (if exists)
    - .sisyphus/evidence/pyrefly-repo-before.txt (if exists)
  </read_first>
  <action>
Per D-07 and D-08, the `make pyre` entrypoint is currently broken.

**Step 1: Diagnose `make pyre` failure.**
Run `make pyre` and capture output. Inspect:
- Does the output show "Invalid search-path" warnings?
- Does it show "projects: 0" or "files_checked: 0"?
- What is the actual exit code and error message?

The RESEARCH.md confirms `pyproject.toml` lines 127-174 already use bare relative paths (no `${PWD}` tokens), so the issue may be in the Makefile invocation or pyrefly configuration.

**Step 2: Fix the entrypoint.**
Based on diagnosis:
- If search-path resolution fails: convert relative paths to absolute paths in `[tool.pyrefly]` section, or fix the Makefile `pyre` target to `cd` to correct directory before invocation.
- If `make check PROJECT=flext-core` shows "Skipped: 1": investigate the gate routing in `base.mk` with `VERBOSE=1` — fix the gate selection so pyrefly/pyright checks actually run.
- Verify fix by running `make pyre` again — it must exit with a numeric error count, NOT "0 files checked".

**Step 3: Capture fresh baseline.**
Run `make pyre` and record:
- Total error count (replaces the stale 4,385 figure)
- Per-project breakdown (especially flext-core count, replacing the ~170 estimate)
- Save output to `.reports/pyrefly/` directory

Also run `make check PROJECT=flext-core CHECK_GATES=pyright` and record pyright error count for flext-core.

**Step 4: Verify `make pol` works.**
Run `make pol` to confirm the policy gate is functional. Record current violations (should be near zero per RESEARCH.md).
  </action>
  <verify>
    <automated>make pyre 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `make pyre` exits with a numeric error count (not "0 files checked" or "Invalid search-path")
    - `make pyre` output shows files_checked > 0
    - `make check PROJECT=flext-core CHECK_GATES=pyright` does NOT show "Skipped: 1"
    - Fresh baseline numbers recorded (exact count will vary — what matters is the entrypoint works)
    - `make pol` exits 0 (policy gate functional)
  </acceptance_criteria>
  <done>make pyre returns authoritative error counts. make check per-project gates run without skipping. Fresh baseline captured.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate all pyrefly errors, pyright errors, and typing shortcuts in flext-core</name>
  <files>
    flext-core/src/flext_core/_utilities/guards.py,
    flext-core/src/flext_core/_utilities/domain.py,
    flext-core/src/flext_core/_utilities/introspection.py,
    flext-core/src/flext_core/_utilities/mapper.py,
    flext-core/src/flext_core/_utilities/transformation.py,
    flext-core/src/flext_core/_models/domain.py,
    flext-core/src/flext_core/loggings.py,
    flext-core/src/flext_core/result.py,
    flext-core/src/flext_core/typings.py,
    flext-core/src/flext_core/protocols.py,
    flext-core/src/flext_core/container.py,
    flext-core/src/flext_core/registry.py,
    flext-core/src/flext_core/service.py,
    flext-core/src/flext_core/dispatcher.py,
    flext-core/src/flext_core/context.py,
    flext-core/src/flext_core/runtime.py
  </files>
  <read_first>
    - .sisyphus/plans/strict-typing-execution-plan.md (ast-grep rules A1-A4 for __class__, B1-B3 for cast())
    - .sisyphus/plans/bare-object-elimination.md (t.* aliases required, annotation replacement map)
    - .sisyphus/plans/pyright-zero-errors.md (root causes: _operation_stats PrivateAttr, _GuardInput widening)
    - .sisyphus/evidence/CORRECTED_BASELINE_2026-03-12.md (per-file violation inventory)
    - flext-core/src/flext_core/_utilities/guards.py (current TypeGuard functions)
    - flext-core/src/flext_core/_models/domain.py (lines 49, 75 — Option C sites)
    - flext-core/src/flext_core/loggings.py (cast() sites at lines 180-185)
    - flext-core/src/flext_core/_utilities/mapper.py (9 cast() violations)
    - flext-core/src/flext_core/typings.py (existing t.* contracts)
    - flext-core/src/flext_core/result.py (authorized cast() exception)
  </read_first>
  <action>
Per D-02, fix ALL error categories for flext-core in a single pass. Work through these categories in order:

**Category A: __class__ rewrites (TYPE-06) — ~31 violations (17 FROZEN+authorized, 14 MODIFIABLE)**

1. Create ast-grep rules A1-A4 from RESEARCH.md in a temp YAML file:
   - Rule A1: `$X.__class__ is $Y` -> `isinstance($X, $Y)`
   - Rule A2: `$X.__class__ is not $Y` -> `not isinstance($X, $Y)`
   - Rule A3: `$X.__class__ in {$$$Y}` -> `isinstance($X, ($$$Y))`
   - Rule A4: `$X.__class__ not in {$$$Y}` -> `not isinstance($X, ($$$Y))`

2. Dry-run first: `sg --pattern '$X.__class__ is $Y' --lang python flext-core/src/` to see all matches.

3. EXCLUDE from mechanical rewrite (manual handling required):
   - `domain.py:49` and `domain.py:75` — these are Option C (exact-type identity). Create `same_type(a, b) -> bool` utility:
     ```python
     def same_type(a: object, b: object) -> bool:
         """Exact-type identity comparison (no MRO traversal)."""
         return type(a) is type(b)  # noqa: E721
     ```
     Place in `flext-core/src/flext_core/_utilities/domain.py` (new file or add to existing).
     Replace `entity_b.__class__ is not entity_a.__class__` with `not same_type(entity_b, entity_a)`.
   - `collection.py:95` (if exists) — verify subclass behavior with tests before rewriting.
   - `data_validator.py:221` (if exists) — manual rewrite for `type(None)` edge case.

4. Apply rules A1-A4 to all OTHER sites in flext-core.

5. Verify: all `__class__ is` and `__class__ not in` patterns are gone from flext-core.

**Category B: cast() removal (TYPE-05) — 15 violations**

Per CORRECTED_BASELINE: 3 `t.cast` in result_helpers.py (FROZEN, authorized), 3 `t.cast` in loggings.py, 9 bare `cast` in mapper.py.

1. PRESERVE: `cast()` in `result.py` (sole authorized exception per TYPE-05).
2. For each `cast()` site:
   - Replace with TypeGuard conditional: `if u.Guards.is_mapping(value): result = value`
   - Or replace with `isinstance()` narrowing where the target type is concrete.
   - Use patterns from strict-typing-execution-plan.md Wave 1.2.
3. Verify: `sg --pattern 'cast($$$)' --lang python flext-core/src/` shows only `result.py`.

**Category C: bare object/Any elimination (TYPE-03, TYPE-04) — ~36 violations**

Per bare-object-elimination.md:
1. Verify that Wave 0 aliases are available: `t.JsonValue`, `t.LoggerFactory`, `t.BootstrapInput` should be importable from `flext_core`.
2. For each `object` annotation in flext-core/src/:
   - Replace with the most specific `t.*` contract available.
   - Common replacements: `object` -> `t.Scalar`, `t.Container`, `BaseModel`, `p.HasModelDump`, etc.
   - NEVER replace with `t.NormalizedValue` (FORBIDDEN) or `Any` (FORBIDDEN).
3. For each `typing.Any` import/annotation:
   - Replace with specific `t.*` contract.
4. For each `# type: ignore` (TYPE-02):
   - Remove the comment and fix the underlying type error.

**Category D: pyright root causes (from pyright-zero-errors.md)**

1. `_operation_stats` PrivateAttr pattern (121+ cascade):
   - Fix the `Annotated[T, PrivateAttr()]` declaration pattern — use `PrivateAttr()` at field level.
2. `_GuardInput` union too narrow:
   - Widen to include `ResultLike`, `BaseModel`, `ConfigMap`, `HasModelDump` in the union.
3. Incompatible method overrides:
   - Fix `model_post_init`, `get`, `register` signatures to match parent types.
4. Missing type annotations (317 `reportUnknownVariableType`):
   - Add explicit annotations to all untyped variables.

**Category E: pyrefly errors (remaining)**

After fixing categories A-D, run `make pyre` and `make check PROJECT=flext-core CHECK_GATES=pyright` again. Fix any remaining errors iteratively. Common pyrefly error clusters from the skill:
- Logger typing drift: annotate as `p.Logger`
- `r.ok(None)` bugs: fix to `r[bool].ok(True)` or correct type
- RootModel container coercion: use `.root` instead of `dict()`
- Mapping mutation: use `MutableMapping` or materialize `dict()` copy

**After all fixes:**
- Run `make gen` if any facade exports changed (autogenerated `__init__.py`).
- Run `make test PROJECT=flext-core` to verify no behavioral regressions.
  </action>
  <verify>
    <automated>make pyre 2>&1 | tail -5 && make check PROJECT=flext-core CHECK_GATES=pyright 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `make pyre` shows 0 errors for flext-core scope (or total repo errors decreased by the full flext-core count)
    - `make check PROJECT=flext-core CHECK_GATES=pyright` exits 0
    - `sg --pattern '$X.__class__ is $Y' --lang python flext-core/src/` returns 0 matches
    - `sg --pattern '$X.__class__ is not $Y' --lang python flext-core/src/` returns 0 matches
    - `sg --pattern 'cast($$$)' --lang python flext-core/src/` shows matches ONLY in `result.py`
    - `make pol` exits 0 (zero Any/type:ignore/NormalizedValue policy violations)
    - `make test PROJECT=flext-core` passes (no behavioral regressions)
    - `flext-core/src/flext_core/_utilities/domain.py` contains `def same_type`
  </acceptance_criteria>
  <done>flext-core has zero pyrefly errors, zero pyright errors, zero cast() outside result.py, zero __class__ comparisons, zero bare object/Any annotations, and zero type:ignore comments. All tests pass.</done>
</task>

</tasks>

<verification>
1. `make pyre` shows 0 errors for all flext-core files
2. `make check PROJECT=flext-core CHECK_GATES=pyright` exits 0
3. `make pol` exits 0 (policy gate clean)
4. `make test PROJECT=flext-core` passes
5. `sg --pattern '$X.__class__ is $Y' --lang python flext-core/src/` returns no matches
6. `sg --pattern 'cast($$$)' --lang python flext-core/src/` only matches result.py
</verification>

<success_criteria>
- flext-core is type-clean: 0 pyrefly errors, 0 pyright errors
- No typing shortcuts remain: 0 Any, 0 object annotations, 0 cast (except result.py), 0 __class__ comparisons, 0 type:ignore
- Fresh baseline established and recorded
- All tests pass without behavioral regressions
- Foundation is clean for Wave 2 to build upon
</success_criteria>

<output>
After completion, create `.planning/phases/01-type-system-hardening/01-01-SUMMARY.md`
</output>
