---
phase: 06-typing-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "*/src/**/*.py"
autonomous: true
requirements:
  - TYPE-08
must_haves:
  truths:
    - "All empty container literals in src/**/*.py have explicit type annotations at assignment sites"
  artifacts:
    - path: "*/src/**/*.py"
      provides: "Annotated empty container assignments"
  key_links: []
---

<objective>
Annotate all 745 unannotated empty container literals (`[]`, `{}`, `set()`) at assignment sites across all src/**/*.py files.

Purpose: Close TYPE-08 — empty containers without annotations produce implicit `list[Unknown]` / `dict[Unknown, Unknown]` types.
Output: Zero unannotated empty container assignments in production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typing-gap-closure/06-CONTEXT.md
@flext-core/src/flext_core/_typings/typings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate empty containers in flext-core</name>
  <files>flext-core/src/flext_core/**/*.py</files>
  <read_first>
- Each file before editing (mandatory per project rules)
- flext-core/src/flext_core/_typings/typings.py (for t.* contracts)
  </read_first>
  <action>
For every unannotated `x = []`, `x = {}`, or `x = set()` assignment in `flext-core/src/`:

1. Read the file and determine the correct type from usage context (what gets appended/inserted/assigned).
2. Add explicit annotation: `x: list[SpecificType] = []`, `x: dict[KeyType, ValType] = {}`, `x: set[SpecificType] = set()`.
3. Per D-05: use `t.*` contracts where applicable (e.g., `t.ContainerList`, `t.ContainerMapping`, `t.MutableContainerMapping`).
4. For class-level attributes in Pydantic models, these should already be `Field(default_factory=list)` — if found as bare `= []`, convert to annotated form.
5. Run `make lint` on flext-core after all changes.

Key files to fix (from scan): dispatcher.py, settings.py, mixins.py, runtime.py, container.py, registry.py, service.py, handler.py, result.py, and files under _utilities/, _models/, _constants/.

Expected scope: ~80-100 assignments in flext-core.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && .venv/bin/python -c "
import ast, pathlib
results = []
for py in pathlib.Path('flext-core/src').rglob('*.py'):
    try: tree = ast.parse(py.read_text())
    except: continue
    for node in ast.walk(tree):
        if isinstance(node, ast.Assign) and node.type_comment is None and len(node.targets) == 1:
            val = node.value
            if (isinstance(val, ast.List) and not val.elts) or (isinstance(val, ast.Dict) and not val.keys) or (isinstance(val, ast.Call) and isinstance(val.func, ast.Name) and val.func.id == 'set' and not val.args):
                results.append(f'{py}:{node.lineno}')
print(f'Remaining: {len(results)}')
for r in results: print(r)
"</automated>
  </verify>
  <acceptance_criteria>
- Zero unannotated empty container assignments in flext-core/src/
- `make lint` passes for flext-core
  </acceptance_criteria>
  <done>All empty container literals in flext-core/src/ have explicit type annotations.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate empty containers in all consumer projects</name>
  <files>*/src/**/*.py (excluding flext-core)</files>
  <read_first>
- Each file before editing (mandatory per project rules)
- flext-core/src/flext_core/_typings/typings.py (for t.* contracts)
  </read_first>
  <action>
For every unannotated `x = []`, `x = {}`, or `x = set()` assignment in all `*/src/` directories EXCEPT flext-core (already done in Task 1):

1. Read each file, determine correct type from usage context.
2. Add explicit annotation using `t.*` contracts per D-05 where applicable.
3. Projects to fix (from scan, sorted by count): flext-ldif (~120), flext-api (~80), flext-meltano (~70), flext-cli (~50), flext-auth (~40), flext-observability (~30), flext-web (~30), flext-tests (~30), flext-ldap (~25), flext-infra (~25), and remaining projects (~100).
4. Work project-by-project. Run `make lint` after completing each project.
5. After all projects done, run the full scan to confirm zero remaining.

For self-referential class attributes (e.g., `children = []` where children are instances of the same class), use `list[Self]` or forward reference as appropriate.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && .venv/bin/python -c "
import ast, pathlib
results = []
for src_dir in pathlib.Path('.').glob('*/src'):
    if 'flext-core' in str(src_dir): continue
    for py in src_dir.rglob('*.py'):
        try: tree = ast.parse(py.read_text())
        except: continue
        for node in ast.walk(tree):
            if isinstance(node, ast.Assign) and node.type_comment is None and len(node.targets) == 1:
                val = node.value
                if (isinstance(val, ast.List) and not val.elts) or (isinstance(val, ast.Dict) and not val.keys) or (isinstance(val, ast.Call) and isinstance(val.func, ast.Name) and val.func.id == 'set' and not val.args):
                    results.append(f'{py}:{node.lineno}')
print(f'Remaining: {len(results)}')
for r in results[:20]: print(r)
"</automated>
  </verify>
  <acceptance_criteria>
- Zero unannotated empty container assignments across all src/**/*.py
- `make lint` passes workspace-wide
  </acceptance_criteria>
  <done>All 745 empty container literals across the monorepo have explicit type annotations at assignment sites.</done>
</task>

</tasks>

<verification>
- Full monorepo scan shows 0 unannotated empty container assignments in src/
- `make lint` passes
- pyrefly reports no new errors
</verification>

<success_criteria>
TYPE-08 satisfied: every empty `[]`, `{}`, `set()` in src/**/*.py has an explicit type annotation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-typing-gap-closure/06-02-SUMMARY.md`
</output>
