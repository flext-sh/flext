---
phase: 06-typing-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flext-cli/src/flext_cli/api.py
autonomous: true
requirements:
  - TYPE-07
must_haves:
  truths:
    - "Zero TypeGuard imports remain in any src/**/*.py file"
    - "The converted function passes pyrefly and pyright strict checks"
  artifacts:
    - path: "flext-cli/src/flext_cli/api.py"
      provides: "TypeIs-based type narrowing for CLI command registration"
      contains: "TypeIs"
  key_links:
    - from: "flext-cli/src/flext_cli/api.py"
      to: "typing.TypeIs"
      via: "import statement"
      pattern: "from typing.*import.*TypeIs"
---

<objective>
Migrate the last remaining TypeGuard function to TypeIs (PEP 742).

Purpose: Close TYPE-07 — only 1 TypeGuard remains in production src/ code.
Output: Zero TypeGuard imports in src/**/*.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-typing-gap-closure/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert TypeGuard to TypeIs in flext-cli/api.py</name>
  <files>flext-cli/src/flext_cli/api.py</files>
  <read_first>flext-cli/src/flext_cli/api.py</read_first>
  <action>
1. In `flext-cli/src/flext_cli/api.py` line 12, replace `TypeGuard` with `TypeIs` in the typing import (per D-02: import from `typing` stdlib).
2. On line 455, change `-> TypeGuard[p.Cli.CliRegisteredCommand]` to `-> TypeIs[p.Cli.CliRegisteredCommand]` (per D-01).
3. Run `make lint` and `make typecheck` to confirm no regressions (per D-03).
4. Verify no other `TypeGuard` imports remain in any `src/**/*.py` file across the monorepo.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && sg --pattern 'TypeGuard' --lang python $(find */src -name '*.py' 2>/dev/null | head -500) && echo "FAIL: TypeGuard still found" || echo "PASS: zero TypeGuard in monorepo src/"</automated>
  </verify>
  <acceptance_criteria>
- Monorepo-wide scan: `sg --pattern 'TypeGuard' --lang python */src/` returns zero matches
- `flext-cli/src/flext_cli/api.py` contains `TypeIs` import and `-> TypeIs[p.Cli.CliRegisteredCommand]`
- pyrefly and pyright pass on flext-cli
  </acceptance_criteria>
  <done>Zero TypeGuard in production src/ code. TypeIs used instead.</done>
</task>

</tasks>

<verification>
- Monorepo-wide `sg --pattern 'TypeGuard' --lang python */src/` returns zero matches
- pyrefly and pyright pass on flext-cli
</verification>

<success_criteria>
TYPE-07 satisfied: zero TypeGuard functions remain in production code.
</success_criteria>

<output>
After completion, create `.planning/phases/06-typing-gap-closure/06-01-SUMMARY.md`
</output>
