---
phase: 09-rope-native-refactor-engine-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flext-infra/src/flext_infra/refactor/engine.py
  - flext-infra/tests/test_infra_refactor_rope_project.py
  - flext-infra/tests/test_infra_refactor_engine.py
  - flext-infra/tests/refactor/__init__.py
  - flext-infra/tests/refactor/test_rope_stubs.py
autonomous: true
requirements: [ROPE-01, ROPE-05, ROPE-06]
must_haves:
  truths:
    - "FlextInfraRefactorEngine can create a rope Project for the monorepo root"
    - "Rope Project excludes .venv, dist, __pycache__, .git from indexing"
    - "Pre-hook and post-hook registration points exist on engine"
    - "Dry-run mode does not apply rope changes to disk"
  artifacts:
    - path: "flext-infra/src/flext_infra/refactor/engine.py"
      provides: "Rope project init + hook infrastructure"
      contains: "_init_rope_project"
    - path: "flext-infra/tests/test_infra_refactor_rope_project.py"
      provides: "Tests for rope project init and hooks"
      contains: "test_rope_project"
    - path: "flext-infra/tests/refactor/test_rope_stubs.py"
      provides: "Wave 0 stub tests confirming rope is importable"
      contains: "test_rope_import"
  key_links:
    - from: "engine.py"
      to: "rope.base.project.Project"
      via: "_init_rope_project method"
      pattern: "RopeProject\\("
---

<objective>
Add rope Project initialization and pre/post hook infrastructure to FlextInfraRefactorEngine.

Purpose: Foundation for migrating 3 cross-file transformers from LibCST to rope in Plan 02. Per D-10, rope ops plug in as pre/post hooks, not new rule types. Per D-15, single monorepo-rooted Project instance with save_objectdb=False (D-18).

Output: Engine with hook points, rope Project lazy init, tests proving it works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rope-native-refactor-engine-rewrite/09-CONTEXT.md
@.planning/phases/09-rope-native-refactor-engine-rewrite/09-RESEARCH.md

@flext-infra/src/flext_infra/refactor/engine.py
@flext-infra/tests/test_infra_refactor_engine.py

<interfaces>
<!-- From engine.py — key class and methods executor needs -->
From flext-infra/src/flext_infra/refactor/engine.py:
```python
class FlextInfraRefactorEngine:
    def __init__(self, config_path: Path | None = None) -> None: ...
    def refactor_project(self, project_path: Path, *, dry_run: bool = False, pattern: str = ..., apply_safety: bool = True) -> Sequence[m.Infra.Result]: ...
    def refactor_workspace(self, workspace_root: Path, *, dry_run: bool = False, pattern: str = ..., apply_safety: bool = True) -> Sequence[m.Infra.Result]: ...
    def refactor_files(self, files: Sequence[Path], *, dry_run: bool = False) -> Sequence[m.Infra.Result]: ...
```

From rope API (already installed):
```python
from rope.base.project import Project  # Project(root, ropefolder=None, ignored_resources=[...], source_folders=[...], save_objectdb=False)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 test stubs for rope infrastructure</name>
  <files>
    flext-infra/tests/refactor/__init__.py,
    flext-infra/tests/refactor/test_rope_stubs.py
  </files>
  <action>
    Create the `flext-infra/tests/refactor/` directory with:

    1. `__init__.py` — empty package init
    2. `test_rope_stubs.py` — minimal stub tests:
       - `test_rope_import`: asserts `from rope.base.project import Project` succeeds
       - `test_rope_rename_import`: asserts `from rope.refactor.rename import Rename` succeeds
       - `test_rope_find_occurrences_import`: asserts `from rope.contrib.findit import find_occurrences` succeeds

    These stubs confirm rope is installed and importable before any implementation begins.
  </action>
  <verify>
    <automated>/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/refactor/test_rope_stubs.py -x -q</automated>
  </verify>
  <done>Wave 0 test stubs exist and pass, confirming rope is importable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add rope Project init and hook infrastructure to engine</name>
  <files>
    flext-infra/src/flext_infra/refactor/engine.py,
    flext-infra/tests/test_infra_refactor_rope_project.py
  </files>
  <read_first>
    flext-infra/src/flext_infra/refactor/engine.py,
    flext-infra/tests/test_infra_refactor_engine.py,
    .planning/phases/09-rope-native-refactor-engine-rewrite/09-RESEARCH.md
  </read_first>
  <behavior>
    - Test: _init_rope_project creates Project with ropefolder=None
    - Test: _init_rope_project sets save_objectdb=False
    - Test: _init_rope_project ignored_resources includes .venv, *.pyc, dist/, __pycache__, .mypy_cache, .git
    - Test: _init_rope_project source_folders includes all flext-*/src dirs that exist
    - Test: rope_project property returns None before init, Project after init
    - Test: _run_rope_pre_hooks called before refactor_files in refactor_project (ordering verified via mock side_effect sequence)
    - Test: _run_rope_post_hooks called after refactor_files in refactor_project (ordering verified via mock side_effect sequence)
    - Test: dry_run=True does not call changeset.do() (no file writes)
    - Test: hook call ordering — a single test that patches pre_hooks, refactor_files, post_hooks and asserts call_args_list order is [pre, refactor, post]
  </behavior>
  <action>
    Per D-10/D-15/D-18/D-19, add to FlextInfraRefactorEngine:

    1. Import at top: `from rope.base.project import Project as RopeProject`

    2. Add `_rope_project: RopeProject | None = None` attribute in __init__

    3. Add `_init_rope_project(self, workspace_root: Path) -> RopeProject` method:
       - Discover source_folders: `[str(d / "src") for d in sorted(workspace_root.iterdir()) if d.is_dir() and d.name.startswith("flext-") and (d / "src").exists()]`
       - Create: `RopeProject(str(workspace_root), ropefolder=None, ignored_resources=["*.pyc", ".venv", "dist/", "__pycache__", ".mypy_cache", ".git", ".tox", "*.egg-info"], source_folders=source_folders, save_objectdb=False)`
       - Store on self._rope_project, return it

    4. Add `rope_project` property returning `self._rope_project`

    5. Add `_run_rope_pre_hooks(self, workspace_root: Path, *, dry_run: bool = False) -> Sequence[m.Infra.Result]` — empty list for now, Plan 02 fills in

    6. Add `_run_rope_post_hooks(self, workspace_root: Path, *, dry_run: bool = False) -> Sequence[m.Infra.Result]` — empty list for now

    7. In `refactor_project()`, insert rope hook calls:
       - After safety stash (line ~487): `rope_pre_results = self._run_rope_pre_hooks(project_path, dry_run=dry_run)`
       - After refactor_files (line ~538): `rope_post_results = self._run_rope_post_hooks(project_path, dry_run=dry_run)`
       - Extend results with both

    8. Same pattern in `refactor_workspace()` — call hooks once with workspace_root before/after the per-project loop

    Create test file `flext-infra/tests/test_infra_refactor_rope_project.py` with tests for all behaviors listed above including the explicit hook ordering test. Use tmp_path fixture for workspace mock with fake flext-*/src dirs.
  </action>
  <verify>
    <automated>/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/test_infra_refactor_rope_project.py -x -q -k "test_rope_project or test_hook_call_ordering"</automated>
  </verify>
  <acceptance_criteria>
    - engine.py contains `from rope.base.project import Project as RopeProject`
    - engine.py contains `def _init_rope_project(self, workspace_root: Path) -> RopeProject:`
    - engine.py contains `ropefolder=None`
    - engine.py contains `save_objectdb=False`
    - engine.py contains `ignored_resources=` with `.venv` in the list
    - engine.py contains `def _run_rope_pre_hooks(`
    - engine.py contains `def _run_rope_post_hooks(`
    - engine.py `refactor_project` method calls `_run_rope_pre_hooks` before `refactor_files`
    - test_infra_refactor_rope_project.py includes test_hook_call_ordering that verifies pre->refactor->post sequence
    - test_infra_refactor_rope_project.py exits 0
    - `/home/marlonsc/flext/.venv/bin/ruff check flext-infra/src/flext_infra/refactor/engine.py` exits 0
    - `/home/marlonsc/flext/.venv/bin/pyright flext-infra/src/flext_infra/refactor/engine.py` shows 0 errors
  </acceptance_criteria>
  <done>Engine has rope Project init + pre/post hook infrastructure with explicit ordering test, all tests pass, zero linter errors</done>
</task>

</tasks>

<verification>
- `/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/refactor/test_rope_stubs.py -x -q` passes (Wave 0)
- `/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/test_infra_refactor_rope_project.py -x -q` passes
- `/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/test_infra_refactor_engine.py -x -q` still passes (no regressions)
- `/home/marlonsc/flext/.venv/bin/ruff check flext-infra/src/flext_infra/refactor/engine.py` exits 0
- `/home/marlonsc/flext/.venv/bin/pyright flext-infra/src/flext_infra/refactor/engine.py` shows 0 errors
</verification>

<success_criteria>
FlextInfraRefactorEngine has lazy rope Project creation and pre/post hook call sites. Existing tests still pass. Hook methods are empty stubs ready for Plan 02. Wave 0 stubs confirm rope importability.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rope-native-refactor-engine-rewrite/09-01-SUMMARY.md`
</output>
