---
phase: 09-rope-native-refactor-engine-rewrite
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - flext-infra/src/flext_infra/refactor/engine.py
  - flext-infra/src/flext_infra/transformers/symbol_propagator.py
  - flext-infra/src/flext_infra/transformers/mro_reference_rewriter.py
  - flext-infra/src/flext_infra/transformers/nested_class_propagation.py
  - flext-infra/tests/test_infra_refactor_rope_migrations.py
autonomous: true
requirements: [ROPE-02, ROPE-03, ROPE-04]
must_haves:
  truths:
    - "symbol_propagator uses rope.contrib.findit.find_occurrences instead of QualifiedNameProvider"
    - "mro_reference_rewriter uses rope.refactor.rename.Rename instead of manual CST rewrites"
    - "nested_class_propagation uses rope for scope-aware name resolution instead of ParentNodeProvider"
    - "All 3 migrated transformers are shorter than before migration"
    - "Existing refactor behavior unchanged — same renames, same results"
  artifacts:
    - path: "flext-infra/src/flext_infra/transformers/symbol_propagator.py"
      provides: "Rope-based symbol propagation"
      contains: "find_occurrences"
    - path: "flext-infra/src/flext_infra/transformers/mro_reference_rewriter.py"
      provides: "Rope-based MRO reference rewriting"
      contains: "Rename"
    - path: "flext-infra/src/flext_infra/transformers/nested_class_propagation.py"
      provides: "Rope-based nested class propagation"
      contains: "rope"
  key_links:
    - from: "engine.py _run_rope_pre_hooks"
      to: "rope.refactor.rename.Rename"
      via: "pre-hook calls rope rename for symbol propagation"
      pattern: "Rename\\(.*get_changes"
---

<objective>
Migrate the 3 target transformers (symbol_propagator, mro_reference_rewriter, nested_class_propagation) from hand-rolled LibCST metadata to rope APIs.

Purpose: Per D-04, these are the only transformers doing cross-file name resolution — rope handles this natively. Per D-20/D-22, each must become simpler (fewer LOC) after migration. Per D-02, rope simplifies, never adds layers.

Output: 3 transformers rewritten to use rope, engine hooks wired, tests proving equivalence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-rope-native-refactor-engine-rewrite/09-CONTEXT.md
@.planning/phases/09-rope-native-refactor-engine-rewrite/09-RESEARCH.md
@.planning/phases/09-rope-native-refactor-engine-rewrite/09-01-SUMMARY.md

@flext-infra/src/flext_infra/refactor/engine.py
@flext-infra/src/flext_infra/transformers/symbol_propagator.py
@flext-infra/src/flext_infra/transformers/mro_reference_rewriter.py
@flext-infra/src/flext_infra/transformers/nested_class_propagation.py

<interfaces>
<!-- Rope APIs used in migration -->
```python
from rope.base.project import Project as RopeProject
from rope.refactor.rename import Rename  # Rename(project, resource, offset).get_changes(new_name)
from rope.contrib.findit import find_occurrences  # find_occurrences(project, resource, offset) -> list[Location]
from rope.base.change import ChangeSet  # changeset.changes, changeset.do()
```

<!-- Current transformer LOC baselines -->
# symbol_propagator.py: 117 LOC (uses QualifiedNameProvider)
# mro_reference_rewriter.py: 79 LOC (uses manual CST leave_Name/leave_Attribute)
# nested_class_propagation.py: 189 LOC (uses ParentNodeProvider)
# TOTAL: 385 LOC — target: < 300 LOC after migration
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate symbol_propagator to rope find_occurrences</name>
  <files>
    flext-infra/src/flext_infra/transformers/symbol_propagator.py,
    flext-infra/src/flext_infra/refactor/engine.py,
    flext-infra/tests/test_infra_refactor_rope_migrations.py
  </files>
  <read_first>
    flext-infra/src/flext_infra/transformers/symbol_propagator.py,
    flext-infra/src/flext_infra/refactor/engine.py,
    flext-infra/tests/test_infra_refactor_class_and_propagation.py
  </read_first>
  <action>
    Rewrite `FlextInfraRefactorSymbolPropagator` to use rope instead of QualifiedNameProvider:

    1. Remove `METADATA_DEPENDENCIES = (QualifiedNameProvider,)` — no longer needed
    2. Remove the `QualifiedNameProvider` and `QualifiedNameSource` imports from libcst.metadata
    3. Replace the `leave_ImportFrom` and `leave_Name` visitors that use qualified name metadata with rope-based logic:
       - Use `rope.contrib.findit.find_occurrences(project, resource, offset)` to find all usages of a symbol
       - Use `rope.refactor.rename.Rename(project, resource, offset).get_changes(new_name)` to rename
    4. The transformer may become a simple function (not a CSTTransformer) if rope handles all the work
    5. Wire into engine's `_run_rope_pre_hooks` — symbol propagation runs as a pre-hook since it's cross-file

    Per D-20: result must be SHORTER than 117 LOC. If it's not shorter, reassess the approach.
    Per D-03: use rope directly, no abstraction layer.

    Create test `test_infra_refactor_rope_migrations.py` with:
    - Test: symbol rename propagates across files using rope
    - Test: module rename updates import paths
    - Test: dry_run=True does not modify files
  </action>
  <verify>
    <automated>/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/test_infra_refactor_rope_migrations.py -x -q -k symbol</automated>
  </verify>
  <acceptance_criteria>
    - symbol_propagator.py does NOT contain `QualifiedNameProvider`
    - symbol_propagator.py does NOT contain `QualifiedNameSource`
    - symbol_propagator.py contains `find_occurrences` or `Rename`
    - `wc -l symbol_propagator.py` shows less than 117 lines
    - test_infra_refactor_rope_migrations.py exits 0 for symbol tests
    - `/home/marlonsc/flext/.venv/bin/ruff check flext-infra/src/flext_infra/transformers/symbol_propagator.py` exits 0
  </acceptance_criteria>
  <done>symbol_propagator uses rope APIs, is shorter than before, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Migrate mro_reference_rewriter and nested_class_propagation to rope</name>
  <files>
    flext-infra/src/flext_infra/transformers/mro_reference_rewriter.py,
    flext-infra/src/flext_infra/transformers/nested_class_propagation.py,
    flext-infra/src/flext_infra/refactor/engine.py,
    flext-infra/tests/test_infra_refactor_rope_migrations.py
  </files>
  <read_first>
    flext-infra/src/flext_infra/transformers/mro_reference_rewriter.py,
    flext-infra/src/flext_infra/transformers/nested_class_propagation.py,
    flext-infra/src/flext_infra/refactor/engine.py,
    flext-infra/tests/test_infra_refactor_rope_migrations.py
  </read_first>
  <action>
    Migrate the remaining 2 transformers to rope:

    **mro_reference_rewriter.py (79 LOC):**
    1. Replace manual `leave_Name`/`leave_Attribute` CST visitors with `rope.refactor.rename.Rename`
    2. The `imported_symbols: Mapping[str, m.Infra.MROImportRewrite]` config becomes input to rope rename operations
    3. For each MROImportRewrite entry: get the resource, find the symbol offset, create Rename, get_changes
    4. Wire into engine's `_run_rope_pre_hooks` — MRO rewrites run as pre-hooks
    5. Target: < 79 LOC

    **nested_class_propagation.py (189 LOC):**
    1. Remove `METADATA_DEPENDENCIES = (ParentNodeProvider,)` — rope handles scope
    2. Replace `_should_propagate` / `_is_inside_class` parent-walk logic with rope's scope resolution
    3. Use `find_occurrences` to find all references to the old class name, then `Rename` to update them
    4. Wire into engine hooks
    5. Target: < 150 LOC (this is the most complex of the 3)

    Per D-21: if any part becomes MORE complex with rope, keep it as LibCST.
    Per D-22: total LOC of all 3 files must be < 385 (the baseline).

    Add tests to `test_infra_refactor_rope_migrations.py`:
    - Test: MRO reference rewrite via rope rename
    - Test: Nested class propagation updates references across files
    - Test: Total LOC of 3 files < 385
  </action>
  <verify>
    <automated>/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/test_infra_refactor_rope_migrations.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - mro_reference_rewriter.py does NOT contain `leave_Name` or `leave_Attribute` CST visitor methods doing manual rewrites (may still have CST for non-rope parts per D-21)
    - nested_class_propagation.py does NOT contain `ParentNodeProvider`
    - mro_reference_rewriter.py contains `Rename` or `find_occurrences`
    - nested_class_propagation.py contains `Rename` or `find_occurrences`
    - Combined `wc -l` of all 3 transformer files < 385
    - test_infra_refactor_rope_migrations.py exits 0
    - `/home/marlonsc/flext/.venv/bin/ruff check flext-infra/src/flext_infra/transformers/mro_reference_rewriter.py flext-infra/src/flext_infra/transformers/nested_class_propagation.py` exits 0
    - `/home/marlonsc/flext/.venv/bin/pyright flext-infra/src/flext_infra/transformers/` shows 0 errors
  </acceptance_criteria>
  <done>All 3 transformers use rope, total LOC reduced, all tests pass, zero linter errors</done>
</task>

</tasks>

<verification>
- `/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/ -x -q` — full test suite passes
- `/home/marlonsc/flext/.venv/bin/ruff check flext-infra/src/` exits 0
- `/home/marlonsc/flext/.venv/bin/pyright flext-infra/src/flext_infra/transformers/` shows 0 errors
- Combined LOC of symbol_propagator.py + mro_reference_rewriter.py + nested_class_propagation.py < 385
</verification>

<success_criteria>
All 3 target transformers migrated to rope. QualifiedNameProvider and ParentNodeProvider removed. LOC reduced. Engine hooks wired. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rope-native-refactor-engine-rewrite/09-02-SUMMARY.md`
</output>
