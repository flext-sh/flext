---
phase: 09-rope-native-refactor-engine-rewrite
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - flext-infra/src/flext_infra/transformers/
autonomous: false
requirements: [ROPE-05, ROPE-07]
must_haves:
  truths:
    - "Remaining LibCST transformers reviewed for rope simplification opportunities"
    - "Total LOC in transformers/ is less than or equal to 4120 baseline"
    - "No transformer became more complex from rope usage"
    - "Full test suite passes with zero ruff/pyright errors"
    - "SUMMARY contains decision table with 13 transformer entries including SRP-ok and DRY-ok columns"
    - "engine.py <= 800 LOC OR rope concerns extracted to _rope.py"
  artifacts:
    - path: "flext-infra/src/flext_infra/transformers/"
      provides: "Simplified transformer directory"
  key_links:
    - from: "engine.py"
      to: "transformers/"
      via: "rule loading and execution"
      pattern: "refactor_files"
---

<objective>
Review remaining LibCST transformers for rope simplification opportunities (per D-05) and verify overall LOC reduction (per D-22).

Purpose: D-05 requires ALL other transformers be reviewed for simplification via rope APIs. D-06/D-07/D-08/D-09 define which stay LibCST. This plan audits each, applies simplifications where rope helps, and confirms the phase goal of reduced complexity.

Output: Audit report on each transformer, any simplifications applied, LOC verification.
</objective>

<architectural_invariants>
See 09-01-PLAN.md for full rules. Summary for this plan:
- SRP audit question per transformer: "Does this class/module do exactly one thing?" If not, flag it — don't fix it in this plan, but record it in the decision log as a future split candidate.
- DRY audit question per transformer: "Does any method here reimplement logic that rope or libcst already provide natively?" If yes, delete the duplicate even if no rope migration is done.
- God Object check: if engine.py exceeded 800 LOC in Plan 01/02, this plan must extract rope concerns to `_rope.py` before Task 2 checkpoint.
</architectural_invariants>

<global_rule name="YAGNI">
For any transformer where simplification is applied: immediately delete any methods, classes, or imports
that no longer have callers after the change. One `ruff check --select F flext-infra/src/` pass at the
end of Task 1 catches everything. Do not keep dead code "just in case".
</global_rule>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-rope-native-refactor-engine-rewrite/09-CONTEXT.md
@.planning/phases/09-rope-native-refactor-engine-rewrite/09-02-SUMMARY.md

@flext-infra/src/flext_infra/refactor/engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Audit remaining transformers — read and decide only, no edits</name>
  <files>flext-infra/src/flext_infra/transformers/</files>
  <read_first>
    flext-infra/src/flext_infra/transformers/import_modernizer.py,
    flext-infra/src/flext_infra/transformers/normalizer.py,
    flext-infra/src/flext_infra/transformers/import_bypass_remover.py,
    flext-infra/src/flext_infra/transformers/tier0_import_fixer.py,
    flext-infra/src/flext_infra/transformers/lazy_import_fixer.py,
    flext-infra/src/flext_infra/transformers/typing_annotation_replacer.py,
    flext-infra/src/flext_infra/transformers/typing_unifier.py,
    flext-infra/src/flext_infra/transformers/class_reconstructor.py,
    flext-infra/src/flext_infra/transformers/alias_remover.py,
    flext-infra/src/flext_infra/transformers/deprecated_remover.py,
    flext-infra/src/flext_infra/transformers/unused_model_remover.py,
    flext-infra/src/flext_infra/transformers/mro_remover.py,
    flext-infra/src/flext_infra/transformers/mro_private_inline.py
  </read_first>
  <action>
    READ ONLY — no edits in this task. For each of the 13 transformers, answer two questions:
    1. SRP: does this class/module do exactly one thing? (flag violations for future split, don't fix now)
    2. DRY: does any method reimplement logic rope or libcst already provide natively?

    Per D-06: mro_remover.py and mro_private_inline.py — NO rope changes, mark keep-libcst.
    Per D-21: mark simplify-with-rope ONLY if rope reduces LOC without adding complexity.

    Output ONLY a decision table in the SUMMARY — no code changes in this task:
    `| {transformer_name} | keep-libcst | simplify-with-rope | {1-sentence rationale} | SRP-ok? | DRY-ok? |`
  </action>
  <verify>
    <automated>echo "audit-only task — no code changed"</automated>
  </verify>
  <acceptance_criteria>
    - No .py files modified (git diff --stat shows 0 changed files in transformers/)
    - SUMMARY contains decision table with exactly 13 transformer entries
    - Each entry has SRP-ok and DRY-ok columns filled
  </acceptance_criteria>
  <done>Decision table written to SUMMARY, no code changed yet</done>
</task>

<task type="auto">
  <name>Task 1b: Apply simplifications from audit decision table</name>
  <files>flext-infra/src/flext_infra/transformers/</files>
  <read_first>
    flext-infra/src/flext_infra/transformers/import_modernizer.py,
    flext-infra/src/flext_infra/transformers/normalizer.py,
    flext-infra/src/flext_infra/transformers/import_bypass_remover.py,
    flext-infra/src/flext_infra/transformers/tier0_import_fixer.py,
    flext-infra/src/flext_infra/transformers/lazy_import_fixer.py,
    flext-infra/src/flext_infra/transformers/typing_annotation_replacer.py,
    flext-infra/src/flext_infra/transformers/typing_unifier.py,
    flext-infra/src/flext_infra/transformers/class_reconstructor.py,
    flext-infra/src/flext_infra/transformers/alias_remover.py,
    flext-infra/src/flext_infra/transformers/deprecated_remover.py,
    flext-infra/src/flext_infra/transformers/unused_model_remover.py
  </read_first>
  <action>
    For each transformer marked `simplify-with-rope` in the Task 1a decision table:
    - Apply the simplification (delete the duplicate logic, use rope API directly)
    - Apply global YAGNI rule (ruff --select F pass after all edits)
    - Per DRY: if a method reimplements what rope/libcst provides natively, delete it even if not in the simplify list

    Do NOT touch transformers marked `keep-libcst`.
    Do NOT touch mro_remover.py or mro_private_inline.py (D-06).

    After all edits: `ruff check --select F flext-infra/src/` must exit 0.
    LOC gate: total transformers/*.py <= 4120.

    If engine.py > 800 LOC (from Plan 01/02 additions): extract rope concerns to
    `flext-infra/src/flext_infra/refactor/_rope.py` and import from engine. SRP enforcement.
  </action>
  <verify>
    <automated>/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/ -x -q && /home/marlonsc/flext/.venv/bin/ruff check flext-infra/src/ && TOTAL=$(cat flext-infra/src/flext_infra/transformers/*.py | wc -l) && test "$TOTAL" -le 4120 && echo "LOC PASS: $TOTAL"</automated>
  </verify>
  <acceptance_criteria>
    - Full test suite exits 0
    - `/home/marlonsc/flext/.venv/bin/ruff check --select F flext-infra/src/` exits 0
    - mro_remover.py unchanged (D-06)
    - mro_private_inline.py unchanged (D-06)
    - Total LOC in transformers/*.py <= 4120
    - If engine.py > 800 LOC: `flext_infra/refactor/_rope.py` exists and engine imports from it
  </acceptance_criteria>
  <done>Simplifications applied, dead code removed, LOC gate passes, SRP enforced on engine</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify LOC reduction and phase completion</name>
  <files>flext-infra/src/flext_infra/transformers/, flext-infra/src/flext_infra/refactor/engine.py</files>
  <action>
    Human verifies the overall phase outcome: LOC reduction, test suite, linters, CLI unchanged.
  </action>
  <what-built>
    Rope integration into FlextInfraRefactorEngine with 3 migrated transformers and audit of remaining transformers.

    Baseline LOC (pre-phase):
    - symbol_propagator.py: 117
    - mro_reference_rewriter.py: 79
    - nested_class_propagation.py: 189
    - Total 3 files: 385
    - engine.py: 748
    - Total transformers/*.py: 4120
  </what-built>
  <how-to-verify>
    1. Run: `wc -l flext-infra/src/flext_infra/transformers/symbol_propagator.py flext-infra/src/flext_infra/transformers/mro_reference_rewriter.py flext-infra/src/flext_infra/transformers/nested_class_propagation.py`
       - Expected: total < 385
    2. Run: `wc -l flext-infra/src/flext_infra/transformers/*.py | tail -1`
       - Expected: total <= 4120
    3. Run: `wc -l flext-infra/src/flext_infra/refactor/engine.py`
       - Expected: modest increase (~30-50 lines for hook infra)
    4. Run: `/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/ -x -q`
       - Expected: all pass
    5. Run: `/home/marlonsc/flext/.venv/bin/ruff check flext-infra/src/ && /home/marlonsc/flext/.venv/bin/pyright flext-infra/src/flext_infra/`
       - Expected: 0 errors
    6. Verify `make refactor` still works (existing CLI unchanged per D-13)
    7. Review SUMMARY decision table — should have 13 transformer entries
  </how-to-verify>
  <verify>
    <automated>/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Combined LOC of 3 target files < 385
    - Total LOC of transformers/*.py <= 4120
    - Full test suite exits 0
    - ruff check exits 0
    - pyright exits 0
    - SUMMARY contains 13 transformer decision entries
  </acceptance_criteria>
  <done>Phase 9 verified: LOC reduced, LOC gate passes, all quality gates pass, CLI unchanged, audit complete</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Full test suite green: `/home/marlonsc/flext/.venv/bin/pytest flext-infra/tests/ -x -q`
- Zero linter errors: `/home/marlonsc/flext/.venv/bin/ruff check flext-infra/src/`
- Zero type errors: `/home/marlonsc/flext/.venv/bin/pyright flext-infra/src/flext_infra/`
- LOC reduction confirmed: combined 3 target files < 385 LOC
- LOC gate: total transformers/*.py <= 4120
- CLI unchanged: `make refactor` works as before
</verification>

<success_criteria>
Phase 9 complete: rope integrated as pre/post hooks, 3 transformers migrated and simplified, remaining 13 transformers audited with per-transformer decision log, total LOC reduced, zero ruff/pyright errors, all tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rope-native-refactor-engine-rewrite/09-03-SUMMARY.md`
</output>
