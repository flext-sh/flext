---
phase: 07-modernization-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flext-tests/src/flext_tests/models.py
  - flext-tests/src/flext_tests/files.py
  - flext-core/src/flext_core/_utilities/deprecation.py
  - flext-core/src/flext_core/utilities.py
  - flext-core/src/flext_core/_utilities/__init__.py
autonomous: true
requirements: [MOD-02, MOD-06]

must_haves:
  truths:
    - "StrEnum fields on strict Pydantic models accept both string literals and enum instances"
    - "FlextUtilitiesDeprecation class no longer exists in production code"
    - "Zero UserDict/UserString in any src/ file"
    - "flext-tests test suite passes (85+ tests)"
  artifacts:
    - path: "flext-tests/src/flext_tests/models.py"
      provides: "CreateKwargsParams with BeforeValidator on fmt field"
      contains: "BeforeValidator"
    - path: "flext-core/src/flext_core/_utilities/deprecation.py"
      provides: "Empty/stub module (FROZEN file retained)"
      contains: "__all__"
  key_links:
    - from: "flext-tests/src/flext_tests/models.py"
      to: "c.Tests.Files.Format"
      via: "BeforeValidator coercion"
      pattern: "BeforeValidator.*Format"
---

<objective>
Fix StrEnum + strict Pydantic coercion mismatch breaking flext-tests, replace custom deprecation framework with PEP 702, and verify UserDict/UserString elimination.

Purpose: Close MOD-02 (deprecation framework) and MOD-06 (UserDict elimination), fix StrEnum integration breakage from Phase 2+4 interaction.
Output: Passing flext-tests suite, dead deprecation code removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-modernization-integration-fixes/07-CONTEXT.md
@.planning/phases/07-modernization-integration-fixes/07-RESEARCH.md

@flext-tests/src/flext_tests/models.py
@flext-tests/src/flext_tests/files.py
@flext-core/src/flext_core/_utilities/deprecation.py
@flext-core/src/flext_core/utilities.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix StrEnum coercion on strict Pydantic models</name>
  <files>flext-tests/src/flext_tests/models.py, flext-tests/src/flext_tests/files.py</files>
  <read_first>
    - flext-tests/src/flext_tests/models.py (see CreateKwargsParams around line 410)
    - flext-tests/src/flext_tests/files.py (see fmt="auto" at line 316)
    - flext-core/src/flext_core/_models/base.py (verify FlextModels.Value uses strict=True)
  </read_first>
  <action>
Per D-03/D-04: Fix StrEnum + strict Pydantic coercion mismatch. Use BeforeValidator approach (per research recommendation — fewer touch points than fixing all call sites).

In `flext-tests/src/flext_tests/models.py` around line 423-429, change the `fmt` field on `CreateKwargsParams` from:
```python
fmt: Annotated[
    c.Tests.Files.Format,
    Field(
        default="auto",
        description="File format override.",
    ),
]
```
to:
```python
fmt: Annotated[
    c.Tests.Files.Format,
    BeforeValidator(lambda v: c.Tests.Files.Format(v) if isinstance(v, str) else v),
    Field(
        default=c.Tests.Files.Format.AUTO,
        description="File format override.",
    ),
]
```

Add `from pydantic import BeforeValidator` to the imports (likely already has `from pydantic import Field`; add BeforeValidator to that import).

Change the default from string `"auto"` to enum `c.Tests.Files.Format.AUTO`.

In `flext-tests/src/flext_tests/files.py` line 316, change `fmt="auto"` to `fmt=c.Tests.Files.Format.AUTO`.

Also grep for any other StrEnum fields with string defaults on strict models (`FlextModels.Value` subclasses) across the entire `*/src/` tree. For each found, apply the same BeforeValidator + enum default pattern.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest flext-tests/tests/ -x --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - flext-tests/src/flext_tests/models.py contains `BeforeValidator`
    - flext-tests/src/flext_tests/models.py contains `c.Tests.Files.Format.AUTO` (not `"auto"`) as default
    - flext-tests/src/flext_tests/files.py contains `c.Tests.Files.Format.AUTO` (not `fmt="auto"`)
    - `.venv/bin/python -m pytest flext-tests/tests/ -x` exits 0
  </acceptance_criteria>
  <done>StrEnum coercion fixed — CreateKwargsParams accepts both string and enum values, flext-tests suite passes</done>
</task>

<task type="auto">
  <name>Task 2: Remove custom deprecation framework + verify UserDict elimination</name>
  <files>flext-core/src/flext_core/_utilities/deprecation.py, flext-core/src/flext_core/utilities.py, flext-core/src/flext_core/_utilities/__init__.py</files>
  <read_first>
    - flext-core/src/flext_core/_utilities/deprecation.py (dead code to remove)
    - flext-core/src/flext_core/utilities.py (MRO facade that imports FlextUtilitiesDeprecation)
    - flext-core/src/flext_core/_utilities/__init__.py (auto-generated exports)
  </read_first>
  <action>
Per D-05/D-06: Replace custom deprecation framework with PEP 702 stdlib.

1. **deprecation.py**: Since `_utilities/*` is FROZEN per AGENTS.md 10.2, do NOT delete the file. Replace the entire class body with a stub:
```python
"""Deprecation utilities — DEPRECATED.

This module is dead code (zero callers). The stdlib ``warnings.deprecated``
(PEP 702, Python 3.13+) should be used directly instead.

Module retained because _utilities/* is FROZEN per AGENTS.md 10.2.
"""

from __future__ import annotations

__all__: list[str] = []
```

2. **utilities.py**: Remove `FlextUtilitiesDeprecation` from both:
   - The import list (around line 22 where it's imported from `._utilities.deprecation`)
   - The MRO base class list of the `FlextUtilities` facade class (around line 48)

3. **Run `make codegen`** to regenerate `__init__.py` exports that reference `FlextUtilitiesDeprecation`.

4. **Verify no remaining references**: `sg --pattern 'FlextUtilitiesDeprecation' --lang py flext-core/src/` should return zero hits (except possibly the stub file docstring).

Per D-07: Verify UserDict/UserString already eliminated.
5. **Run**: `sg --pattern 'UserDict' --lang py` and `sg --pattern 'UserString' --lang py` across all `*/src/` directories. Expect zero hits. If any found, replace with Pydantic BaseModel.
  </action>
  <verify>
    <automated>sg --pattern 'FlextUtilitiesDeprecation' --lang py flext-core/src/flext_core/utilities.py; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - flext-core/src/flext_core/_utilities/deprecation.py does NOT contain `class FlextUtilitiesDeprecation`
    - flext-core/src/flext_core/_utilities/deprecation.py contains `__all__: list[str] = []`
    - flext-core/src/flext_core/utilities.py does NOT contain `FlextUtilitiesDeprecation`
    - `sg --pattern 'UserDict' --lang py` returns zero matches in `*/src/`
    - `sg --pattern 'UserString' --lang py` returns zero matches in `*/src/`
  </acceptance_criteria>
  <done>Custom deprecation framework removed (MOD-02 complete), UserDict/UserString confirmed absent (MOD-06 complete)</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest flext-tests/tests/ -x` passes (StrEnum coercion fixed)
- `sg --pattern 'FlextUtilitiesDeprecation' --lang py flext-core/src/flext_core/utilities.py` returns no matches
- `sg --pattern 'UserDict' --lang py` returns zero in `*/src/`
- `make pyre` passes (no type regressions)
</verification>

<success_criteria>
- MOD-02: FlextUtilitiesDeprecation removed, `warnings.deprecated` is the only deprecation mechanism
- MOD-06: Zero UserDict/UserString in production code (verified)
- StrEnum coercion: flext-tests suite passes with strict Pydantic models accepting both strings and enum instances
</success_criteria>

<output>
After completion, create `.planning/phases/07-modernization-integration-fixes/07-01-SUMMARY.md`
</output>
