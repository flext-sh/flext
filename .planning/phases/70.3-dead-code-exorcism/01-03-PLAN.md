---
wave: 3
depends_on: []
files_modified: []
autonomous: true
---

# 01-03: Prune Stale Branches

Remove the stale `refactor/remove-dead-code` branch if it exists.

<tasks>
  <task id="1" status="pending">
    <objective>Delete refactor/remove-dead-code branch</objective>
    <command>git branch -D refactor/remove-dead-code || echo "Branch not found, skipping"</command>
  </task>
</tasks>

<verification>
  <step>Run `git branch` and verify it's gone.</step>
</verification>

<must_haves>
  - Branch deleted if it existed.
</must_haves>
