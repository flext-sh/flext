---
phase: 10-unified-docs-generation-baseline
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - flext-infra/src/flext_infra/workspace/orchestrator.py
  - flext-infra/src/flext_infra/workspace/migrator.py
  - flext-infra/src/flext_infra/workspace/detector.py
  - flext-infra/src/flext_infra/workspace/sync.py
  - flext-infra/src/flext_infra/workspace/project_makefile.py
  - flext-infra/src/flext_infra/workspace/workspace_makefile.py
autonomous: true
requirements: [DOCS-04]

must_haves:
  truths:
    - "workspace orchestrator delegates project iteration and make execution to u.Infra.*"
    - "workspace migrator delegates file operations and toml manipulation to u.Infra.*"
    - "workspace detector delegates project discovery to u.Infra.*"
    - "workspace sync delegates synchronization logic to u.Infra.*"
  artifacts:
    - path: "flext-infra/src/flext_infra/workspace/orchestrator.py"
      provides: "Thin multi-project orchestrator"
    - path: "flext-infra/src/flext_infra/workspace/migrator.py"
      provides: "Thin workspace migration service"
    - path: "flext-infra/src/flext_infra/workspace/sync.py"
      provides: "Thin workspace sync service"
  key_links:
    - from: "workspace/orchestrator.py"
      to: "u.Infra.*"
      via: "utility delegation"
      pattern: "u\\.Infra\\."
    - from: "workspace/migrator.py"
      to: "u.Infra.*"
      via: "utility delegation"
      pattern: "u\\.Infra\\."
---

<objective>
Refactor workspace domain to thin orchestrator pattern.

Purpose: workspace (1,674 LOC, 8 files) handles multi-project orchestration, migration, detection, and sync. The orchestrator is the largest file (283 LOC) and the migrator (388 LOC). These need audit for inline subprocess/file logic.

Output: All workspace services delegate business logic to u.Infra.*, keeping only orchestration flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-unified-docs-generation-baseline/10-CONTEXT.md
@.planning/phases/10-unified-docs-generation-baseline/10-01-SUMMARY.md

<interfaces>
<!-- workspace domain structure -->
<!-- orchestrator.py (283 LOC): FlextInfraOrchestratorService(s[bool]) — multi-project make execution -->
<!-- migrator.py (388 LOC): FlextInfraMigratorService — workspace migration -->
<!-- detector.py (93 LOC): FlextInfraDetectorService — project detection -->
<!-- sync.py (246 LOC): FlextInfraSyncService — workspace synchronization -->
<!-- project_makefile.py (125 LOC): Project-level Makefile operations -->
<!-- workspace_makefile.py (206 LOC): Workspace-level Makefile operations -->
<!-- cli.py (50 LOC): CLI entry points -->

From workspace/orchestrator.py:
```python
class FlextInfraOrchestratorService(s[bool]):
    verb: Annotated[str, Field(description="Make verb to execute")]
    projects: Annotated[Sequence[str], Field(...)]
    fail_fast: Annotated[bool, Field(default=False)]
    # ... has inline loop-over-projects logic with subprocess calls
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor workspace orchestrator and detector</name>
  <files>flext-infra/src/flext_infra/workspace/orchestrator.py, flext-infra/src/flext_infra/workspace/detector.py</files>
  <read_first>
    - flext-infra/src/flext_infra/workspace/orchestrator.py (full — 283 LOC)
    - flext-infra/src/flext_infra/workspace/detector.py (full — 93 LOC)
    - flext-infra/src/flext_infra/workspace/cli.py (CLI entry)
    - flext-infra/src/flext_infra/workspace/__init__.py (exports)
    - flext-infra/src/flext_infra/_utilities/base.py (check for existing workspace utilities)
  </read_first>
  <action>
    Use `scope sketch FlextInfraOrchestratorService` and `scope sketch FlextInfraDetectorService` (if exists).

    **orchestrator.py** (283 LOC — highest risk in workspace):
    1. Audit execute() and run methods for inline subprocess calls
    2. Check for direct `subprocess.run` or `u.Infra.run_command` (latter is acceptable — it's already delegated)
    3. The loop-over-projects-and-run-make pattern is orchestration logic — acceptable in service
    4. But any inline file I/O, parsing, or report generation should use u.Infra.*
    5. Check for direct `os.environ`, `time.time()`, `Path.mkdir()` — these should use u.Infra.* equivalents if available

    **detector.py** (93 LOC — small):
    1. Audit for direct directory walking or git operations
    2. Should delegate project discovery to u.Infra.discover_projects or similar

    For inline logic found:
    - Direct subprocess calls should use u.Infra.run_command or u.Infra.run_make
    - Direct file I/O should use u.Infra.* (atomic_write_file, etc.)
    - os.environ access should use settings or c.Infra.*
    - time.time() for timing is acceptable (infrastructure, not business logic)

    After changes: `ruff check` and `pyrefly check` on modified files.
  </action>
  <verify>
    <automated>ruff check flext-infra/src/flext_infra/workspace/orchestrator.py flext-infra/src/flext_infra/workspace/detector.py && pyrefly check flext-infra/src/flext_infra/workspace/orchestrator.py flext-infra/src/flext_infra/workspace/detector.py</automated>
  </verify>
  <acceptance_criteria>
    - orchestrator.py service methods delegate subprocess execution to u.Infra.*
    - No direct subprocess.run or Popen in service methods
    - detector.py delegates project discovery to u.Infra.*
    - Loop-over-projects orchestration logic preserved (this IS the service's job)
    - `ruff check` and `pyrefly check` pass with 0 errors
  </acceptance_criteria>
  <done>workspace orchestrator and detector are thin orchestrators, zero lint/type errors</done>
</task>

<task type="auto">
  <name>Task 2: Refactor workspace migrator, sync, and makefile services</name>
  <files>flext-infra/src/flext_infra/workspace/migrator.py, flext-infra/src/flext_infra/workspace/sync.py, flext-infra/src/flext_infra/workspace/project_makefile.py, flext-infra/src/flext_infra/workspace/workspace_makefile.py</files>
  <read_first>
    - flext-infra/src/flext_infra/workspace/migrator.py (full — 388 LOC)
    - flext-infra/src/flext_infra/workspace/sync.py (full — 246 LOC)
    - flext-infra/src/flext_infra/workspace/project_makefile.py (full — 125 LOC)
    - flext-infra/src/flext_infra/workspace/workspace_makefile.py (full — 206 LOC)
  </read_first>
  <action>
    Use `scope sketch` on each service class.

    **migrator.py** (388 LOC — largest in workspace):
    1. Audit for inline TOML manipulation — should use u.Infra.toml_* utilities
    2. Check for inline file copy/move/delete — should use u.Infra.* file operations
    3. Check for inline pyproject.toml parsing — should use u.Infra.toml_parse_* or u.Infra.read_pyproject
    4. Migration logic (deciding WHAT to migrate) is orchestration — acceptable
    5. Migration execution (HOW to migrate files) is business logic — should delegate

    **sync.py** (246 LOC):
    1. Audit for inline file sync operations
    2. Check for direct shutil calls or Path operations for copying
    3. Should delegate file operations to u.Infra.*

    **project_makefile.py** (125 LOC) and **workspace_makefile.py** (206 LOC):
    1. These generate/modify Makefile content
    2. Template rendering should use u.Infra.* or Jinja2 templates
    3. Direct string building for Makefile content is business logic — extract if >10 lines

    For each file: extract inline business logic (>10 lines) to u.Infra.* utilities. Keep orchestration flow in service.

    After changes: `ruff check` and `pyrefly check` on all workspace files.
  </action>
  <verify>
    <automated>ruff check flext-infra/src/flext_infra/workspace/ && pyrefly check flext-infra/src/flext_infra/workspace/</automated>
  </verify>
  <acceptance_criteria>
    - migrator.py delegates TOML manipulation to u.Infra.toml_* utilities
    - migrator.py delegates file operations to u.Infra.* (no direct shutil or raw Path writes)
    - sync.py delegates file sync to u.Infra.*
    - makefile services delegate template rendering to u.Infra.* or use Jinja2 templates
    - `ruff check` and `pyrefly check` pass with 0 errors on workspace/
  </acceptance_criteria>
  <done>workspace migrator, sync, and makefile services are thin orchestrators, zero lint/type errors</done>
</task>

</tasks>

<verification>
- `ruff check flext-infra/src/flext_infra/workspace/` — 0 errors
- `pyrefly check flext-infra/src/flext_infra/workspace/` — 0 errors
</verification>

<success_criteria>
- All workspace services follow thin orchestrator pattern
- No inline file I/O, subprocess, or TOML manipulation in service methods
- Zero ruff + pyrefly errors across workspace domain
</success_criteria>

<output>
After completion, create `.planning/phases/10-unified-docs-generation-baseline/10-04-SUMMARY.md`
</output>
