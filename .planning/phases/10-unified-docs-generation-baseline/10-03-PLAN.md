---
phase: 10-unified-docs-generation-baseline
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - flext-infra/src/flext_infra/check/workspace_check.py
  - flext-infra/src/flext_infra/check/_workspace_check_gates.py
  - flext-infra/src/flext_infra/check/workspace_check_cli.py
  - flext-infra/src/flext_infra/validate/scanner.py
  - flext-infra/src/flext_infra/validate/basemk_validator.py
  - flext-infra/src/flext_infra/validate/namespace_validator.py
  - flext-infra/src/flext_infra/validate/skill_validator.py
autonomous: true
requirements: [DOCS-03]

must_haves:
  truths:
    - "check domain services delegate gate execution and report generation to u.Infra.*"
    - "validate domain services delegate scanning and validation to u.Infra.*"
    - "No direct argparse usage in service classes — CLI concerns stay in cli.py"
  artifacts:
    - path: "flext-infra/src/flext_infra/check/workspace_check.py"
      provides: "Thin workspace checker service"
    - path: "flext-infra/src/flext_infra/validate/scanner.py"
      provides: "Thin validation scanner service"
    - path: "flext-infra/src/flext_infra/validate/namespace_validator.py"
      provides: "Thin namespace validator service"
  key_links:
    - from: "check/workspace_check.py"
      to: "u.Infra.*"
      via: "utility delegation"
      pattern: "u\\.Infra\\."
    - from: "validate/scanner.py"
      to: "u.Infra.*"
      via: "utility delegation"
      pattern: "u\\.Infra\\."
---

<objective>
Refactor check and validate domains to thin orchestrator pattern.

Purpose: check (768 LOC) and validate (1,836 LOC) are medium-complexity domains with multiple service classes each. Both have CLI-heavy code that may mix presentation with business logic.

Output: Service classes in both domains delegate all business logic to u.Infra.*, keeping only orchestration flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-unified-docs-generation-baseline/10-CONTEXT.md
@.planning/phases/10-unified-docs-generation-baseline/10-01-SUMMARY.md

<interfaces>
<!-- check domain structure -->
<!-- workspace_check.py: FlextInfraWorkspaceChecker(FlextInfraWorkspaceCheckGatesMixin, s[bool]) -->
<!-- Has __init__ with argparse-like parsing, direct registry usage -->
<!-- workspace_check_cli.py: Heavy CLI argument parsing (165 LOC) -->
<!-- _workspace_check_gates.py: Gate registration mixin -->

<!-- validate domain structure -->
<!-- scanner.py: Validation scanning service -->
<!-- basemk_validator.py: Base.mk specific validation -->
<!-- namespace_validator.py: Namespace rule validation -->
<!-- namespace_rules.py: Rule definitions (347 LOC — library, not service) -->
<!-- skill_validator.py: Skill file validation -->
<!-- stub_chain.py: Stub chain validation -->
<!-- inventory.py: Project inventory -->
<!-- pytest_diag.py: Pytest diagnostic tool -->

From check/workspace_check.py:

```python
class FlextInfraWorkspaceChecker(FlextInfraWorkspaceCheckGatesMixin, s[bool]):
    def __init__(self, workspace_root=None, *, workspace=None):
        # Direct registry creation, report dir setup
        self._registry = FlextInfraGateRegistry.default()
```

Target thin pattern:

```python
class FlextInfraWorkspaceChecker(s[bool]):
    @override
    def execute(self) -> p.Result[bool]:
        return u.Infra.run_workspace_check(self.workspace_root, ...)
```

NOTE on pyproject.toml settings SSOT: For command domains, settings comes from FlextSettings
and c.Infra.* constants (per AGENTS.md section 2.6). Do not introduce pyproject.toml settings
reading in command services.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor check domain to thin orchestrator</name>
  <files>flext-infra/src/flext_infra/check/workspace_check.py, flext-infra/src/flext_infra/check/_workspace_check_gates.py, flext-infra/src/flext_infra/check/workspace_check_cli.py</files>
  <read_first>
    - flext-infra/src/flext_infra/check/workspace_check.py (full — 200 LOC)
    - flext-infra/src/flext_infra/check/_workspace_check_gates.py (full — 168 LOC)
    - flext-infra/src/flext_infra/check/workspace_check_cli.py (full — 165 LOC)
    - flext-infra/src/flext_infra/check/cli.py (CLI entry — 11 LOC)
    - flext-infra/src/flext_infra/check/services.py (exports)
    - flext-infra/src/flext_infra/check/__init__.py (exports)
    - flext-infra/src/flext_infra/check/maintenance/ (if exists, check structure)
    - flext-infra/src/flext_infra/utilities.py (FlextInfraUtilities MRO — check what u.Infra.* provides for check domain)
  </read_first>
  <action>
    Use `scope callers FlextInfraWorkspaceChecker` to find all callers.
    Use `scope sketch FlextInfraWorkspaceChecker` and `scope sketch FlextInfraWorkspaceCheckGatesMixin` for class structure.

    FIRST: Check what u.Infra.* provides for check operations. If a needed utility method does not exist yet, create it within this task's scope (add to appropriate _utilities/ file and wire into MRO).

    Audit workspace_check.py for violations:
    1. `__init__` with direct FlextInfraGateRegistry.default() — should gate registry be accessed via u.Infra.* or is it acceptable as DI setup?
    2. argparse usage in workspace_check.py — if present, this is a CLI concern and should be in cli.py only
    3. Direct report_dir.mkdir() — should use u.Infra.* for file operations
    4. Methods that loop over projects and run gates — this is orchestration (acceptable) vs business logic (must extract)

    Audit _workspace_check_gates.py:
    - Gate registration methods — these are configuration, not business logic (acceptable in mixin)
    - If any gate execution logic is inline, extract to u.Infra.*

    Audit workspace_check_cli.py:
    - This file IS the CLI layer — presentation logic is expected here
    - But if it contains business logic (gate evaluation, report generation), that should be in services

    Key principle: The workspace_check.py service should be a thin orchestrator:
    ```
    execute() -> iterate projects -> for each project, call u.Infra.run_gate(gate, project) -> collect results -> return r[report]
    ```

    If the existing structure already follows this pattern with the mixin providing gate configuration, that's acceptable. Document but don't force unnecessary changes.

    After changes: `ruff check flext-infra/src/flext_infra/check/` and `pyrefly check flext-infra/src/flext_infra/check/`.
  </action>
  <verify>
    <automated>ruff check flext-infra/src/flext_infra/check/ && pyrefly check flext-infra/src/flext_infra/check/</automated>
  </verify>
  <acceptance_criteria>
    - No argparse in service classes (only in cli.py files)
    - No direct file I/O (mkdir, open, write) in service methods — delegate to u.Infra.*
    - Gate execution logic delegated to u.Infra.* or gate registry (not inline in service)
    - `ruff check` and `pyrefly check` pass with 0 errors on check/
  </acceptance_criteria>
  <done>check domain services are thin orchestrators, zero lint/type errors</done>
</task>

<task type="auto">
  <name>Task 2: Refactor validate domain to thin orchestrator</name>
  <files>flext-infra/src/flext_infra/validate/scanner.py, flext-infra/src/flext_infra/validate/basemk_validator.py, flext-infra/src/flext_infra/validate/namespace_validator.py, flext-infra/src/flext_infra/validate/skill_validator.py</files>
  <read_first>
    - flext-infra/src/flext_infra/validate/scanner.py (full — 124 LOC)
    - flext-infra/src/flext_infra/validate/basemk_validator.py (full — 83 LOC)
    - flext-infra/src/flext_infra/validate/namespace_validator.py (full — 106 LOC)
    - flext-infra/src/flext_infra/validate/namespace_rules.py (full — 347 LOC, library — audit only)
    - flext-infra/src/flext_infra/validate/skill_validator.py (full — 283 LOC)
    - flext-infra/src/flext_infra/validate/stub_chain.py (full — 194 LOC)
    - flext-infra/src/flext_infra/validate/inventory.py (full — 108 LOC)
    - flext-infra/src/flext_infra/validate/pytest_diag.py (full — 244 LOC)
    - flext-infra/src/flext_infra/validate/cli.py (CLI entry)
    - flext-infra/src/flext_infra/validate/__init__.py (exports)
    - flext-infra/src/flext_infra/utilities.py (FlextInfraUtilities MRO — check what u.Infra.* provides for validate domain)
  </read_first>
  <action>
    Use `scope sketch` on each service class to understand their structure.

    FIRST: Check what u.Infra.* provides for validate operations. If a needed utility method does not exist yet, create it within this task's scope.

    For each service file in validate/:
    1. Identify service classes (those inheriting from s[T] or standalone classes with execute/run methods)
    2. Check if business logic is inline or delegated to u.Infra.*
    3. Identify file I/O, subprocess calls, or complex validation logic that should be in _utilities

    Specific files to audit:
    - **scanner.py** — If it directly walks directories or reads files, should delegate to u.Infra.*
    - **basemk_validator.py** — If it reads/parses Makefiles inline, should delegate to u.Infra.*
    - **namespace_validator.py** — If it does AST analysis inline, should delegate to u.Infra.*
    - **skill_validator.py** — Largest file (283 LOC) — most likely to have inline logic
    - **namespace_rules.py** — Library file (rule definitions) — no structural changes, just verify patterns
    - **stub_chain.py** — If it analyzes import chains inline, check for u.Infra.* delegation
    - **inventory.py** — Project inventory building — should delegate to u.Infra.*
    - **pytest_diag.py** — Diagnostic tool (244 LOC) — check for inline subprocess/parsing

    For inline business logic found:
    - If >10 lines and reusable, extract to new u.Infra.* utility
    - If <10 lines and domain-specific, acceptable as orchestration logic
    - namespace_rules.py is a rule library — no changes unless it violates naming conventions

    After changes: `ruff check flext-infra/src/flext_infra/validate/` and `pyrefly check flext-infra/src/flext_infra/validate/`.
  </action>
  <verify>
    <automated>ruff check flext-infra/src/flext_infra/validate/ && pyrefly check flext-infra/src/flext_infra/validate/</automated>
  </verify>
  <acceptance_criteria>
    - Service classes in validate/ delegate file I/O and analysis to u.Infra.*
    - namespace_rules.py verified as clean library (rule definitions only)
    - skill_validator.py refactored if inline logic >10 lines found
    - No direct subprocess calls in service classes
    - `ruff check` and `pyrefly check` pass with 0 errors on validate/
  </acceptance_criteria>
  <done>validate domain services are thin orchestrators, zero lint/type errors</done>
</task>

</tasks>

<verification>
- `ruff check flext-infra/src/flext_infra/check/ flext-infra/src/flext_infra/validate/` — 0 errors
- `pyrefly check flext-infra/src/flext_infra/check/ flext-infra/src/flext_infra/validate/` — 0 errors
</verification>

<success_criteria>

- check and validate domains follow thin orchestrator pattern
- No inline business logic in service methods (only orchestration flow)
- Zero ruff + pyrefly errors across both domains
</success_criteria>

<output>
After completion, create `.planning/phases/10-unified-docs-generation-baseline/10-03-SUMMARY.md`
</output>
