---
phase: 10-unified-docs-generation-baseline
plan: 06
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - flext-infra/src/flext_infra/deps/modernizer.py
  - flext-infra/src/flext_infra/deps/detection.py
  - flext-infra/src/flext_infra/deps/detection_analysis.py
  - flext-infra/src/flext_infra/deps/detector.py
  - flext-infra/src/flext_infra/deps/_detector_runtime.py
  - flext-infra/src/flext_infra/deps/internal_sync.py
  - flext-infra/src/flext_infra/deps/path_sync.py
  - flext-infra/src/flext_infra/deps/path_sync_rewrite.py
  - flext-infra/src/flext_infra/deps/extra_paths.py
  - flext-infra/src/flext_infra/deps/extra_paths_pyrefly.py
  - flext-infra/src/flext_infra/deps/fix_pyrefly_config.py
autonomous: true
requirements: [DOCS-06]

must_haves:
  truths:
    - "deps modernizer delegates TOML manipulation and build system changes to u.Infra.*"
    - "deps detection services delegate dependency scanning to u.Infra.*"
    - "deps sync services delegate file synchronization to u.Infra.*"
    - "No direct toml/pyproject file manipulation in service methods — all through u.Infra.toml_*"
  artifacts:
    - path: "flext-infra/src/flext_infra/deps/modernizer.py"
      provides: "Thin dependency modernizer service"
    - path: "flext-infra/src/flext_infra/deps/detection.py"
      provides: "Thin detection service"
    - path: "flext-infra/src/flext_infra/deps/internal_sync.py"
      provides: "Thin internal sync service"
  key_links:
    - from: "deps/modernizer.py"
      to: "u.Infra.toml_*"
      via: "TOML utility delegation"
      pattern: "u\\.Infra\\."
    - from: "deps/detection.py"
      to: "u.Infra.*"
      via: "utility delegation"
      pattern: "u\\.Infra\\."
---

<objective>
Refactor deps domain to thin orchestrator pattern.

Purpose: deps (2,942 LOC, 13 files) is the second most complex command domain. It handles dependency detection, modernization (pyproject.toml management), path sync, and pyrefly config fixing. The modernizer alone is 392 LOC.

Output: All deps services delegate business logic to u.Infra.*, keeping only orchestration flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-unified-docs-generation-baseline/10-CONTEXT.md
@.planning/phases/10-unified-docs-generation-baseline/10-01-SUMMARY.md

<interfaces>
<!-- deps domain structure (13 files, 2942 LOC) -->
<!-- modernizer.py (392 LOC): FlextInfraDepsModernizer — pyproject.toml modernization -->
<!-- detection.py (154 LOC): FlextInfraDepsDetection — dependency detection -->
<!-- detection_analysis.py (361 LOC): Analysis helper -->
<!-- detector.py (132 LOC): FlextInfraDepsDetector — dependency detector -->
<!-- _detector_runtime.py (216 LOC): Runtime detection helper -->
<!-- internal_sync.py (279 LOC): FlextInfraInternalSync — internal dep sync -->
<!-- path_sync.py (193 LOC): FlextInfraPathSync — path synchronization -->
<!-- path_sync_rewrite.py (259 LOC): Path sync rewrite operations -->
<!-- extra_paths.py (222 LOC): Extra path management -->
<!-- extra_paths_pyrefly.py (128 LOC): Pyrefly extra path management -->
<!-- fix_pyrefly_config.py (280 LOC): FlextInfraConfigFixer — pyrefly config fixer -->
<!-- cli.py (111 LOC): CLI entry points -->

Key utilities: u.Infra.toml_*, u.Infra.read_pyproject, u.Infra.parse_*, u.Infra.discover_projects

NOTE on pyproject.toml config SSOT: For command domains, config comes from FlextSettings
and c.Infra.* constants (per AGENTS.md section 2.6). The deps domain reads/modifies pyproject.toml
as its JOB (modernization), not as config source. This is correct — pyproject.toml is the data
being operated on, not the configuration driving the operation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor deps modernizer and detection services</name>
  <files>flext-infra/src/flext_infra/deps/modernizer.py, flext-infra/src/flext_infra/deps/detection.py, flext-infra/src/flext_infra/deps/detection_analysis.py, flext-infra/src/flext_infra/deps/detector.py, flext-infra/src/flext_infra/deps/_detector_runtime.py</files>
  <read_first>
    - flext-infra/src/flext_infra/deps/modernizer.py (full — 392 LOC)
    - flext-infra/src/flext_infra/deps/detection.py (full — 154 LOC)
    - flext-infra/src/flext_infra/deps/detection_analysis.py (full — 361 LOC)
    - flext-infra/src/flext_infra/deps/detector.py (full — 132 LOC)
    - flext-infra/src/flext_infra/deps/_detector_runtime.py (full — 216 LOC)
    - flext-infra/src/flext_infra/deps/cli.py (CLI entry)
    - flext-infra/src/flext_infra/deps/__init__.py (exports)
    - flext-infra/src/flext_infra/utilities.py (FlextInfraUtilities MRO — check what u.Infra.* provides for deps)
  </read_first>
  <action>
    Use `scope sketch` on FlextInfraDepsModernizer, FlextInfraDepsDetection, FlextInfraDepsDetector.

    FIRST: Check what u.Infra.* provides for deps operations. If a needed utility method does not exist yet, create it within this task's scope (add to appropriate _utilities/ file and wire into MRO).

    **modernizer.py** (392 LOC — most complex):
    1. This modernizes pyproject.toml files across projects
    2. Check for direct tomlkit/toml manipulation — MUST use u.Infra.toml_* utilities
    3. Check for direct file reads/writes — MUST use u.Infra.* file operations
    4. Decision logic (what to modernize) is orchestration — acceptable
    5. Execution logic (how to modify TOML) is business — must delegate

    **detection.py** and **detection_analysis.py**:
    1. Check for inline dependency analysis (import parsing, requirement comparison)
    2. Should delegate to u.Infra.* for import analysis and dependency comparison
    3. detection_analysis.py (361 LOC) is a helper — if it's pure logic, consider extracting to u.Infra.*

    **detector.py** and **_detector_runtime.py**:
    1. Check for inline runtime detection logic
    2. Should delegate to u.Infra.* for system introspection

    For each file with inline business logic >10 lines:
    - If u.Infra.* has matching utility, refactor to delegate
    - If no matching utility exists, CREATE the utility method in the appropriate _utilities/ file and wire into u.Infra.* MRO facade
    - For detection_analysis.py (361 LOC), if it's a pure helper: leave as internal helper but document

    After changes: `ruff check` and `pyrefly check` on modified files.
  </action>
  <verify>
    <automated>ruff check flext-infra/src/flext_infra/deps/modernizer.py flext-infra/src/flext_infra/deps/detection.py flext-infra/src/flext_infra/deps/detection_analysis.py flext-infra/src/flext_infra/deps/detector.py flext-infra/src/flext_infra/deps/_detector_runtime.py && pyrefly check flext-infra/src/flext_infra/deps/modernizer.py flext-infra/src/flext_infra/deps/detection.py flext-infra/src/flext_infra/deps/detection_analysis.py flext-infra/src/flext_infra/deps/detector.py flext-infra/src/flext_infra/deps/_detector_runtime.py</automated>
  </verify>
  <acceptance_criteria>
    - modernizer.py delegates TOML manipulation to u.Infra.toml_* (no direct tomlkit usage in service methods)
    - detection services delegate import analysis to u.Infra.*
    - No direct file I/O in service execute/run methods
    - detection_analysis.py documented as internal helper or extracted to u.Infra.*
    - `ruff check` and `pyrefly check` pass with 0 errors
  </acceptance_criteria>
  <done>deps modernizer and detection services are thin orchestrators, zero lint/type errors</done>
</task>

<task type="auto">
  <name>Task 2: Refactor deps sync and config fixer services</name>
  <files>flext-infra/src/flext_infra/deps/internal_sync.py, flext-infra/src/flext_infra/deps/path_sync.py, flext-infra/src/flext_infra/deps/path_sync_rewrite.py, flext-infra/src/flext_infra/deps/extra_paths.py, flext-infra/src/flext_infra/deps/extra_paths_pyrefly.py, flext-infra/src/flext_infra/deps/fix_pyrefly_config.py</files>
  <read_first>
    - flext-infra/src/flext_infra/deps/internal_sync.py (full — 279 LOC)
    - flext-infra/src/flext_infra/deps/path_sync.py (full — 193 LOC)
    - flext-infra/src/flext_infra/deps/path_sync_rewrite.py (full — 259 LOC)
    - flext-infra/src/flext_infra/deps/extra_paths.py (full — 222 LOC)
    - flext-infra/src/flext_infra/deps/extra_paths_pyrefly.py (full — 128 LOC)
    - flext-infra/src/flext_infra/deps/fix_pyrefly_config.py (full — 280 LOC)
    - flext-infra/src/flext_infra/utilities.py (FlextInfraUtilities MRO — check what u.Infra.* provides)
  </read_first>
  <action>
    Use `scope sketch` on each service class.

    FIRST: Check what u.Infra.* provides for sync/config operations. If a needed utility method does not exist yet, create it within this task's scope.

    **internal_sync.py** (279 LOC):
    - Internal dependency synchronization
    - Check for inline TOML manipulation — must use u.Infra.toml_*
    - Check for inline file diff/copy operations — must use u.Infra.*

    **path_sync.py** (193 LOC) and **path_sync_rewrite.py** (259 LOC):
    - Path synchronization between projects
    - Check for inline config file manipulation
    - Should delegate to u.Infra.* for file operations

    **extra_paths.py** (222 LOC) and **extra_paths_pyrefly.py** (128 LOC):
    - Extra path management for type checkers
    - Check for inline TOML/config manipulation
    - Should delegate to u.Infra.toml_* and u.Infra.* file operations

    **fix_pyrefly_config.py** (280 LOC):
    - FlextInfraConfigFixer(s[bool]) — pyrefly config fixer
    - Check for inline config file parsing and modification
    - Should delegate to u.Infra.* for config file operations

    For each: extract inline business logic >10 lines to u.Infra.* utilities (create new utility methods as needed). Keep orchestration flow.

    After changes: `ruff check` and `pyrefly check` on all deps/ files.
  </action>
  <verify>
    <automated>ruff check flext-infra/src/flext_infra/deps/ && pyrefly check flext-infra/src/flext_infra/deps/</automated>
  </verify>
  <acceptance_criteria>
    - sync services delegate file operations to u.Infra.*
    - path_sync services delegate config manipulation to u.Infra.*
    - fix_pyrefly_config delegates config parsing/modification to u.Infra.*
    - No direct TOML manipulation in service methods (use u.Infra.toml_*)
    - `ruff check` and `pyrefly check` pass with 0 errors on deps/
  </acceptance_criteria>
  <done>deps sync and config fixer services are thin orchestrators, zero lint/type errors</done>
</task>

</tasks>

<verification>
- `ruff check flext-infra/src/flext_infra/deps/` — 0 errors
- `pyrefly check flext-infra/src/flext_infra/deps/` — 0 errors
</verification>

<success_criteria>
- All deps services follow thin orchestrator pattern
- No inline TOML manipulation or file I/O in service methods
- Zero ruff + pyrefly errors across deps domain
</success_criteria>

<output>
After completion, create `.planning/phases/10-unified-docs-generation-baseline/10-06-SUMMARY.md`
</output>
