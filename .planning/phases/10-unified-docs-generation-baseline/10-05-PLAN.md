---
phase: 10-unified-docs-generation-baseline
plan: 05
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - flext-infra/src/flext_infra/codegen/lazy_init.py
  - flext-infra/src/flext_infra/codegen/census.py
  - flext-infra/src/flext_infra/codegen/scaffolder.py
  - flext-infra/src/flext_infra/codegen/fixer.py
  - flext-infra/src/flext_infra/codegen/constants_quality_gate.py
  - flext-infra/src/flext_infra/codegen/_codegen_generation.py
  - flext-infra/src/flext_infra/codegen/py_typed.py
  - flext-infra/src/flext_infra/services/pipeline.py
  - flext-infra/src/flext_infra/services/consolidator.py
  - flext-infra/src/flext_infra/services/deduplicator.py
autonomous: true
requirements: [DOCS-05]

must_haves:
  truths:
    - "codegen services delegate code generation, census, and scaffolding to u.Infra.*"
    - "codegen services are thin orchestrators — no inline AST manipulation or file generation"
    - "pipeline/consolidator/deduplicator services in services/ delegate to u.Infra.*"
  artifacts:
    - path: "flext-infra/src/flext_infra/codegen/lazy_init.py"
      provides: "Thin lazy init code generator service"
    - path: "flext-infra/src/flext_infra/codegen/fixer.py"
      provides: "Thin code fixer service"
    - path: "flext-infra/src/flext_infra/codegen/census.py"
      provides: "Thin census service"
  key_links:
    - from: "codegen/*.py"
      to: "u.Infra.*"
      via: "utility delegation"
      pattern: "u\\.Infra\\."
    - from: "services/*.py"
      to: "u.Infra.*"
      via: "utility delegation"
      pattern: "u\\.Infra\\."
---

<objective>
Refactor codegen domain and root services/ to thin orchestrator pattern.

Purpose: codegen (1,826 LOC, 10 files) has 7 service classes and a _codegen_generation helper. The root services/ (325 LOC, 4 files) contains pipeline, consolidator, and deduplicator. These need audit for inline code generation logic. Task 1 is split into two parts (1a: 696 LOC, 1b: 874 LOC) to keep each task under the size cap.

Output: All codegen and services/ classes delegate business logic to u.Infra.*, keeping only orchestration flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-unified-docs-generation-baseline/10-CONTEXT.md
@.planning/phases/10-unified-docs-generation-baseline/10-01-SUMMARY.md

<interfaces>
<!-- codegen domain structure -->
<!-- lazy_init.py (274 LOC): FlextInfraLazyInitService — generates __init__.py with lazy imports -->
<!-- census.py (263 LOC): FlextInfraCensusService — counts/reports code metrics -->
<!-- scaffolder.py (159 LOC): FlextInfraScaffolder — generates project scaffolding -->
<!-- fixer.py (326 LOC): FlextInfraFixer — fixes code issues -->
<!-- constants_quality_gate.py (189 LOC): FlextInfraConstantsQualityGate — validates constants -->
<!-- _codegen_generation.py (284 LOC): Generation helper (private) -->
<!-- py_typed.py (75 LOC): py.typed marker management -->
<!-- _utilities.py (30 LOC): Small codegen utilities -->

<!-- services/ at root level (codegen-related) -->
<!-- pipeline.py (79 LOC): FlextInfraCodegenPipeline -->
<!-- consolidator.py (138 LOC): FlextInfraConsolidatorService -->
<!-- deduplicator.py (31 LOC): FlextInfraDeduplicatorService -->

Codegen has an existing FlextInfraUtilitiesCodegen in _utilities that provides utility methods.
Check: `from flext_infra import FlextInfraUtilitiesCodegen` in utilities.py facade.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Refactor codegen lazy_init, census, scaffolder (696 LOC)</name>
  <files>flext-infra/src/flext_infra/codegen/lazy_init.py, flext-infra/src/flext_infra/codegen/census.py, flext-infra/src/flext_infra/codegen/scaffolder.py</files>
  <read_first>
    - flext-infra/src/flext_infra/codegen/lazy_init.py (full — 274 LOC)
    - flext-infra/src/flext_infra/codegen/census.py (full — 263 LOC)
    - flext-infra/src/flext_infra/codegen/scaffolder.py (full — 159 LOC)
    - flext-infra/src/flext_infra/codegen/_utilities.py (30 LOC)
    - flext-infra/src/flext_infra/codegen/cli.py (CLI entry)
    - flext-infra/src/flext_infra/codegen/__init__.py (exports)
    - flext-infra/src/flext_infra/utilities.py (FlextInfraUtilities MRO — check what u.Infra.* provides for codegen)
  </read_first>
  <action>
    Use `scope sketch` on each service class: FlextInfraLazyInitService, FlextInfraCensusService, FlextInfraScaffolder.

    FIRST: Check what FlextInfraUtilitiesCodegen provides (it's already in the u.Infra.* MRO). If a needed utility method does not exist yet, create it within this task's scope.

    **lazy_init.py** (274 LOC — generates __init__.py files):
    - Most likely has inline AST/code generation logic
    - This SHOULD delegate to u.Infra.* for: file reading, import analysis, __init__.py content generation
    - Orchestration (deciding which files to generate) is acceptable in service

    **census.py** (263 LOC):
    - Code metrics counting — check for inline file parsing
    - Should delegate counting to u.Infra.* and just orchestrate report generation

    **scaffolder.py** (159 LOC):
    - Project scaffolding — check for inline template rendering
    - Should use u.Infra.* for file creation and template rendering

    For inline logic >10 lines: extract to u.Infra.* (via FlextInfraUtilitiesCodegen or new utility mixin).
    After changes: `ruff check` and `pyrefly check` on modified files.
  </action>
  <verify>
    <automated>ruff check flext-infra/src/flext_infra/codegen/lazy_init.py flext-infra/src/flext_infra/codegen/census.py flext-infra/src/flext_infra/codegen/scaffolder.py && pyrefly check flext-infra/src/flext_infra/codegen/lazy_init.py flext-infra/src/flext_infra/codegen/census.py flext-infra/src/flext_infra/codegen/scaffolder.py</automated>
  </verify>
  <acceptance_criteria>
    - lazy_init.py delegates code generation to u.Infra.* (no inline __init__.py content building)
    - census.py delegates metric counting to u.Infra.*
    - scaffolder.py delegates file creation to u.Infra.*
    - `ruff check` and `pyrefly check` pass with 0 errors
  </acceptance_criteria>
  <done>codegen lazy_init, census, scaffolder are thin orchestrators, zero lint/type errors</done>
</task>

<task type="auto">
  <name>Task 1b: Refactor codegen fixer, _codegen_generation, constants_quality_gate, py_typed (874 LOC)</name>
  <files>flext-infra/src/flext_infra/codegen/fixer.py, flext-infra/src/flext_infra/codegen/_codegen_generation.py, flext-infra/src/flext_infra/codegen/constants_quality_gate.py, flext-infra/src/flext_infra/codegen/py_typed.py</files>
  <read_first>
    - flext-infra/src/flext_infra/codegen/fixer.py (full — 326 LOC)
    - flext-infra/src/flext_infra/codegen/_codegen_generation.py (full — 284 LOC)
    - flext-infra/src/flext_infra/codegen/constants_quality_gate.py (full — 189 LOC)
    - flext-infra/src/flext_infra/codegen/py_typed.py (full — 75 LOC)
    - flext-infra/src/flext_infra/utilities.py (FlextInfraUtilities MRO — check what u.Infra.* provides)
  </read_first>
  <action>
    Use `scope sketch` on FlextInfraFixer, FlextInfraConstantsQualityGate.

    FIRST: Check what u.Infra.* provides. If a needed utility method does not exist yet, create it within this task's scope.

    **fixer.py** (326 LOC — largest, code fixer):
    - Likely has inline code manipulation
    - Check for direct AST/LibCST/rope usage — should go through u.Infra.*
    - Check for direct file writes — should use u.Infra.atomic_write_file

    **_codegen_generation.py** (284 LOC — private helper):
    - This is a helper module, not a service class
    - If it contains business logic, it should be absorbed into u.Infra.* utility classes
    - If it's already used ONLY by codegen services, it's acceptable as internal helper

    **constants_quality_gate.py** (189 LOC) and **py_typed.py** (75 LOC):
    - Smaller files — audit for inline logic
    - constants_quality_gate should delegate validation to u.Infra.*
    - py_typed should delegate file operations to u.Infra.*

    For inline logic >10 lines: extract to u.Infra.* (via FlextInfraUtilitiesCodegen or new utility mixin).
    After changes: `ruff check` and `pyrefly check` on modified files.
  </action>
  <verify>
    <automated>ruff check flext-infra/src/flext_infra/codegen/fixer.py flext-infra/src/flext_infra/codegen/_codegen_generation.py flext-infra/src/flext_infra/codegen/constants_quality_gate.py flext-infra/src/flext_infra/codegen/py_typed.py && pyrefly check flext-infra/src/flext_infra/codegen/fixer.py flext-infra/src/flext_infra/codegen/_codegen_generation.py flext-infra/src/flext_infra/codegen/constants_quality_gate.py flext-infra/src/flext_infra/codegen/py_typed.py</automated>
  </verify>
  <acceptance_criteria>
    - fixer.py delegates code manipulation to u.Infra.* (no direct AST/LibCST in service methods)
    - No direct file writes in service methods — use u.Infra.atomic_write_file
    - _codegen_generation.py either absorbed into u.Infra.* or documented as acceptable internal helper
    - `ruff check` and `pyrefly check` pass with 0 errors
  </acceptance_criteria>
  <done>codegen fixer, generation helper, quality gate, py_typed are thin orchestrators, zero lint/type errors</done>
</task>

<task type="auto">
  <name>Task 2: Refactor root services/ (pipeline, consolidator, deduplicator)</name>
  <files>flext-infra/src/flext_infra/services/pipeline.py, flext-infra/src/flext_infra/services/consolidator.py, flext-infra/src/flext_infra/services/deduplicator.py</files>
  <read_first>
    - flext-infra/src/flext_infra/services/pipeline.py (full — 79 LOC)
    - flext-infra/src/flext_infra/services/consolidator.py (full — 138 LOC)
    - flext-infra/src/flext_infra/services/deduplicator.py (full — 31 LOC)
    - flext-infra/src/flext_infra/services/__init__.py (exports — 77 LOC)
  </read_first>
  <action>
    Use `scope sketch` on each class: FlextInfraCodegenPipeline, FlextInfraConsolidatorService, FlextInfraDeduplicatorService.
    Use `scope callers` on each to understand who uses them.

    These are codegen-related services living in a root `services/` directory:
    - **pipeline.py** (79 LOC) — likely orchestrates codegen phases
    - **consolidator.py** (138 LOC) — consolidates codegen outputs
    - **deduplicator.py** (31 LOC) — deduplicates something

    Audit each for thin orchestrator compliance:
    1. Check if they delegate to u.Infra.* or call codegen services directly
    2. If they have inline business logic, extract to u.Infra.*
    3. deduplicator.py is only 31 LOC — likely already clean

    After changes: `ruff check flext-infra/src/flext_infra/services/` and `pyrefly check flext-infra/src/flext_infra/services/`.
  </action>
  <verify>
    <automated>ruff check flext-infra/src/flext_infra/services/ && pyrefly check flext-infra/src/flext_infra/services/</automated>
  </verify>
  <acceptance_criteria>
    - pipeline.py orchestrates codegen phases via delegation (no inline generation)
    - consolidator.py delegates file operations to u.Infra.*
    - deduplicator.py verified as thin (31 LOC — likely already clean)
    - `ruff check` and `pyrefly check` pass with 0 errors on services/
  </acceptance_criteria>
  <done>root services/ classes are thin orchestrators, zero lint/type errors</done>
</task>

</tasks>

<verification>
- `ruff check flext-infra/src/flext_infra/codegen/ flext-infra/src/flext_infra/services/` — 0 errors
- `pyrefly check flext-infra/src/flext_infra/codegen/ flext-infra/src/flext_infra/services/` — 0 errors
</verification>

<success_criteria>
- All codegen and services/ classes follow thin orchestrator pattern
- Code generation logic delegated to u.Infra.* utilities
- Zero ruff + pyrefly errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-unified-docs-generation-baseline/10-05-SUMMARY.md`
</output>
