---
phase: 10-unified-docs-generation-baseline
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - flext-infra/src/flext_infra/basemk/engine.py
  - flext-infra/src/flext_infra/basemk/generator.py
  - flext-infra/src/flext_infra/github/service.py
  - flext-infra/src/flext_infra/release/orchestrator.py
  - flext-infra/src/flext_infra/release/orchestrator_phases.py
autonomous: true
requirements: [DOCS-02]

must_haves:
  truths:
    - "basemk services delegate all business logic to u.Infra.* — no inline Jinja2 or template logic in service methods"
    - "github service is already thin orchestrator pattern — verify and document"
    - "release orchestrator delegates phase logic to u.Infra.* — no inline subprocess or version logic"
  artifacts:
    - path: "flext-infra/src/flext_infra/basemk/engine.py"
      provides: "Thin template engine service"
    - path: "flext-infra/src/flext_infra/basemk/generator.py"
      provides: "Thin generator service"
    - path: "flext-infra/src/flext_infra/github/service.py"
      provides: "Already-thin GitHub service (verify)"
    - path: "flext-infra/src/flext_infra/release/orchestrator.py"
      provides: "Thin release orchestrator service"
  key_links:
    - from: "basemk/engine.py"
      to: "u.Infra.*"
      via: "utility delegation"
      pattern: "u\\.Infra\\."
    - from: "github/service.py"
      to: "u.Infra.*"
      via: "utility delegation"
      pattern: "u\\.Infra\\."
    - from: "release/orchestrator.py"
      to: "u.Infra.*"
      via: "utility delegation"
      pattern: "u\\.Infra\\."
---

<objective>
Refactor the three simplest command domains (basemk, github, release) to thin orchestrator pattern.

Purpose: These are low-LOC, low-risk domains (basemk=397, github=173, release=688 LOC). Refactoring them first builds confidence in the pattern and provides templates for medium/complex domains.

Output: All three domains follow thin-orchestrator pattern — service methods validate inputs, call u.Infra.*, return r[T].
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-unified-docs-generation-baseline/10-CONTEXT.md
@.planning/phases/10-unified-docs-generation-baseline/10-01-SUMMARY.md

<interfaces>
<!-- github/service.py is ALREADY thin — delegates to u.Infra.github_sync_workflows, etc. -->
<!-- basemk has inline Jinja2 logic in engine.py and generator.py -->
<!-- release has inline subprocess/version logic in orchestrator.py -->

From flext-infra/src/flext_infra/github/service.py (REFERENCE — already correct):
```python
class FlextInfraGithubService:
    def execute_workflow_sync(self, request) -> r[m.Infra.GithubWorkflowSyncReport]:
        return u.Infra.github_sync_workflows(request)

    def execute_workflow_lint(self, request) -> r[m.Infra.GithubWorkflowLintOutcome]:
        return u.Infra.github_lint_workflows(request)
```

From flext-infra/src/flext_infra/basemk/engine.py (needs refactoring):
- FlextInfraBaseMkTemplateEngine(s[str]) — has inline Jinja2 Environment setup
- _TEMPLATES_DIR module-level constant
- _build_environment() module-level function

From flext-infra/src/flext_infra/release/orchestrator.py (needs audit):
- FlextInfraReleaseOrchestrator(FlextInfraReleaseOrchestratorPhases, s[bool])
- _resolve_version has inline logic but already delegates to u.Infra.*

NOTE on pyproject.toml config SSOT: For command domains, config comes from FlextSettings
and c.Infra.* constants (per AGENTS.md section 2.6). pyproject.toml SSOT applies primarily to the
excluded docs domain. Do not introduce pyproject.toml config reading in command services.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor basemk domain to thin orchestrator</name>
  <files>flext-infra/src/flext_infra/basemk/engine.py, flext-infra/src/flext_infra/basemk/generator.py</files>
  <read_first>
    - flext-infra/src/flext_infra/basemk/engine.py (full file — 98 LOC)
    - flext-infra/src/flext_infra/basemk/generator.py (full file — 159 LOC)
    - flext-infra/src/flext_infra/basemk/cli.py (CLI entry points)
    - flext-infra/src/flext_infra/basemk/__init__.py (exports)
    - flext-infra/src/flext_infra/_utilities/base.py (check for existing basemk utilities in u.Infra.* facade)
    - flext-infra/src/flext_infra/utilities.py (FlextInfraUtilities MRO — see what u.Infra.* provides)
    - flext-infra/src/flext_infra/github/service.py (reference thin pattern)
  </read_first>
  <action>
    Use `scope callers FlextInfraBaseMkTemplateEngine` and `scope callers FlextInfraBaseMkGenerator` to find all callers.

    FIRST: Check what u.Infra.* already provides for basemk by reading the utilities facade. If a needed utility method does not exist yet, create it within this task's scope (add to the appropriate _utilities/ file and wire into the MRO facade).

    Audit basemk/engine.py and basemk/generator.py for inline business logic:
    1. If Jinja2 Environment setup is inline in engine.py — check if u.Infra already has template rendering utilities
    2. If generator.py has inline template rendering — should delegate to u.Infra.* or to the engine

    For each file:
    - Identify methods with >5 lines of business logic (not just delegation)
    - If business logic exists that should be in _utilities, extract to u.Infra.* (create new utility method if needed)
    - Service methods should be: validate input -> call u.Infra.* -> return r[T]

    Key things to check:
    - `_build_environment()` module-level function — if only used by engine, keep in engine (it's config, not business logic)
    - `_render_template()` module-level function — same
    - `_TEMPLATES_DIR` — acceptable as module constant

    The basemk domain is small (397 LOC total). If services already mostly delegate to u.Infra.*, document that and make minimal changes. Don't force-extract what's already clean.

    After changes, run `ruff check flext-infra/src/flext_infra/basemk/` and `pyrefly check flext-infra/src/flext_infra/basemk/`.
  </action>
  <verify>
    <automated>ruff check flext-infra/src/flext_infra/basemk/ && pyrefly check flext-infra/src/flext_infra/basemk/</automated>
  </verify>
  <acceptance_criteria>
    - No inline Jinja2 business logic in service execute() methods — only delegation calls
    - Module-level helper functions (_build_environment, _render_template) acceptable if small
    - Service methods follow pattern: validate -> u.Infra.* -> return r[T]
    - `ruff check` and `pyrefly check` pass with 0 errors on basemk/
  </acceptance_criteria>
  <done>basemk services are thin orchestrators delegating to u.Infra.*, zero lint/type errors</done>
</task>

<task type="auto">
  <name>Task 2: Audit github + release domains for thin orchestrator compliance</name>
  <files>flext-infra/src/flext_infra/github/service.py, flext-infra/src/flext_infra/release/orchestrator.py, flext-infra/src/flext_infra/release/orchestrator_phases.py</files>
  <read_first>
    - flext-infra/src/flext_infra/github/service.py (52 LOC — likely already compliant)
    - flext-infra/src/flext_infra/github/cli.py (CLI entry)
    - flext-infra/src/flext_infra/github/__init__.py (exports)
    - flext-infra/src/flext_infra/release/orchestrator.py (full file)
    - flext-infra/src/flext_infra/release/orchestrator_phases.py (full file — 200 LOC)
    - flext-infra/src/flext_infra/release/cli.py (CLI entry)
    - flext-infra/src/flext_infra/release/__init__.py (exports)
    - flext-infra/src/flext_infra/utilities.py (FlextInfraUtilities MRO — see what u.Infra.* provides for release)
  </read_first>
  <action>
    **GitHub domain:**
    Use `scope sketch FlextInfraGithubService` to see its structure.
    github/service.py is already a perfect thin orchestrator (52 LOC, all methods delegate to u.Infra.*).
    Verify and document — no changes expected.

    **Release domain:**
    Use `scope sketch FlextInfraReleaseOrchestrator` and `scope sketch FlextInfraReleaseOrchestratorPhases` to understand structure.

    FIRST: Check what u.Infra.* provides for release operations. If a needed utility method does not exist yet, create it within this task's scope.

    Audit orchestrator.py for inline business logic:
    - `_resolve_version()` — has inline logic but delegates to u.Infra.parse_semver, u.Infra.current_workspace_version, u.Infra.bump_version. This is orchestration logic (choosing which utility to call based on input), which is acceptable.
    - Check `run_release()` and other methods for direct subprocess calls or inline logic that should be in u.Infra.*

    Audit orchestrator_phases.py:
    - Check each phase method (validate, version, build, publish) for inline logic vs delegation
    - If methods directly call subprocess or manipulate files without u.Infra.*, extract to utilities

    For any inline business logic found:
    - If u.Infra.* already has a matching utility, refactor to delegate
    - If no utility exists and logic is >10 lines, create a new u.Infra.* utility method
    - If logic is <10 lines and domain-specific, leave in service (orchestration logic is OK)

    After changes, run `ruff check` and `pyrefly check` on both domains.
  </action>
  <verify>
    <automated>ruff check flext-infra/src/flext_infra/github/ flext-infra/src/flext_infra/release/ && pyrefly check flext-infra/src/flext_infra/github/ flext-infra/src/flext_infra/release/</automated>
  </verify>
  <acceptance_criteria>
    - github/service.py confirmed as thin orchestrator (no changes needed unless anti-pattern found)
    - release/orchestrator.py methods delegate to u.Infra.* for subprocess, file, and version operations
    - release/orchestrator_phases.py phase methods are orchestration-only (validate->call->return pattern)
    - No direct subprocess.run or raw file I/O in service methods (must go through u.Infra.*)
    - `ruff check` and `pyrefly check` pass with 0 errors on github/ and release/
  </acceptance_criteria>
  <done>github confirmed compliant, release refactored to thin orchestrator, zero lint/type errors</done>
</task>

</tasks>

<verification>
- `ruff check flext-infra/src/flext_infra/basemk/ flext-infra/src/flext_infra/github/ flext-infra/src/flext_infra/release/` — 0 errors
- `pyrefly check flext-infra/src/flext_infra/basemk/ flext-infra/src/flext_infra/github/ flext-infra/src/flext_infra/release/` — 0 errors
- All service methods in these domains follow: validate -> u.Infra.* -> return r[T]
</verification>

<success_criteria>
- basemk, github, release domains all follow thin orchestrator pattern
- No inline business logic in service execute()/run() methods
- Zero ruff + pyrefly errors across all three domains
</success_criteria>

<output>
After completion, create `.planning/phases/10-unified-docs-generation-baseline/10-02-SUMMARY.md`
</output>
