---
phase: quick
plan: 260323-r3o
type: execute
wave: 1
depends_on: []
files_modified: [] # Determined dynamically by vulture/qlty output
autonomous: true
requirements: [YAGNI-cleanup]

must_haves:
  truths:
    - "All dead code identified by vulture (min-confidence 80) is removed"
    - "All code smells identified by qlty smells are addressed or documented"
    - "Zero new ruff lint errors introduced"
    - "Zero new pyrefly type errors introduced"
    - "All existing pytest tests still pass"
    - "All __init__.py re-exports remain valid after removals"
  artifacts: []
  key_links:
    - from: "removed symbols"
      to: "__init__.py exports"
      via: "make gen after removal"
      pattern: "__all__|_LAZY_IMPORTS"
---

<objective>
Apply YAGNI patterns across the flext monorepo by removing dead/unused code detected by vulture and addressing code smells from qlty. All removals must be validated against ruff, pyrefly, and pytest to ensure zero regressions.

Purpose: Reduce codebase surface area, improve maintainability, eliminate unused code that adds cognitive load and type-checking burden.
Output: Cleaner codebase with dead code removed and zero quality gate regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md (sections 3.1, 3.5 — dead code deletion is explicitly encouraged as VALID code reduction)
@CLAUDE.md (tooling rules — use make targets, not direct tool calls)
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dead code discovery and baseline capture</name>
  <files>
    .planning/quick/260323-r3o-apply-yagni-patterns-using-vulture-and-q/vulture-report.txt
    .planning/quick/260323-r3o-apply-yagni-patterns-using-vulture-and-q/qlty-report.txt
    .planning/quick/260323-r3o-apply-yagni-patterns-using-vulture-and-q/baseline.txt
  </files>
  <action>
    1. Capture current quality baseline:
       - Run `make check PROJECT=flext-core CHECK_GATES=lint 2>&1 | tail -5` and record pass/fail
       - Run `make check PROJECT=flext-core CHECK_GATES=pyrefly 2>&1 | tail -5` and record pass/fail
       - Save baseline counts to `baseline.txt`

    2. Run vulture across all src/ directories with min-confidence 80:
       ```
       /home/marlonsc/flext/.venv/bin/vulture */src/ --min-confidence 80 \
         --exclude "*_pb2*,*__pycache__*,*.venv*,*node_modules*,*migrations*" \
         --ignore-decorators "@app.route,@router.*,@handler,@field_validator,@model_validator,@computed_field,@override,@abstractmethod,@property,@classmethod,@staticmethod,@pytest.fixture,@pytest.mark.*" \
         --ignore-names "model_config,model_fields,__all__,__getattr__,_LAZY_IMPORTS,visit_*,test_*,setup_*,teardown_*,conftest,pytest_*" \
         > vulture-report.txt 2>&1
       ```

    3. Run qlty smells for all src/ code:
       ```
       qlty smells --all --no-snippets */src/ > qlty-report.txt 2>&1
       ```

    4. Analyze vulture output:
       - Group findings by project (flext-core, flext-api, etc.)
       - Categorize: unused functions, unused classes, unused imports, unused variables, unused attributes
       - Filter out false positives: framework hooks, protocol implementations, MRO-inherited methods, __init__.py exports, Pydantic model fields, test fixtures
       - For EACH candidate removal, use `sg` (ast-grep) or Grep tool to verify the symbol is truly unused across the entire workspace (not just the declaring file)
       - Create a prioritized removal list in vulture-report.txt with annotations

    5. Analyze qlty smells output:
       - Identify duplicated code blocks that could be consolidated
       - Identify overly complex functions that may indicate dead branches
       - Save actionable findings to qlty-report.txt

    CRITICAL VALIDATION FOR EACH SYMBOL:
    - Before marking anything for removal, search ALL 34 projects: `sg --pattern 'SYMBOL_NAME' --lang python` across the entire workspace
    - Check __init__.py files for re-exports of the symbol
    - Check if symbol is part of a Protocol or ABC that subclasses implement
    - Check if symbol is used in tests/ directories
    - If symbol appears ANYWHERE outside its definition, it is NOT dead code — skip it

    FALSE POSITIVE PATTERNS TO SKIP:
    - Pydantic model fields (used by serialization, not direct code references)
    - Protocol/ABC method definitions (implemented by subclasses)
    - `__init__.py` lazy-loaded exports (_LAZY_IMPORTS entries)
    - @override methods (required by parent contract)
    - Fixture functions (used by pytest discovery)
    - Handler methods decorated with @handler (used by dispatcher)
    - MRO-composed methods (inherited via facade classes)
    - Constants in `c.*` namespace (may be used externally)
  </action>
  <verify>
    <automated>test -f .planning/quick/260323-r3o-apply-yagni-patterns-using-vulture-and-q/vulture-report.txt && test -f .planning/quick/260323-r3o-apply-yagni-patterns-using-vulture-and-q/qlty-report.txt && test -f .planning/quick/260323-r3o-apply-yagni-patterns-using-vulture-and-q/baseline.txt && echo "PASS"</automated>
  </verify>
  <done>Vulture and qlty reports generated. Each candidate removal verified with cross-workspace search. False positives filtered out. Prioritized removal list ready for execution.</done>
</task>

<task type="auto">
  <name>Task 2: Remove confirmed dead code and fix all impacts</name>
  <files>Determined by Task 1 vulture-report.txt analysis</files>
  <action>
    For each confirmed dead code item from the prioritized removal list in Task 1:

    1. REMOVE dead code using the Edit tool (never sed/awk):
       - Unused functions: delete entire function definition
       - Unused classes: delete entire class definition
       - Unused imports: remove the import line
       - Unused variables/attributes: remove the assignment
       - Unused methods in namespace classes: remove the method

    2. After EACH removal, immediately check cascading impacts:
       - If removed symbol was in __init__.py `__all__` or `_LAZY_IMPORTS`: run `make gen` to regenerate exports
       - If removed symbol was imported elsewhere: remove those imports too
       - If removed symbol was in a facade class MRO chain: verify remaining MRO is valid
       - If removal leaves an empty module: consider removing the module file (mv to .bak per CLAUDE.md rules)

    3. For qlty smell findings (duplication/complexity):
       - Only act on clear dead branches within functions (unreachable code after return/raise)
       - Only consolidate duplicated code if both copies are in the same project
       - Do NOT refactor across project boundaries (out of scope)

    4. After ALL removals complete, run validation in this order:
       a. `make gen` — regenerate all __init__.py lazy exports
       b. `make check CHECK_GATES=lint` — zero ruff errors
       c. `make check CHECK_GATES=pyrefly` — zero pyrefly errors
       d. `make test FAIL_FAST=1` — all tests pass

    5. If any validation fails:
       - Read the error output carefully
       - Fix the specific error (missing import, broken reference, etc.)
       - Re-run the failing validation
       - Repeat until clean

    RULES:
    - Per AGENTS.md 3.1: "deleting dead/unused code, removing unnecessary helpers and pass-through wrappers" is VALID and ENCOURAGED
    - Per AGENTS.md 3.5: "Legacy Extermination — compatibility wrappers, deprecation shims are ABOMINABLE. Delete and replace immediately."
    - Do NOT remove code that vulture flagged but cross-workspace search showed is used
    - Do NOT rename or refactor anything — only DELETE confirmed dead code
    - Do NOT touch auto-generated __init__.py files manually — use `make gen`
    - Use `/home/marlonsc/flext/.venv/bin/` for all Python tooling
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && make check CHECK_GATES=lint FAIL_FAST=1 2>&1 | tail -3 && make check CHECK_GATES=pyrefly FAIL_FAST=1 2>&1 | tail -3</automated>
  </verify>
  <done>All confirmed dead code removed. `make check CHECK_GATES=lint` passes with zero errors. `make check CHECK_GATES=pyrefly` passes with zero errors. `make test` passes with no regressions. No new lint/type errors introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Final validation and summary</name>
  <files>
    .planning/quick/260323-r3o-apply-yagni-patterns-using-vulture-and-q/260323-r3o-SUMMARY.md
  </files>
  <action>
    1. Run full validation suite:
       - `make check FAIL_FAST=1` — all lint gates pass
       - `make test FAIL_FAST=1` — all tests pass

    2. Re-run vulture to confirm reduction:
       ```
       /home/marlonsc/flext/.venv/bin/vulture */src/ --min-confidence 80 \
         --exclude "*_pb2*,*__pycache__*,*.venv*,*node_modules*,*migrations*" \
         --ignore-decorators "@app.route,@router.*,@handler,@field_validator,@model_validator,@computed_field,@override,@abstractmethod,@property,@classmethod,@staticmethod,@pytest.fixture,@pytest.mark.*" \
         --ignore-names "model_config,model_fields,__all__,__getattr__,_LAZY_IMPORTS,visit_*,test_*,setup_*,teardown_*,conftest,pytest_*"
       ```

    3. Compare before/after vulture counts to quantify improvement

    4. Write SUMMARY.md with:
       - Number of dead code items removed (by category: functions, classes, imports, variables)
       - Projects affected
       - Lines of code removed
       - Before/after vulture finding counts
       - Any items intentionally skipped (with reason)
       - Validation evidence (make check + make test output)

    5. Commit all changes:
       ```
       git add -A && git commit -m "chore: apply YAGNI patterns — remove dead code identified by vulture/qlty

       Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
       ```
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && make check FAIL_FAST=1 2>&1 | tail -5 && make test FAIL_FAST=1 2>&1 | tail -5</automated>
  </verify>
  <done>All quality gates pass. Summary documents the removed dead code with before/after metrics. Changes committed.</done>
</task>

</tasks>

<verification>
1. `make check FAIL_FAST=1` — zero lint/type errors across all 34 projects
2. `make test FAIL_FAST=1` — all tests pass with no regressions
3. Re-run vulture shows reduced finding count vs baseline
</verification>

<success_criteria>
- Dead code identified by vulture (confidence >= 80%) removed after cross-workspace verification
- Code smells from qlty addressed where actionable (dead branches, unreachable code)
- Zero ruff lint errors (same or better than baseline)
- Zero pyrefly type errors (same or better than baseline)
- All pytest tests pass
- All __init__.py exports remain valid
- Summary with before/after metrics committed
</success_criteria>

<output>
After completion, create `.planning/quick/260323-r3o-apply-yagni-patterns-using-vulture-and-q/260323-r3o-SUMMARY.md`
</output>
