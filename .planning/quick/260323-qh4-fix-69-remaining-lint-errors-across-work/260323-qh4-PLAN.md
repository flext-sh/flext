---
phase: quick
plan: 260323-qh4
type: execute
wave: 1
depends_on: []
files_modified: [] # TBD — executor discovers exact files via ruff output
autonomous: true
requirements: [LINT-ZERO]
must_haves:
  truths:
    - "make check CHECK_GATES=lint passes with zero errors across all 34 projects"
    - "No new per-file-ignores added to pyproject.toml (errors fixed at source)"
    - "Security noqa suppressions have justification comments"
  artifacts: []
  key_links: []
---

<objective>
Fix 69 remaining ruff lint errors across the workspace.

Purpose: Drive the workspace to zero lint errors — aligning with the project's zero-tolerance quality policy (AGENTS.md Section 3.5).
Output: All ruff-reportable errors resolved via code fixes, docstrings, or justified noqa suppressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@pyproject.toml (ruff config — see [tool.ruff.lint] and [tool.ruff.lint.per-file-ignores])
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix code-level errors (F821, PLC2701, PLC0415, PLC1901, PLC2801, PLW1510)</name>
  <files>TBD — discovered via ruff output</files>
  <action>
Run `make check CHECK_GATES=lint` to get the current baseline. If all projects pass, run ruff from workspace root via `make check CHECK_GATES=lint RUFF_ARGS="--select F821,PLC2701,PLC0415,PLC1901,PLC2801,PLW1510 --no-fix"` to surface the specific errors. If that still shows zero (because per-file-ignores suppress them), check pyproject.toml per-file-ignores for these codes and assess whether suppressions are still needed.

For each category:

**F821 (undefined-name) — 10 errors:**
- Investigate each: usually a missing import, typo, or forward reference issue.
- Fix by adding the correct import or fixing the reference.
- These are in `*models*` files (per-file-ignore exists). If the per-file-ignore in pyproject.toml masks real bugs, consider narrowing the ignore scope.

**PLC2701 (import-private-name) — 5 errors:**
- These import `_internal` modules from outside their package.
- Per AGENTS.md Section 2.4: "Public contracts MUST be consumed from package facades and root exports only."
- Fix by switching to the public facade import (e.g., `from flext_core import FlextXyz` instead of `from flext_core._internal import Xyz`).
- If in `*models*` or `*result*` files (per-file-ignore exists), assess if the ignore is still warranted.

**PLC0415 (import-outside-top-level) — 3 errors:**
- Move imports to top of file where possible.
- If conditional/deferred import is genuinely needed (e.g., circular import avoidance via TYPE_CHECKING), add `# noqa: PLC0415` with a comment explaining why.

**PLC1901 (compare-to-empty-string) — 1 error:**
- Replace `x == ""` with `not x` (or `x != ""` with `x`).

**PLC2801 (unnecessary-dunder-call) — 1 error:**
- Replace direct `obj.__method__()` call with the equivalent operator or builtin (e.g., `obj.__len__()` -> `len(obj)`).

**PLW1510 (subprocess-run-without-check) — 1 error:**
- Add `check=True` to the `subprocess.run()` call, or capture and handle the return code explicitly.
  </action>
  <verify>
    <automated>make check CHECK_GATES=lint</automated>
  </verify>
  <done>All F821, PLC2701, PLC0415, PLC1901, PLC2801, PLW1510 errors resolved. No new ruff errors introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Add docstrings and suppress intentional violations (D101, D102, D106, SLF001, S-prefixed)</name>
  <files>TBD — discovered via ruff output</files>
  <action>
**D101 (undocumented-public-class) — 18 errors:**
- Add one-line docstrings to each public class missing one.
- Follow the project convention: explain the class's responsibility and key methods.
- For facade/MRO composition classes (e.g., `class FlextXyzModels(FlextBaseModels): pass`), use: `"""Facade composing {domain} models via MRO."""`
- For Pydantic models, describe what the model represents.

**D102 (undocumented-public-method) — 1 error:**
- Add a docstring describing the method's purpose.

**D106 (undocumented-public-nested-class) — 1 error:**
- Usually a Pydantic `Config` or `model_config` inner class. Add: `"""Pydantic model configuration."""` or similar.

**SLF001 (private-member-access) — 15 errors:**
- Review each case. If accessing a private member from WITHIN the same package (e.g., infra tools accessing internals they own), add `# noqa: SLF001 — internal access within package boundary` on the specific line.
- If accessing from OUTSIDE the package, refactor to use the public API instead.
- Many of these are already suppressed via per-file-ignores for `*exceptions*`, `*instrumentation*`, `*result*`, `*utilities*` files. Only fix errors NOT already covered by per-file-ignores.

**S404 (suspicious-subprocess-import) — 4 errors:**
- Add `# noqa: S404 — subprocess required for process management` on the import line.
- Only for files NOT already covered by per-file-ignores (`*scripts*`, `*docs*`, `*examples*`, `*utilities*` are already exempt).

**S105 (hardcoded-password-string) — 3 errors:**
- These are typically variable names containing "password" in test/config code, not actual secrets.
- Add `# noqa: S105 — variable name pattern, not a hardcoded credential` on each line.
- Only for files NOT already covered by per-file-ignores.

**S603 (subprocess-without-shell-equals-true) — 3 errors:**
- Add `# noqa: S603 — shell=False is the safe default, subprocess args are controlled` on each line.

**S310 (suspicious-url-open-usage) — 2 errors:**
- Add `# noqa: S310 — URL scheme validated/controlled by caller` on each line.

**S607 (start-process-with-partial-path) — 1 error:**
- Add `# noqa: S607 — executable resolved via PATH, acceptable for CLI tooling` on the line.

IMPORTANT: For every `# noqa` suppression, the justification comment MUST be on the same line explaining WHY the suppression is acceptable. Never add bare `# noqa` without explanation.
  </action>
  <verify>
    <automated>make check CHECK_GATES=lint</automated>
  </verify>
  <done>All D101/D102/D106 errors have docstrings added. All SLF001/S-prefixed errors either fixed at source or suppressed with justification. Zero ruff errors remain.</done>
</task>

</tasks>

<verification>
1. `make check CHECK_GATES=lint` passes (zero errors, all 33 Python projects green)
2. `make scan` — security scan results unchanged or improved (no regressions)
3. No `# type: ignore` added, no `Any` introduced, no per-file-ignores widened in pyproject.toml
</verification>

<success_criteria>
- Zero ruff lint errors across all workspace projects
- All docstring additions follow AGENTS.md conventions (explain WHY, not WHAT)
- All noqa suppressions have inline justification comments
- No regressions in existing passing gates
</success_criteria>

<output>
After completion, create `.planning/quick/260323-qh4-fix-69-remaining-lint-errors-across-work/260323-qh4-SUMMARY.md`
</output>
