---
phase: quick
plan: 260323-o3c
type: execute
wave: 1
depends_on: []
files_modified: [Makefile]
autonomous: true

must_haves:
  truths:
    - "WORKSPACE_PYTHONPATH variable auto-detects all project src/ directories"
    - "No hardcoded flext-infra/src:flext-core/src paths remain in Makefile"
    - "make gen, make sync, and make imp targets expand PYTHONPATH correctly"
    - "Changes are committed and pushed to remote"
  artifacts:
    - path: "Makefile"
      provides: "WORKSPACE_PYTHONPATH variable and updated gen/sync/imp targets"
      contains: "WORKSPACE_PYTHONPATH"
  key_links:
    - from: "WORKSPACE_PYTHONPATH"
      to: "ALL_PROJECTS"
      via: "shell loop over ALL_PROJECTS checking for src/ dirs"
      pattern: "for d in .\\$\\(ALL_PROJECTS\\)"
---

<objective>
Verify and commit the WORKSPACE_PYTHONPATH Makefile refactoring.

Purpose: A prior session replaced hardcoded `flext-infra/src:flext-core/src` PYTHONPATH
entries with a dynamic `WORKSPACE_PYTHONPATH` variable that auto-detects all project
`src/` directories. The changes are uncommitted. This plan verifies correctness via
dry-run expansion and runtime execution, then commits and pushes.

Output: Committed and pushed Makefile with dynamic WORKSPACE_PYTHONPATH.
</objective>

<execution_context>
@/home/marlonsc/.claude/get-shit-done/workflows/execute-plan.md
@/home/marlonsc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify WORKSPACE_PYTHONPATH expansion and run gen target</name>
  <files>Makefile</files>
  <action>
  1. Run `make -n gen` (dry-run) and confirm WORKSPACE_PYTHONPATH expands to include
     all project src/ directories (flext-core/src, flext-infra/src, flext-api/src, etc.)
     and ends with $(CURDIR)/src. Confirm no hardcoded `flext-infra/src:flext-core/src`
     pattern remains anywhere in Makefile.

  2. Run `make -n sync` and `make -n imp` dry-runs — confirm same correct expansion.

  3. Run `make gen` (actual execution) to confirm the codegen lazy-init target works
     with the new PYTHONPATH. This is the fastest of the three targets and validates
     that flext_infra can be imported via the dynamic path.

  If `make gen` fails, inspect the error. The most likely issue would be a trailing
  colon in WORKSPACE_PYTHONPATH for projects without src/ directories, but the shell
  loop already guards with `[ -d ... ]`.
  </action>
  <verify>
  - `make -n gen 2>&1` output contains `/flext-core/src:` and `/flext-api/src:` (not just flext-infra+flext-core)
  - `make gen` exits 0
  - No `flext-infra/src:$(CURDIR)/flext-core/src` literal remains in Makefile (use Grep)
  </verify>
  <done>All three targets expand WORKSPACE_PYTHONPATH correctly and `make gen` succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Commit Makefile changes and push</name>
  <files>Makefile</files>
  <action>
  Stage ONLY the Makefile (not flext-core submodule which has unrelated changes).
  Commit with a message describing the refactoring: replacing hardcoded PYTHONPATH
  entries with auto-detected WORKSPACE_PYTHONPATH variable.

  Then push to remote (branch is already 2 commits ahead of origin/0.12.0-dev,
  this will add a third).

  Use `bd sync` before push per project convention.
  </action>
  <verify>
  - `git status` shows Makefile no longer modified
  - `git log --oneline -1` shows the new commit
  - `git status` shows branch is ahead of origin (or up to date after push)
  </verify>
  <done>Makefile changes committed with descriptive message and pushed to origin/0.12.0-dev</done>
</task>

</tasks>

<verification>
- `make -n gen` shows dynamic PYTHONPATH with all project src/ dirs
- `make gen` succeeds (exit 0)
- No hardcoded `flext-infra/src:.*flext-core/src` patterns in Makefile
- Commit exists in git log
- Changes pushed to remote
</verification>

<success_criteria>
- WORKSPACE_PYTHONPATH variable correctly auto-detects all project src/ directories
- Zero hardcoded PYTHONPATH entries remain in Makefile
- `make gen` runs successfully with the new variable
- Changes committed and pushed to origin/0.12.0-dev
</success_criteria>

<output>
After completion, create `.planning/quick/260323-o3c-continue-workspace-pythonpath-changes-ve/260323-o3c-SUMMARY.md`
</output>
