---
phase: 05-package-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flext-infra/src/flext_infra/deps/modernizer.py
  - flext-core/pyproject.toml
  - flext-infra/pyproject.toml
  - flext-tests/pyproject.toml
autonomous: true
requirements: [MIG-01, MIG-02, MIG-03]
must_haves:
  truths:
    - "flext-infra is a git submodule with its own repo"
    - "flext-tests is a git submodule with its own repo"
    - "flext-core/src/ contains only flext_core/"
    - "Foundation projects use hatchling build backend"
  artifacts:
    - path: "flext-core/pyproject.toml"
      provides: "hatchling build settings for flext-core"
      contains: "hatchling.build"
    - path: "flext-infra/pyproject.toml"
      provides: "hatchling build settings for flext-infra"
      contains: "hatchling.build"
    - path: "flext-tests/pyproject.toml"
      provides: "hatchling build settings for flext-tests"
      contains: "hatchling.build"
  key_links:
    - from: "flext-core/pyproject.toml"
      to: "hatchling"
      via: "build-system.build-backend"
      pattern: 'build-backend = "hatchling.build"'
---

<objective>
Validate submodule extraction (MIG-01/02/03 already done) and convert the 3 foundation projects (flext-infra, flext-core, flext-tests) from Poetry to hatchling build backend.

Purpose: Bootstrap the migration by converting foundation projects first per D-04 dependency order. Update modernizer to emit hatchling so it can be used for bulk conversion in Plan 02.
Output: 3 foundation pyproject.toml files using hatchling, modernizer updated to emit hatchling settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-package-migration/05-CONTEXT.md
@.planning/phases/05-package-migration/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate MIG-01/02/03 and convert flext-infra pyproject.toml to hatchling</name>
  <files>flext-infra/pyproject.toml</files>
  <read_first>
    - flext-infra/pyproject.toml
    - .gitmodules
    - flext-core/src/ (ls to confirm only flext_core exists)
  </read_first>
  <action>
1. Validate MIG-01: Run `git submodule status flext-infra` — confirm it shows a commit hash (not missing).
2. Validate MIG-02: Run `git submodule status flext-tests` — confirm it shows a commit hash.
3. Validate MIG-03: Run `ls flext-core/src/` — confirm only `flext_core/` directory exists (no flext_infra, flext_tests).
4. Convert flext-infra/pyproject.toml (per D-05):
   - Replace `[build-system]` section:
     ```toml
     [build-system]
     build-backend = "hatchling.build"
     requires = ["hatchling"]
     ```
   - Add hatch build target (find the package name from existing `[tool.poetry].packages`):
     ```toml
     [tool.hatch.build.targets.wheel]
     packages = ["src/flext_infra"]
     ```
   - Remove entire `[tool.poetry]` section (including `packages = [...]`).
   - Consolidate dev deps per D-06: if `[project.optional-dependencies].dev` exists AND `[dependency-groups].dev` exists, merge all entries into `[dependency-groups].dev` only and remove `[project.optional-dependencies].dev`.
   - Keep all `[tool.ruff]`, `[tool.pytest]`, `[tool.pyright]`, etc. sections unchanged.
  </action>
  <verify>
    <automated>grep -c "hatchling.build" flext-infra/pyproject.toml | grep -q "1" && ! grep -q "poetry.core.masonry" flext-infra/pyproject.toml && ! grep -q "\[tool.poetry\]" flext-infra/pyproject.toml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - flext-infra/pyproject.toml contains `build-backend = "hatchling.build"`
    - flext-infra/pyproject.toml contains `requires = ["hatchling"]`
    - flext-infra/pyproject.toml contains `packages = ["src/flext_infra"]`
    - flext-infra/pyproject.toml does NOT contain `poetry.core.masonry`
    - flext-infra/pyproject.toml does NOT contain `[tool.poetry]`
    - `git submodule status flext-infra` exits 0 with a commit hash
    - `git submodule status flext-tests` exits 0 with a commit hash
    - `ls flext-core/src/` shows only `flext_core`
  </acceptance_criteria>
  <done>flext-infra uses hatchling, MIG-01/02/03 validated as complete</done>
</task>

<task type="auto">
  <name>Task 2: Convert flext-core and flext-tests pyproject.toml to hatchling + update modernizer</name>
  <files>flext-core/pyproject.toml, flext-tests/pyproject.toml, flext-infra/src/flext_infra/deps/modernizer.py</files>
  <read_first>
    - flext-core/pyproject.toml
    - flext-tests/pyproject.toml
    - flext-infra/src/flext_infra/deps/modernizer.py
  </read_first>
  <action>
1. Convert flext-core/pyproject.toml (per D-05):
   - Replace `[build-system]`:
     ```toml
     [build-system]
     build-backend = "hatchling.build"
     requires = ["hatchling"]
     ```
   - Add:
     ```toml
     [tool.hatch.build.targets.wheel]
     packages = ["src/flext_core"]
     ```
   - Remove `[tool.poetry]` section (currently `packages = [{include = "flext_core", from = "src"}]`).
   - Consolidate dev deps per D-06: merge `[project.optional-dependencies].dev` into `[dependency-groups].dev`, remove the optional-dependencies.dev section. Note: `flext-infra @ file:../flext-infra` and `flext-tests @ file:../flext-tests` in dev deps should stay as-is for now (will become workspace refs in Plan 02).

2. Convert flext-tests/pyproject.toml — same pattern:
   - Replace build-system to hatchling
   - Add `[tool.hatch.build.targets.wheel]` with correct package path
   - Remove `[tool.poetry]`
   - Consolidate dev deps per D-06

3. Update modernizer.py:
   - Find where it emits `build-backend = "poetry.core.masonry.api"` and change to `build-backend = "hatchling.build"`
   - Find where it emits `requires = ["poetry-core>=2"]` and change to `requires = ["hatchling"]`
   - Find where it emits `[tool.poetry]` package declarations and replace with `[tool.hatch.build.targets.wheel]` using `packages = ["src/{package_name}"]`
   - If it manages `[project.optional-dependencies].dev`, update to emit `[dependency-groups].dev` instead (per D-06)
  </action>
  <verify>
    <automated>grep -c "hatchling.build" flext-core/pyproject.toml flext-tests/pyproject.toml | grep -v ":0" | wc -l | grep -q "2" && ! grep -q "poetry.core.masonry" flext-core/pyproject.toml && ! grep -q "poetry.core.masonry" flext-tests/pyproject.toml && grep -q "hatchling" flext-infra/src/flext_infra/deps/modernizer.py && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - flext-core/pyproject.toml contains `build-backend = "hatchling.build"`
    - flext-core/pyproject.toml does NOT contain `poetry.core.masonry`
    - flext-core/pyproject.toml does NOT contain `[tool.poetry]`
    - flext-tests/pyproject.toml contains `build-backend = "hatchling.build"`
    - flext-tests/pyproject.toml does NOT contain `poetry.core.masonry`
    - modernizer.py contains string `hatchling` (emits hatchling build backend)
    - modernizer.py does NOT contain string `poetry.core.masonry` (no longer emits poetry)
  </acceptance_criteria>
  <done>All 3 foundation projects use hatchling, modernizer emits hatchling for bulk conversion</done>
</task>

</tasks>

<verification>
- `git submodule status flext-infra flext-tests` shows valid commits
- `ls flext-core/src/` shows only flext_core
- All 3 foundation pyproject.toml files use hatchling build backend
- No `poetry.core.masonry` or `[tool.poetry]` in any foundation pyproject.toml
- Modernizer updated to emit hatchling
</verification>

<success_criteria>
MIG-01/02/03 validated. Foundation projects (flext-core, flext-infra, flext-tests) converted to hatchling. Modernizer ready for bulk conversion.
</success_criteria>

<output>
After completion, create `.planning/phases/05-package-migration/05-01-SUMMARY.md`
</output>
