---
phase: 05-package-migration
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - base.mk
  - Makefile
  - .envrc
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [MIG-06]
must_haves:
  truths:
    - "All make targets use uv/direct invocation instead of poetry run"
    - "CI installs uv instead of Poetry"
    - ".envrc has no Poetry env vars"
    - "make check and make test work without Poetry installed"
  artifacts:
    - path: "base.mk"
      provides: "Shared Makefile with uv-based commands"
    - path: "Makefile"
      provides: "Root Makefile with uv-based setup"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline using setup-uv"
    - path: ".github/workflows/release.yml"
      provides: "Release pipeline using setup-uv"
  key_links:
    - from: "base.mk"
      to: "uv / direct venv commands"
      via: "removed $(POETRY) variable"
      pattern: "uv sync"
    - from: ".github/workflows/ci.yml"
      to: "astral-sh/setup-uv"
      via: "uses: astral-sh/setup-uv"
      pattern: "setup-uv"
---

<objective>
Replace all Poetry references in Makefiles, CI workflows, and .envrc with uv equivalents. Hard-cut per D-10.

Purpose: Complete MIG-06 — the entire build/test/CI pipeline runs on uv without Poetry installed.
Output: Zero Poetry references in build tooling. CI uses setup-uv. Make targets use direct venv commands or uv run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-package-migration/05-CONTEXT.md
@.planning/phases/05-package-migration/05-RESEARCH.md
@.planning/phases/05-package-migration/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Poetry in base.mk and root Makefile</name>
  <files>base.mk, Makefile</files>
  <read_first>
    - base.mk (full file — ~750 lines, need to see all POETRY refs)
    - Makefile (full file — need to see POETRY_BIN, POETRY_ENV usage)
  </read_first>
  <action>
**base.mk changes (per D-10):**

1. Remove the `POETRY := poetry` variable (line 93).

2. Remove all `POETRY_VIRTUALENVS_*` exports (lines 68-82):
   - Delete `export POETRY_VIRTUALENVS_PATH`, `POETRY_VIRTUALENVS_IN_PROJECT`, `POETRY_VIRTUALENVS_CREATE` in all 3 conditional branches.

3. Replace all `$(POETRY) run X` with direct `X` (tools are in venv PATH per .envrc):
   - `$(POETRY) run ruff check` → `ruff check`
   - `$(POETRY) run ruff format` → `ruff format`
   - `$(POETRY) run pyright` → `pyright`
   - `$(POETRY) run pyrefly check` → `pyrefly check`
   - `$(POETRY) run pytest` → `pytest`
   - `$(POETRY) run radon` → `radon`
   - `$(POETRY) run interrogate` → `interrogate`
   - `$(POETRY) run python -m flext_infra` → `python -m flext_infra`
   - `$(POETRY) run pre-commit install` → `pre-commit install` (per D-12)

4. Replace Poetry-specific commands:
   - `$(POETRY) lock` → `uv lock`
   - `$(POETRY) install --all-extras --all-groups` → `uv sync --all-groups`
   - `$(POETRY) build` → `uv build` (or `hatch build`)

**Root Makefile changes (per D-10):**

1. Remove `POETRY_BIN := $(WORKSPACE_VENV)/bin/poetry` (line 15).
2. Remove `POETRY_ENV := VIRTUAL_ENV=... POETRY_VIRTUALENVS_CREATE=false ...` (line 18).
3. Replace `ORCHESTRATOR := $(POETRY_ENV) $(PY) -m flext_infra workspace orchestrate` with `ORCHESTRATOR := $(PY) -m flext_infra workspace orchestrate` (line 19 — venv already active via PATH).
4. Replace all `$(POETRY_ENV) $(POETRY_BIN) -C "$$proj" lock` → `uv lock --directory "$$proj"` (or remove if workspace lock covers all).
5. Replace all `$(POETRY_ENV) $(POETRY_BIN) -C "$$proj" install --all-extras --all-groups` → `uv sync --directory "$$proj" --all-groups`.
6. Replace all `$(POETRY_ENV) $(POETRY_BIN) lock` → `uv lock`.
7. Replace all `$(POETRY_ENV) $(POETRY_BIN) install --all-extras --all-groups` → `uv sync --all-groups`.
8. Replace all `$(POETRY_ENV) $(PY) -m ...` → `$(PY) -m ...` (venv PATH handles it).
9. Replace all `$(POETRY_ENV) ruff format` → `ruff format`.
10. In the setup target (line ~315), replace `pip install -U pip poetry pipx uv` with `pip install -U pip pipx uv` (remove poetry).
11. Replace `$(POETRY_ENV) $(POETRY_BIN) -C flext-core install --only main` → `uv sync --directory flext-core --no-dev`.
12. Replace `$(POETRY_ENV) $(POETRY_BIN) -C "$$proj" update` → `uv lock --directory "$$proj" --upgrade` (or remove per-project locking if workspace lock suffices).

Note: Since this is a uv workspace now, per-project `uv lock` / `uv sync` may not be needed — the root `uv lock` + `uv sync` covers all members. Simplify setup/lock targets to just run at root level where possible.
  </action>
  <verify>
    <automated>! grep -E "POETRY|poetry run|poetry lock|poetry install|poetry build" base.mk Makefile 2>/dev/null | grep -v "^#" | grep -v ".bak" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - base.mk does NOT contain `POETRY :=` variable
    - base.mk does NOT contain `$(POETRY) run`
    - base.mk does NOT contain `POETRY_VIRTUALENVS`
    - Makefile does NOT contain `POETRY_BIN`
    - Makefile does NOT contain `POETRY_ENV`
    - Makefile does NOT contain `poetry` (except comments)
    - All tool invocations use direct commands or `uv run`
  </acceptance_criteria>
  <done>Zero Poetry references in base.mk and root Makefile, all using uv/direct venv commands</done>
</task>

<task type="auto">
  <name>Task 2: Update CI workflows and .envrc</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, .envrc</files>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
    - .envrc
  </read_first>
  <action>
**CI workflow (ci.yml) per D-11:**

1. Replace the Poetry install step:
   ```yaml
   # Remove:
   - name: Install Poetry
     uses: snok/install-poetry@76e04a911780d5b312d89783f7b1cd627778900a

   # Add:
   - name: Install uv
     uses: astral-sh/setup-uv@v5
     with:
       enable-cache: true
   ```

2. Add `submodules: recursive` to the checkout step (pitfall 6 from research):
   ```yaml
   - uses: actions/checkout@v4
     with:
       submodules: recursive
   ```

3. Replace any `poetry install` / `poetry run` commands with `uv sync` / direct commands.

**Release workflow (release.yml) per D-11:**
1. Same pattern: replace `snok/install-poetry@v1` with `astral-sh/setup-uv@v5`.
2. Add `submodules: recursive` to checkout.
3. Replace `poetry build` / `poetry publish` with `uv build` / `uv publish` (or `hatch build`).

**.envrc per D-13:**
1. Remove these lines:
   ```bash
   export POETRY_VIRTUALENVS_CREATE=false
   export POETRY_VIRTUALENVS_IN_PROJECT=false
   export POETRY_VIRTUALENVS_PATH="${PWD}"
   ```
2. Remove the comment `# --- Poetry / uv: workspace-mode flags ---` and replace with `# --- uv workspace flags ---`.
3. Keep `export PYTHON_KEYRING_BACKEND="keyring.backends.null.Keyring"` (still useful).
4. Keep `export UV_PROJECT_ENVIRONMENT="${VENV_DIR}"` (already correct).
  </action>
  <verify>
    <automated>! grep -q "snok/install-poetry" .github/workflows/ci.yml && ! grep -q "snok/install-poetry" .github/workflows/release.yml && grep -q "setup-uv" .github/workflows/ci.yml && ! grep -q "POETRY_VIRTUALENVS" .envrc && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml contains `astral-sh/setup-uv` (not `snok/install-poetry`)
    - ci.yml contains `submodules: recursive` in checkout step
    - release.yml contains `astral-sh/setup-uv` (not `snok/install-poetry`)
    - .envrc does NOT contain `POETRY_VIRTUALENVS_CREATE`
    - .envrc does NOT contain `POETRY_VIRTUALENVS_IN_PROJECT`
    - .envrc does NOT contain `POETRY_VIRTUALENVS_PATH`
    - .envrc still contains `UV_PROJECT_ENVIRONMENT`
  </acceptance_criteria>
  <done>CI uses setup-uv, .envrc cleaned of Poetry vars, full pipeline runs without Poetry</done>
</task>

</tasks>

<verification>
- `grep -r "POETRY\|poetry run\|poetry install\|poetry lock\|poetry build\|snok/install-poetry" base.mk Makefile .envrc .github/workflows/*.yml` returns empty (excluding comments)
- `grep "setup-uv" .github/workflows/ci.yml .github/workflows/release.yml` shows matches
- `.envrc` has no POETRY_VIRTUALENVS exports
</verification>

<success_criteria>
Zero Poetry references in build tooling, CI, and environment settings. All make targets work via uv/direct venv invocation. CI uses astral-sh/setup-uv.
</success_criteria>

<output>
After completion, create `.planning/phases/05-package-migration/05-03-SUMMARY.md`
</output>
