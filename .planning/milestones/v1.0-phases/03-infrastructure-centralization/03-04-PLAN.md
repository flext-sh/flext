---
phase: 03-infrastructure-centralization
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - flext-cli/src/flext_cli/services/output.py
  - flext-infra/src/flext_infra/docs/generator.py
  - flext-infra/src/flext_infra/validate/checker.py
  - flext-observability/src/flext_observability/cli.py
  - flext-quality/src/flext_quality/tools/style_validator.py
  - flext-quality/src/flext_quality/tools/content_analyzer.py
autonomous: true
requirements:
  - WA-05

must_haves:
  truths:
    - "Zero print() calls in production code except documented CLI output services"
    - "Exempted files have inline # CLI output comments documenting the exemption"
  artifacts:
    - path: "flext-cli/src/flext_cli/services/output.py"
      provides: "Documented CLI output service (exempted from WA-05)"
      contains: "# CLI output"
  key_links: []
---

<objective>
Triage and fix 52 print() instances across 20 production files — replace with FlextLogger or output.* where appropriate, document exemptions for intentional CLI output.

Purpose: WA-05 requires zero print() in production except documented CLI output services per D-15.
Output: Clean print() sweep with documented exemptions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-infrastructure-centralization/03-CONTEXT.md
@.planning/phases/03-infrastructure-centralization/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage print() instances and categorize</name>
  <files>N/A (analysis only)</files>
  <read_first>
    - .planning/phases/03-infrastructure-centralization/03-RESEARCH.md (pitfall 1: print() false positives)
  </read_first>
  <action>
Perform a full triage of all print() instances in production code:

1. Grep for `print(` across all `*/src/**/*.py` files
2. For each instance, categorize as:
   - **REPLACE**: Logging/debug output -> replace with FlextLogger (`output.info()`, `output.debug()`, etc.)
   - **EXEMPT**: Intentional CLI output (e.g., flext-cli output service, terminal-facing tools) -> add `# CLI output` comment
   - **REMOVE**: Dead/leftover debug prints -> delete
3. Create a triage list with file, line, category, and action
4. Document which files/modules are exempt CLI output services

Expected exempt modules (from research):

- `flext-cli/src/flext_cli/services/output.py` — intentional CLI output service
- Other CLI-facing tools that produce user-visible terminal output

The triage output is used by Task 2 to apply fixes.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && echo "Total print() in src/:"; grep -rn 'print(' --include='*.py'*/src/ 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Triage list exists categorizing all print() instances as REPLACE, EXEMPT, or REMOVE
    - Each categorization has a rationale (logging vs CLI output vs dead code)
  </acceptance_criteria>
  <done>All 52 print() instances triaged with clear action plan</done>
</task>

<task type="auto">
  <name>Task 2: Apply print() fixes based on triage</name>
  <files>
    flext-cli/src/flext_cli/services/output.py
    flext-infra/src/flext_infra/docs/generator.py
    flext-infra/src/flext_infra/validate/checker.py
    flext-observability/src/flext_observability/cli.py
    flext-quality/src/flext_quality/tools/style_validator.py
    flext-quality/src/flext_quality/tools/content_analyzer.py
  </files>
  <read_first>
    - Each file being modified (see current print() usage and context)
    - flext-core/src/flext_core/_utilities/logging.py (FlextLogger API for replacements)
  </read_first>
  <action>
Apply the triage results from Task 1:

**For REPLACE instances:**

- Replace `print(msg)` with appropriate logger call:
  - Status/progress messages -> `output.info(msg)` or `logger.info(msg)`
  - Error messages -> `output.error(msg)` or `logger.error(msg)`
  - Debug output -> `logger.debug(msg)`
- Add `from flext_core import FlextServices as s` and get logger if not present
- Or use existing `output` module if available in the project

**For EXEMPT instances:**

- Add `# CLI output` comment on same line: `print(result)  # CLI output`
- Document in a comment block at top of module: `# This module uses print() for intentional CLI output per WA-05 exemption`

**For REMOVE instances:**

- Delete the print() line

Run ruff check on each modified file. Verify no test breakage with `make test PROJECT=<affected>`.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && .venv/bin/python -c "
import subprocess as sp
r = sp.run(['grep', '-rn', 'print(', '--include=*.py'], capture_output=True, text=True, cwd='.')
violations = []
for line in r.stdout.splitlines():
    if '/src/' in line and '# CLI output' not in line and 'test' not in line.lower() and '**pycache**' not in line:
        violations.append(line)
print(f'Violations: {len(violations)}')
for v in violations[:10]:
    print(v)
if not violations:
    print('CLEAN')
"</automated>
  </verify>
  <acceptance_criteria>
    - Zero print() in production `*/src/` files without `# CLI output` exemption comment
    - All REPLACE instances use FlextLogger or output.* module
    - All EXEMPT instances have `# CLI output` inline comment
    - ruff check passes on all modified files
  </acceptance_criteria>
  <done>WA-05 fully resolved — zero unauthorized print() in production, all exemptions documented</done>
</task>

</tasks>

<verification>
- Grep for `print(` in `*/src/` shows only lines with `# CLI output` comment
- ruff check passes on all modified files
</verification>

<success_criteria>

- Zero print() in production code without documented exemption
- All exempted print() calls have `# CLI output` comment
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-centralization/03-04-SUMMARY.md`
</output>
