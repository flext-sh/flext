---
phase: 03-infrastructure-centralization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flext-infra/src/flext_infra/_utilities/cli.py
  - flext-infra/src/flext_infra/_utilities/discovery.py
  - flext-infra/src/flext_infra/docs/__main__.py
  - flext-infra/src/flext_infra/release/__main__.py
  - flext-infra/src/flext_infra/git/__main__.py
  - flext-infra/src/flext_infra/validate/__main__.py
  - flext-infra/src/flext_infra/refactor/__main__.py
  - flext-infra/src/flext_infra/__main__.py
  - flext-infra/src/flext_infra/deps/__main__.py
  - flext-infra/src/flext_infra/workspace/__main__.py
  - flext-infra/src/flext_infra/workspace/maintenance/__main__.py
  - flext-infra/src/flext_infra/check/__main__.py
  - flext-infra/src/flext_infra/github/__main__.py
  - flext-infra/src/flext_infra/codegen/__main__.py
  - flext-infra/src/flext_infra/basemk/__main__.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "u.Infra.run_cli(main_fn) centralizes structlog init, argparse, dispatch, and exception-to-exit-code"
    - "All 12 __main__.py files in flext_infra use u.Infra.run_cli() as sole bootstrap"
    - "u.Infra.iter_projects(cli) returns filtered, sorted project list"
    - "u.Infra.emit(data, cli=cli) outputs JSON or text based on cli.output_format"
    - "No root: Path parameter remains in flext_infra service interfaces that should be workspace_root"
    - "release/__main__.py:143 dry_run bug is fixed (was dry_run=cli.apply, now dry_run=not cli.apply)"
  artifacts:
    - path: "flext-infra/src/flext_infra/_utilities/cli.py"
      provides: "run_cli(), iter_projects(), and emit() static methods"
      contains: "def run_cli"
    - path: "flext-infra/src/flext_infra/_utilities/cli.py"
      provides: "iter_projects() and emit() static methods"
      contains: "def iter_projects"
  key_links:
    - from: "flext-infra/src/flext_infra/_utilities/cli.py"
      to: "flext-infra/src/flext_infra/_utilities/discovery.py"
      via: "iter_projects calls discover_projects"
      pattern: "discover_projects"
    - from: "flext-infra/src/flext_infra/**/__main__.py"
      to: "flext-infra/src/flext_infra/_utilities/cli.py"
      via: "all __main__.py bootstrap through run_cli"
      pattern: "run_cli"
---

<objective>
Create `run_cli()`, `iter_projects()`, and `emit()` utilities in the centralized CLI facade, migrate all 12 `__main__.py` files to use `run_cli()`, fix the D-07 dry_run bug, and normalize `root: Path` to `workspace_root: Path` across all flext_infra service interfaces.

Purpose: Eliminates 18 duplicate bootstrap patterns (INFRA-01), 13 `discover_projects()` call-site duplications (INFRA-02), inconsistent parameter naming (INFRA-03), and fixes a semantically inverted boolean bug (D-07).
Output: Three new utility methods + migrated **main**.py files + consistent `workspace_root` naming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-infrastructure-centralization/03-CONTEXT.md
@.planning/phases/03-infrastructure-centralization/03-RESEARCH.md

@flext-infra/src/flext_infra/_utilities/cli.py
@flext-infra/src/flext_infra/_utilities/discovery.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add run_cli(), iter_projects(), and emit() to FlextInfraUtilitiesCli</name>
  <files>flext-infra/src/flext_infra/_utilities/cli.py</files>
  <read_first>
    - flext-infra/src/flext_infra/_utilities/cli.py
    - flext-infra/src/flext_infra/_utilities/discovery.py
    - flext-infra/src/flext_infra/models.py
    - .planning/phases/03-infrastructure-centralization/03-RESEARCH.md
    - .planning/phases/03-infrastructure-centralization/03-CONTEXT.md (D-04, D-06 for run_cli design)
  </read_first>
  <action>
Add three new static methods to `FlextInfraUtilitiesCli`:

1. `run_cli(main_fn: Callable[[FlextInfraUtilitiesCli.CliArgs], int | None]) -> NoReturn` per D-04:
   - Initialize structlog (call existing structlog setup if available, or configure JSON processor)
   - Build argparse parser from CliArgs model fields (--workspace, --dry-run/--apply, --format, --check, --projects) per D-06
   - Parse args into a `CliArgs` Pydantic model instance
   - Call `main_fn(cli_args)` inside try/except
   - On success: `sys.exit(0)` or `sys.exit(return_code)` if main_fn returns int
   - On `SystemExit`: re-raise
   - On `Exception`: log via structlog, `sys.exit(1)` — centralizes exception-to-exit-code
   - This eliminates the 18 duplicate bootstrap patterns across 12 `__main__.py` files

2. `iter_projects(cli: FlextInfraUtilitiesCli.CliArgs) -> r[list[m.Infra.Workspace.ProjectInfo]]`:
   - Call `FlextInfraUtilitiesDiscovery.discover_projects(cli.workspace)` to get all projects
   - If result is failure, return it
   - Filter by `cli.project_names()` if set (use the `project_names()` method on CliArgs)
   - Return `r[...].ok(sorted(projects))` — sort by project name
   - Import discovery class at module level

3. `emit(data: BaseModel | t.ScalarMapping, *, text_fn: Callable[..., str] | None = None, cli: FlextInfraUtilitiesCli.CliArgs) -> None`:
   - If `cli.output_format == "json"`: serialize via `data.model_dump_json()` (BaseModel) or `orjson.dumps(data).decode()` (Mapping), write to stdout with newline
   - Elif `text_fn is not None`: call `output.write(text_fn(data))`
   - Else: `output.write(str(data))`
   - Import `orjson`, `sys` at module level as needed
   - Import `BaseModel` from `pydantic` for isinstance check

All methods follow the existing static method pattern in FlextInfraUtilitiesCli.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && .venv/bin/python -c "from flext_infra import FlextInfraUtilitiesCli; assert hasattr(FlextInfraUtilitiesCli, 'run_cli'); assert hasattr(FlextInfraUtilitiesCli, 'iter_projects'); assert hasattr(FlextInfraUtilitiesCli, 'emit')" && .venv/bin/ruff check flext-infra/src/flext_infra/_utilities/cli.py</automated>
  </verify>
  <done>run_cli(), iter_projects(), and emit() exist as static methods on FlextInfraUtilitiesCli, importable and lint-clean</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 12 __main__.py files to run_cli() and fix D-07 dry_run bug</name>
  <files>
    flext-infra/src/flext_infra/__main__.py
    flext-infra/src/flext_infra/docs/__main__.py
    flext-infra/src/flext_infra/release/__main__.py
    flext-infra/src/flext_infra/git/__main__.py
    flext-infra/src/flext_infra/validate/__main__.py
    flext-infra/src/flext_infra/refactor/__main__.py
    flext-infra/src/flext_infra/deps/__main__.py
    flext-infra/src/flext_infra/workspace/__main__.py
    flext-infra/src/flext_infra/workspace/maintenance/__main__.py
    flext-infra/src/flext_infra/check/__main__.py
    flext-infra/src/flext_infra/github/__main__.py
    flext-infra/src/flext_infra/codegen/__main__.py
    flext-infra/src/flext_infra/basemk/__main__.py
  </files>
  <read_first>
    - All 12 __main__.py files listed above
    - .planning/phases/03-infrastructure-centralization/03-CONTEXT.md (D-04, D-07)
  </read_first>
  <action>
Per D-04 (INFRA-01), migrate all 12 `__main__.py` files to use `u.Infra.run_cli(main_fn)`:

1. For each `__main__.py`:
   - Read the file to understand its current bootstrap pattern (argparse setup, structlog init, try/except, sys.exit)
   - Extract the core logic into a `def main(cli: FlextInfraUtilitiesCli.CliArgs) -> int | None:` function
   - Replace the entire `if __name__ == "__main__":` block with: `FlextInfraUtilitiesCli.run_cli(main)`
   - Remove duplicate structlog init, argparse construction, exception handling
   - Preserve all business logic — only the bootstrap scaffolding changes

2. **D-07 bug fix in release/**main**.py**: Find the line near line 143 where `dry_run=cli.apply` is used. This is semantically inverted — `cli.apply` means "do apply changes", so `dry_run` should be `not cli.apply`. Fix to: `dry_run=not cli.apply`.

3. Run ruff on each modified file after changes.

Expected: 12 files simplified to use centralized bootstrap. Each file loses ~20-50 lines of duplicate scaffold.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && sg --pattern 'run_cli($$$)' --lang py flext-infra/src/flext_infra/ 2>/dev/null | wc -l && echo "dry_run bug:"; sg --pattern 'dry_run=cli.apply' --lang py flext-infra/src/flext_infra/release/**main**.py 2>/dev/null; echo "Exit: $?" && .venv/bin/ruff check flext-infra/src/flext_infra/ --select E,F,W</automated>
  </verify>
  <done>All 12 **main**.py files use run_cli() as sole bootstrap, D-07 dry_run bug fixed (dry_run=not cli.apply), all lint-clean</done>
</task>

<task type="auto">
  <name>Task 3: Normalize root: Path to workspace_root: Path across flext_infra</name>
  <files>
    flext-infra/src/flext_infra/docs/__main__.py
    flext-infra/src/flext_infra/release/__main__.py
    flext-infra/src/flext_infra/git/__main__.py
    flext-infra/src/flext_infra/validate/__main__.py
    flext-infra/src/flext_infra/refactor/__main__.py
    flext-infra/src/flext_infra/_utilities/discovery.py
  </files>
  <read_first>
    - .planning/phases/03-infrastructure-centralization/03-RESEARCH.md (pitfall 2: project_root vs workspace_root)
  </read_first>
  <action>
Per D-08, normalize parameter naming across flext_infra service interfaces:

1. Use Grep to find all `root: Path` parameters in `flext-infra/src/`:
2. For EACH match, trace the call site to determine if it receives `cli.workspace` (workspace root) or a specific project directory:
   - If it receives `cli.workspace` or is a workspace-scoped operation: rename `root` -> `workspace_root`
   - If `project_root: Path` correctly refers to a project directory: LEAVE AS-IS (per research pitfall 2)
3. Update all call sites to pass `workspace_root=` keyword argument
4. Do NOT rename `project_root` that semantically refers to a project directory
5. Run ruff after each file change

Expected: ~20 `root: Path` renames in service methods across docs, release, git, validate, refactor, discovery subpackages.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && sg --pattern 'def $FN($$$, root: Path, $$$)' --lang py flext-infra/src/ 2>/dev/null; echo "Exit: $?"; .venv/bin/ruff check flext-infra/src/ --select E,F,W</automated>
  </verify>
  <done>workspace_root is the canonical parameter name across all flext_infra signatures — no bare root: Path variants remain</done>
</task>

</tasks>

<verification>
- `sg --pattern 'def $FN($$$, root: Path, $$$)' --lang py flext-infra/src/` returns 0 matches
- `python -c "from flext_infra import FlextInfraUtilitiesCli; print(FlextInfraUtilitiesCli.run_cli)"` succeeds
- `sg --pattern 'run_cli($$$)' --lang py flext-infra/src/flext_infra/` shows 12 call sites
- `sg --pattern 'dry_run=cli.apply' --lang py flext-infra/src/` returns 0 matches
- `ruff check flext-infra/src/` passes
</verification>

<success_criteria>
- run_cli() is callable static method centralizing bootstrap
- All 12 **main**.py files use run_cli() — zero duplicate bootstrap patterns
- iter_projects() and emit() are callable static methods
- D-07 dry_run bug fixed (dry_run=not cli.apply)
- Zero `root: Path` (bare) parameters in flext_infra service interfaces
- All files lint-clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-centralization/03-01-SUMMARY.md`
</output>
