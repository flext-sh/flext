---
phase: 03-infrastructure-centralization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - flext-infra/tests/unit/test_namespace_source_detector.py
  - flext-infra/src/flext_infra/namespace/__main__.py
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "NamespaceSourceDetector detects namespace source violations"
    - "NamespaceSourceDetector test suite passes with full coverage of detection logic"
    - "Workspace-wide application produces zero new violations"
  artifacts:
    - path: "flext-infra/tests/unit/test_namespace_source_detector.py"
      provides: "Unit tests for NamespaceSourceDetector"
      contains: "test_detect"
  key_links:
    - from: "flext-infra/tests/unit/test_namespace_source_detector.py"
      to: "flext-infra/src/flext_infra/namespace/"
      via: "imports detector class"
      pattern: "NamespaceSourceDetector"
---

<objective>
Verify and complete the NamespaceSourceDetector test suite, then run workspace-wide to confirm zero violations.

Purpose: INFRA-04 requires NamespaceSourceDetector live with passing tests per D-09.
Output: Complete test suite + workspace-wide clean run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-infrastructure-centralization/03-CONTEXT.md
@.planning/phases/03-infrastructure-centralization/03-RESEARCH.md

@flext-infra/src/flext_infra/namespace/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and complete NamespaceSourceDetector test suite</name>
  <files>flext-infra/tests/unit/test_namespace_source_detector.py</files>
  <read_first>
    - flext-infra/src/flext_infra/namespace/ (all files — understand detector model, detector class, fixer)
    - flext-infra/tests/unit/ (check for existing namespace detector tests)
    - .planning/phases/03-infrastructure-centralization/03-RESEARCH.md
  </read_first>
  <action>
Per D-09, ensure NamespaceSourceDetector has a complete test suite:

1. Read the NamespaceSourceDetector class to understand its detection API (what methods exist, what models it returns)
2. Check if tests already exist in `flext-infra/tests/`
3. If tests exist: review coverage, add missing cases
4. If tests don't exist: create `test_namespace_source_detector.py` with:
   - Test valid namespace source (no violation detected)
   - Test violation: importing from wrong namespace root
   - Test violation: importing private internals from sibling project
   - Test auto-fixer rewrites violation correctly
   - Test edge cases: re-exports, conditional imports, TYPE_CHECKING blocks
5. Use `from flext_infra import ...` imports following project conventions
6. Use pytest fixtures for sample Python source strings
7. Run tests: `make test PROJECT=flext-infra` or `pytest flext-infra/tests/unit/test_namespace_source_detector.py`
  </action>

  <verify>
    <automated>cd /home/marlonsc/flext && .venv/bin/pytest flext-infra/tests/unit/test_namespace_source_detector.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test_namespace_source_detector.py exists in flext-infra/tests/unit/
    - At least 5 test functions covering: valid source, violation detection, auto-fix, edge cases
    - All tests pass (pytest exit code 0)
    - Tests import from `flext_infra` namespace (not private internals)
  </acceptance_criteria>
  <done>NamespaceSourceDetector has a passing test suite covering detection, fixing, and edge cases</done>
</task>

<task type="auto">
  <name>Task 2: Run NamespaceSourceDetector workspace-wide and fix violations</name>
  <files>flext-infra/src/flext_infra/namespace/__main__.py</files>
  <read_first>
    - flext-infra/src/flext_infra/namespace/__main__.py (understand CLI entry point)
  </read_first>
  <action>
Run the NamespaceSourceDetector across the entire workspace:

1. Find the CLI entry point: `python -m flext_infra.namespace --help` (or equivalent)
2. Run in check mode first: `python -m flext_infra.namespace --check` to see current violations
3. If violations found:
   - Run in apply mode: `python -m flext_infra.namespace --apply` to auto-fix
   - Verify fixes with ruff check on affected files
   - Re-run in check mode to confirm 0 violations
4. If no violations: document clean state
5. Run `make check PROJECT=flext-infra` to verify nothing broken
  </action>

  <verify>
    <automated>cd /home/marlonsc/flext && .venv/bin/python -m flext_infra.namespace --check 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m flext_infra.namespace --check` exits 0 with 0 violations
    - No ruff errors introduced by auto-fixes
    - make check PROJECT=flext-infra passes (or affected projects pass)
  </acceptance_criteria>
  <done>NamespaceSourceDetector runs workspace-wide with 0 violations, test suite passes</done>
</task>

</tasks>

<verification>
- pytest flext-infra/tests/unit/test_namespace_source_detector.py passes
- python -m flext_infra.namespace --check exits 0
</verification>

<success_criteria>

- NamespaceSourceDetector has complete, passing test suite
- Workspace-wide run shows 0 namespace source violations
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-centralization/03-02-SUMMARY.md`
</output>
