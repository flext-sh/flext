---
phase: 03-infrastructure-centralization
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - flext-tests/src/flext_tests/_utilities/validators.py
  - flext-core/tests/unit/test_models.py
  - flext-ldif/tests/unit/test_models.py
  - flext-quality/src/flext_quality/tools/style_validator.py
  - flext-quality/src/flext_quality/tools/content_analyzer.py
  - flext-meltano/src/flext_meltano/services/runner.py
  - flext-quality/src/flext_quality/tools/workspace_makefile.py
autonomous: true
requirements:
  - WA-01
  - WA-02
  - WA-03
  - WA-04
  - WA-06

must_haves:
  truths:
    - "Zero try/except ImportError in production code"
    - "Zero model_rebuild() calls anywhere"
    - "Zero bare except Exception: in production code"
    - "Zero sys.exit() outside __main__.py"
    - "Zero subprocess.run() direct calls outside wrapper"
  artifacts:
    - path: "flext-tests/src/flext_tests/_utilities/validators.py"
      provides: "find_spec() replacements for ImportError catches"
      contains: "find_spec"
  key_links:
    - from: "flext-meltano/src/flext_meltano/services/runner.py"
      to: "flext-cli/src/flext_cli/_utilities/runtime.py"
      via: "subprocess.run -> u.Infra.run_raw/run_checked"
      pattern: "run_raw\\|run_checked"
---

<objective>
Eradicate workaround antipatterns WA-01, WA-02, WA-03, WA-04, WA-06 across all 33 projects.

Purpose: Zero workarounds in production per D-11 through D-16 (excluding WA-05 print() which is handled separately due to size).
Output: Clean grep sweeps for all 5 workaround categories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-infrastructure-centralization/03-CONTEXT.md
@.planning/phases/03-infrastructure-centralization/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WA-01 (ImportError), WA-02 (model_rebuild), WA-04 (sys.exit)</name>
  <files>
    flext-tests/src/flext_tests/_utilities/validators.py
    flext-core/tests/unit/test_models.py
    flext-ldif/tests/unit/test_models.py
    flext-quality/src/flext_quality/tools/style_validator.py
    flext-quality/src/flext_quality/tools/content_analyzer.py
  </files>
  <read_first>
    - .planning/phases/03-infrastructure-centralization/03-RESEARCH.md (violation baseline table)
  </read_first>
  <action>
Fix three workaround categories:

**WA-01 — try/except ImportError (3 instances in flext-tests/src):**
Per D-11, replace with `importlib.util.find_spec()` feature flags:

1. Use Grep tool for `except ImportError` in `flext-tests/src/`
2. For each match: replace `try: import X; except ImportError:` with:

   ```python
   import importlib.util

   HAS_X = importlib.util.find_spec("x") is not None
   ```

3. Update downstream code to check `HAS_X` flag instead of try/except

**WA-02 — model_rebuild() (7 instances in test files):**
Per D-12, remove all model_rebuild() calls:

1. Use Grep tool for `model_rebuild` in entire repo
2. For each match in test files: remove the call. Pydantic v2 with bare sibling names in namespace classes doesn't need rebuild. If forward refs are needed, use `from __future__ import annotations` or restructure.
3. Verify tests still pass after removal

**WA-04 — sys.exit() outside **main**.py (2 instances in flext-quality):**
Per D-14, in `style_validator.py` and `content_analyzer.py`:

1. Replace `sys.exit(N)` with `raise SystemExit(N)` — this propagates to `__main__.py` for handling
2. Or refactor to return an exit code that `__main__.py` uses

Run `ruff check` on each modified file after changes.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && echo "WA-01:" && sg --pattern 'except ImportError' --lang py flext-tests/src/ 2>/dev/null && echo "WA-02:" && sg --pattern 'model_rebuild()' --lang py . 2>/dev/null && echo "WA-04:" && sg --pattern 'sys.exit($$$)' --lang py flext-quality/src/ 2>/dev/null && echo "=== DONE ==="</automated>
  </verify>
  <acceptance_criteria>
    - Zero `except ImportError` in flext-tests/src/ (replaced with find_spec flags)
    - Zero `model_rebuild()` in any .py file across the repo
    - Zero `sys.exit()` in production code outside **main**.py files
    - ruff check passes on all modified files
  </acceptance_criteria>
  <done>WA-01, WA-02, WA-04 violations are zero across the entire monorepo</done>
</task>

<task type="auto">
  <name>Task 2: Fix WA-06 (subprocess.run) and verify WA-03</name>
  <files>
    flext-meltano/src/flext_meltano/services/runner.py
    flext-quality/src/flext_quality/tools/workspace_makefile.py
  </files>
  <read_first>
    - flext-cli/src/flext_cli/_utilities/runtime.py (existing run_raw/run_checked API)
    - .planning/phases/03-infrastructure-centralization/03-RESEARCH.md (WA-06 baseline: 5 instances in 4 files)
  </read_first>
  <action>
**WA-06 — subprocess.run() direct calls (5 instances outside wrapper):**
Per D-16, route through `u.Infra.run_raw()` or `u.Infra.run_checked()`:
1. Use Grep tool for `subprocess.run` across all `*/src/` directories (exclude the flext-cli runtime itself in flext-cli/_utilities/runtime.py)
2. For each match:
   - Read the call to understand args (capture_output, check, cwd, etc.)
   - Replace with `u.Infra.run_raw(cmd, ...)` (if caller handles return code) or `u.Infra.run_checked(cmd, ...)` (if subprocess errors should raise)
   - Add `from flext_infra import u` import if not present
   - Match the existing API of run_raw/run_checked (read subprocess.py first to understand signatures)
3. Expected files: flext-meltano runner.py, flext-quality tools, gruponos workspace_makefile

**WA-03 — bare except Exception: (verification only):**
Research shows 0 in production. Verify with Grep tool sweep:

1. Use Grep tool for `except Exception:` (with colon, no variable) across `*/src/`
2. Confirm 0 matches
3. If any found, replace with specific exception types

Run ruff check on all modified files.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && echo "WA-06:" && sg --pattern 'subprocess.run($$$)' --lang py flext-meltano/src/ flext-quality/src/ 2>/dev/null && echo "WA-03:" && sg --pattern 'except Exception:' --lang py . 2>/dev/null | head -20 && echo "=== DONE ==="</automated>
  </verify>
  <acceptance_criteria>
    - Zero `subprocess.run` in any `*/src/` file except flext-cli/_utilities/runtime.py
    - Zero `except Exception:` in any `*/src/` file
    - ruff check passes on all modified files
    - All subprocess calls routed through u.Infra.run_raw() or u.Infra.run_checked()
  </acceptance_criteria>
  <done>WA-03 confirmed clean, WA-06 violations are zero — all subprocess calls route through centralized wrapper</done>
</task>

</tasks>

<verification>
- ast-grep sweeps for all 5 categories return 0 matches in production code
- ruff check passes on all modified files
- make test PROJECT=flext-core passes (model_rebuild removal)
</verification>

<success_criteria>

- WA-01: Zero try/except ImportError in production
- WA-02: Zero model_rebuild() anywhere
- WA-03: Zero bare except Exception: in production (confirmed)
- WA-04: Zero sys.exit() outside **main**.py
- WA-06: Zero subprocess.run() outside wrapper
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-centralization/03-03-SUMMARY.md`
</output>
