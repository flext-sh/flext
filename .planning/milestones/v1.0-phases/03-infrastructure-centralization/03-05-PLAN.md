---
phase: 03-infrastructure-centralization
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - Makefile
autonomous: true
requirements:
  - INFRA-01
  - INFRA-05

must_haves:
  truths:
    - "make pyre exits 0 with 0 Any/object/ignore violations"
    - "make pyre outputs file+line on failure"
    - "u.Infra.run_cli() is the single bootstrap pattern across all __main__.py files"
    - "All INFRA and WA requirements verified clean in final sweep"
  artifacts:
    - path: "Makefile"
      provides: "Enhanced pyre target with policy gate output"
      contains: "pyrefly-repo"
  key_links:
    - from: "Makefile"
      to: "flext-infra/src/flext_infra/_utilities/cli.py"
      via: "make pyre invokes pyrefly with policy checks"
      pattern: "pyrefly"
---

<objective>
Enhance `make pyre` policy gate to enforce 0 `Any`/`object`/`ignore` violations with file+line output, verify INFRA-01 (run_cli centralization), and run final sweep confirming all 11 requirements are met.

Purpose: INFRA-01 verification + INFRA-05 policy gate + final phase gate.
Output: Enhanced Makefile target + clean verification of all requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-infrastructure-centralization/03-CONTEXT.md
@.planning/phases/03-infrastructure-centralization/03-RESEARCH.md

@.planning/phases/03-infrastructure-centralization/03-01-SUMMARY.md
@.planning/phases/03-infrastructure-centralization/03-02-SUMMARY.md
@.planning/phases/03-infrastructure-centralization/03-03-SUMMARY.md
@.planning/phases/03-infrastructure-centralization/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance make pyre policy gate with file+line violation output</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (find existing pyre/pyrefly-repo target around line 796)
    - .planning/phases/03-infrastructure-centralization/03-RESEARCH.md (INFRA-05 details)
  </read_first>
  <action>
Per D-10, enhance the `make pyre` (or `pyrefly-repo`) Makefile target:

1. Read the existing target to understand current implementation
2. Add post-run grep checks that scan pyrefly output for:
   - `Any` type usage violations
   - `object` annotation violations
   - `# type: ignore` comments
3. On failure: output file path + line number for each violation
4. Exit non-zero if any violations found
5. The enhanced target should:
   - Run pyrefly as before
   - Then run grep sweeps: `grep -rn 'typing.Any\|: Any\|-> Any' --include='*.py' */src/` (adapted for actual patterns)
   - Output violations with file+line format: `filename:line: violation description`
   - Exit 1 if count > 0

Keep the target compatible with existing `make pyre` usage.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && make pyre 2>&1 | tail -5; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - Makefile contains enhanced pyre/pyrefly-repo target
    - Target outputs file+line on violation detection
    - Target exits non-zero if Any/object/ignore violations found
    - Target exits 0 when workspace is clean
    - `make pyre` still works as before (backward compatible)
  </acceptance_criteria>
  <done>make pyre policy gate enforces 0 violations with file+line output on failure</done>
</task>

<task type="auto">
  <name>Task 2: Final sweep — verify all 11 requirements</name>
  <files>N/A (verification only)</files>
  <read_first>
    - .planning/phases/03-infrastructure-centralization/03-CONTEXT.md (all requirement definitions)
    - .planning/REQUIREMENTS.md (INFRA-01 through WA-06)
  </read_first>
  <action>
Run a comprehensive verification sweep for all 11 phase requirements:

**INFRA-01**: Verify `u.Infra.run_cli()` is the single bootstrap pattern:
```
grep -rn 'def main\|if __name__' --include='*.py' flext-infra/src/ | grep -v run_cli
```
Should show only `__main__.py` files using `run_cli`.

**INFRA-02**: Verify `iter_projects()` exists and is callable:
```
python -c "from flext_infra._utilities.cli import FlextInfraUtilitiesCli; print(FlextInfraUtilitiesCli.iter_projects)"
```

**INFRA-03**: Verify no bare `root: Path` in flext_infra service interfaces:
```
sg --pattern 'def $FN($$$, root: Path, $$$)' --lang py flext-infra/src/
```

**INFRA-04**: Verify NamespaceSourceDetector tests pass:
```
pytest flext-infra/tests/unit/test_namespace_source_detector.py -x
```

**INFRA-05**: Verify `make pyre` exits 0:
```
make pyre
```

**WA-01 through WA-06**: Run grep sweeps for each category (see 03-03 and 03-04 verification commands).

Document results. If any requirement fails, fix it in this task.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && echo "=== FINAL SWEEP ===" && echo "INFRA-02:" && .venv/bin/python -c "from flext_infra._utilities.cli import FlextInfraUtilitiesCli; print('OK')" && echo "WA-02:" && grep -rn 'model_rebuild' --include='*.py' */src/ */tests/ 2>/dev/null | wc -l && echo "WA-05:" && grep -rn 'print(' --include='*.py' */src/ 2>/dev/null | grep -v '# CLI output' | grep -v __pycache__ | wc -l && echo "=== DONE ==="</automated>
  </verify>
  <acceptance_criteria>
    - INFRA-01: All __main__.py files use u.Infra.run_cli()
    - INFRA-02: iter_projects() is importable and callable
    - INFRA-03: Zero bare `root: Path` in flext_infra service interfaces
    - INFRA-04: NamespaceSourceDetector tests pass
    - INFRA-05: make pyre exits 0
    - WA-01: Zero except ImportError in production
    - WA-02: Zero model_rebuild() anywhere
    - WA-03: Zero bare except Exception: in production
    - WA-04: Zero sys.exit() outside __main__.py
    - WA-05: Zero unauthorized print() in production
    - WA-06: Zero subprocess.run() outside wrapper
  </acceptance_criteria>
  <done>All 11 phase requirements verified clean — phase 03 complete</done>
</task>

</tasks>

<verification>
- make pyre exits 0
- All 11 requirement grep sweeps return 0 violations
- All modified files lint-clean
</verification>

<success_criteria>
- Policy gate enforced in Makefile
- All 11 INFRA + WA requirements verified at zero violations
- Phase 03 complete and ready for Phase 04
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-centralization/03-05-SUMMARY.md`
</output>
