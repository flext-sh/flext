---
phase: 08-workaround-residual-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flext-quality/src/flext_quality/docs/scheduled_maintenance.py
  - flext-quality/docs/maintenance/scheduled_maintenance.py
  - flext-core/tests/unit/test_context.py
  - flext-observability/tests/integration/test_phase_11_integration.py
  - flext-meltano/tests/unit/test_tap_abstractions.py
  - flext-meltano/tests/unit/test_services.py
  - flext-oracle-wms/tests/
  - flext-web/tests/
  - flext-plugin/examples/03_docker_integration.py
  - flext-auth/examples/
  - flext-db-oracle/examples/
  - flext-grpc/examples/03_error_handling_patterns.py
  - flext-meltano/examples/01_simple_working.py
  - flext-oracle-wms/examples/
  - flext-web/examples/
  - flext-target-oracle-wms/examples/03_error_handling.py
  - examples/advanced_processing_example.py
autonomous: true
requirements:
  - WA-03
  - WA-04
  - WA-05
must_haves:
  truths:
    - "Zero bare except Exception: in production src/ code (already true — verify only)"
    - "Zero sys.exit() outside __main__.py guards (already true — verify only)"
    - "Zero print() in production code — scheduled_maintenance.py fixed"
    - "Test files use specific exception types where feasible (per D-03)"
    - "Example files use specific exception types where trivial (per D-04)"
  artifacts:
    - path: "flext-quality/src/flext_quality/docs/scheduled_maintenance.py"
      provides: "print() replaced with structlog logger"
      contains: "structlog.get_logger"
    - path: "flext-quality/docs/maintenance/scheduled_maintenance.py"
      provides: "print() replaced with structlog logger"
      contains: "structlog.get_logger"
  key_links:
    - from: "scheduled_maintenance.py"
      to: "structlog"
      via: "get_logger import"
      pattern: "structlog\\.get_logger"
---

<objective>
Eliminate all residual workaround violations from v1.0 audit: replace 2 bare print() calls with structlog, fix ~30 bare except Exception: in tests/examples with specific types, and verify WA-04 compliance.

Purpose: Close WA-03, WA-04, WA-05 requirements for milestone completion.
Output: Zero violations across all three patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-workaround-residual-cleanup/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix print() in production + verify WA-04 compliance</name>
  <files>
    flext-quality/src/flext_quality/docs/scheduled_maintenance.py
    flext-quality/docs/maintenance/scheduled_maintenance.py
  </files>
  <read_first>
    flext-quality/src/flext_quality/docs/scheduled_maintenance.py
    flext-quality/docs/maintenance/scheduled_maintenance.py
    .planning/phases/08-workaround-residual-cleanup/08-RESEARCH.md
  </read_first>
  <action>
Per D-09 and D-10:

1. In `flext-quality/src/flext_quality/docs/scheduled_maintenance.py` around line 590:
   - Add `import structlog` at top if not present
   - Create module-level logger: `logger = structlog.get_logger(__name__)`
   - Replace `print(message)` in `_execute_echo_command` with `logger.info(message)`

2. In `flext-quality/docs/maintenance/scheduled_maintenance.py` around line 652:
   - Same treatment: add structlog import, module logger, replace print() with logger.info()

3. Verify WA-04 (per D-06/D-07/D-08): Run grep for sys.exit() across all src/ — confirm all are inside `if __name__ == "__main__":` guards. No changes needed per research.

4. Verify WA-03 production: Run grep for `except Exception:` across all `**/src/**/*.py` — confirm zero hits. No changes needed per research.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && sg --pattern 'print($$$)' --lang py flext-quality/src/flext_quality/docs/scheduled_maintenance.py | grep -v 'console.print\|cli_api.print\|self.print\|\.print(' | grep -c 'print(' | grep -q '^0$' && echo "WA-05 PASS" || echo "WA-05 FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "print(message)" flext-quality/src/flext_quality/docs/scheduled_maintenance.py returns 0 hits
    - grep -n "print(message)" flext-quality/docs/maintenance/scheduled_maintenance.py returns 0 hits
    - grep -n "structlog.get_logger" flext-quality/src/flext_quality/docs/scheduled_maintenance.py returns 1+ hits
    - grep -rn "except Exception:" --include="*.py" */src/ returns 0 hits (WA-03 verified)
    - All sys.exit() in src/ are inside __main__ guards (WA-04 verified)
  </acceptance_criteria>
  <done>Zero print() in production. WA-03 and WA-04 verified as already compliant.</done>
</task>

<task type="auto">
  <name>Task 2: Fix bare except Exception: in tests and examples</name>
  <files>
    flext-core/tests/unit/test_context.py
    flext-observability/tests/integration/test_phase_11_integration.py
    flext-meltano/tests/unit/test_tap_abstractions.py
    flext-meltano/tests/unit/test_services.py
    flext-oracle-wms/tests/
    flext-web/tests/
    flext-plugin/examples/03_docker_integration.py
    flext-auth/examples/
    flext-db-oracle/examples/
    flext-grpc/examples/03_error_handling_patterns.py
    flext-meltano/examples/01_simple_working.py
    flext-oracle-wms/examples/
    flext-web/examples/
    flext-target-oracle-wms/examples/03_error_handling.py
    examples/advanced_processing_example.py
  </files>
  <read_first>
    .planning/phases/08-workaround-residual-cleanup/08-RESEARCH.md
  </read_first>
  <action>
Per D-01, D-03, D-04:

**Tests (~30 occurrences in ~15 files):** For each bare `except Exception:`, read the try block and replace with the specific exception type per the research mapping:
- File I/O → `OSError`
- JSON → `json.JSONDecodeError`
- Dict/list → `KeyError`, `IndexError`
- Import → `ImportError`
- Type conversion → `ValueError`, `TypeError`
- Docker → `docker.errors.DockerException`
- Pydantic → `pydantic.ValidationError`
- Multiple → tuple `(SpecificA, SpecificB)`

**EXEMPT per D-03 (do NOT change):**
- `flext-ldif/tests/support/conftest_factory.py` (3 occurrences) — test cleanup utility
- `flext-meltano/tests/helpers/docker_test_manager.py` (1) — infrastructure cleanup
- `flext-ldap/tests/_utilities/docker_infra.py` (1) — infrastructure cleanup

**Examples (~12 occurrences):** Fix if the specific exception is obvious from context (per D-04). If complex, leave with a `# TODO: specify exception` comment.

**Other:** `fix_dollar.py` in root (2 occurrences) — fix if trivial.

**Per D-05:** Skip `.claude/skills/` validation scripts entirely.

For each file: read the try block, determine the operation, replace `except Exception:` with the specific type. Add imports if needed (e.g., `import json` for `json.JSONDecodeError`).
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && COUNT=$(sg --pattern 'except Exception:' --lang py flext-core/tests/ flext-observability/tests/ flext-meltano/tests/ flext-oracle-wms/tests/ flext-web/tests/ flext-plugin/examples/ flext-auth/examples/ flext-db-oracle/examples/ flext-grpc/examples/ flext-meltano/examples/ flext-oracle-wms/examples/ flext-web/examples/ flext-target-oracle-wms/examples/ examples/ 2>/dev/null | grep -c 'except Exception:' || echo 0) && EXEMPT=$(sg --pattern 'except Exception:' --lang py flext-ldif/tests/support/conftest_factory.py flext-meltano/tests/helpers/docker_test_manager.py flext-ldap/tests/_utilities/docker_infra.py 2>/dev/null | grep -c 'except Exception:' || echo 0) && echo "Non-exempt: $COUNT, Exempt: $EXEMPT"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "except Exception:" in test files returns only exempt files (conftest_factory, docker_test_manager, docker_infra)
    - grep -rn "except Exception:" in example files returns 0 or only complex cases with TODO comments
    - No new ruff or pyrefly errors introduced
  </acceptance_criteria>
  <done>All non-exempt bare except Exception: in tests/examples replaced with specific types. D-03 exemptions preserved.</done>
</task>

</tasks>

<verification>
Final phase verification:
1. `sg --pattern 'except Exception:' --lang py` across all `**/src/**/*.py` → 0 hits
2. `sg --pattern 'except Exception:' --lang py` across tests/examples → only D-03 exempt files
3. `grep -rn 'print(message)' flext-quality/src/` → 0 hits
4. All `sys.exit()` in src/ inside `__main__` guards
5. `make lint` passes on changed files
</verification>

<success_criteria>
- WA-03: Zero bare except Exception: in src/. Tests/examples have specific types except D-03 exemptions.
- WA-04: Zero sys.exit() outside **main** guards (verified, no changes needed).
- WA-05: Zero print() in production code.
</success_criteria>

<output>
After completion, create `.planning/phases/08-workaround-residual-cleanup/08-01-SUMMARY.md`
</output>
