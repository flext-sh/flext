---
phase: 08-workaround-residual-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - flext-plugin/src/flext_plugin/hot_reload.py
  - flext-plugin/src/flext_plugin/loader.py
autonomous: true
requirements: [WA-05]
gap_closure: true

must_haves:
  truths:
    - "Zero print() calls in production src/ code (except documented CLI output exemptions)"
  artifacts:
    - path: "flext-plugin/src/flext_plugin/hot_reload.py"
      provides: "structlog logger instead of print"
    - path: "flext-plugin/src/flext_plugin/loader.py"
      provides: "structlog logger instead of print"
  key_links:
    - from: "flext-plugin/src/flext_plugin/hot_reload.py"
      to: "structlog"
      via: "structlog.get_logger import"
      pattern: "structlog\\.get_logger"
    - from: "flext-plugin/src/flext_plugin/loader.py"
      to: "structlog"
      via: "structlog.get_logger import"
      pattern: "structlog\\.get_logger"
---

<objective>
Replace 2 remaining print() calls in flext-plugin with structlog logger calls.

Purpose: Close WA-05 gap — flext-plugin/hot_reload.py and loader.py have bare print() in production code.
Output: Both files use structlog, zero print() in production src/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-workaround-residual-cleanup/08-CONTEXT.md
@.planning/phases/08-workaround-residual-cleanup/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace print() with structlog in flext-plugin</name>
  <files>
    flext-plugin/src/flext_plugin/hot_reload.py
    flext-plugin/src/flext_plugin/loader.py
  </files>
  <read_first>
    flext-plugin/src/flext_plugin/hot_reload.py
    flext-plugin/src/flext_plugin/loader.py
  </read_first>
  <action>
Per D-09 and the Phase 08 pattern (structlog.get_logger), replace both print() calls:

1. **flext-plugin/src/flext_plugin/hot_reload.py:84**
   - Add `import structlog` at top if not present
   - Add `logger = structlog.get_logger(__name__)` at module level if not present
   - Replace `print("Hot reload monitoring started")` with `logger.info("hot_reload_monitoring_started")`

2. **flext-plugin/src/flext_plugin/loader.py:40**
   - Add `import structlog` at top if not present
   - Add `logger = structlog.get_logger(__name__)` at module level if not present
   - Replace `print(f"Loaded plugin: {load_data.name}")` with `logger.info("plugin_loaded", plugin_name=load_data.name)`

Use snake_case event names (structlog convention). Pass dynamic data as keyword arguments, not f-strings.

Run ruff check on both files after edits.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && sg --pattern 'print($$$)' --lang py flext-plugin/src/flext_plugin/hot_reload.py flext-plugin/src/flext_plugin/loader.py | wc -l | xargs test 0 -eq && sg --pattern 'structlog.get_logger($$$)' --lang py flext-plugin/src/flext_plugin/hot_reload.py flext-plugin/src/flext_plugin/loader.py | wc -l | xargs test 2 -eq</automated>
  </verify>
  <acceptance_criteria>
    - Zero print() calls in hot_reload.py and loader.py
    - Both files contain `structlog.get_logger`
    - ruff check passes on both files
  </acceptance_criteria>
  <done>Both files use structlog logger — zero print() in flext-plugin production code</done>
</task>

</tasks>

<verification>
```bash
# Zero print() in flext-plugin src/
cd /home/marlonsc/flext
sg --pattern 'print($$$)' --lang py flext-plugin/src/flext_plugin/hot_reload.py flext-plugin/src/flext_plugin/loader.py
# Expected: 0 matches
```
</verification>

<success_criteria>

- Zero print() in flext-plugin/src/ production code
- structlog.get_logger present in both modified files
- WA-05 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-workaround-residual-cleanup/08-03-SUMMARY.md`
</output>
