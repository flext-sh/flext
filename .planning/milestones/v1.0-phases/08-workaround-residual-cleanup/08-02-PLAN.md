---
phase: 08-workaround-residual-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - flext-api/src/flext_api/serializers.py
  - flext-core/src/flext_core/context.py
  - flext-ldif/src/flext_ldif/services/conversion.py
  - flext-target-ldif/src/flext_target_ldif/writer.py
  - flext-tap-oracle-wms/src/flext_tap_oracle_wms/tap.py
autonomous: true
requirements: [WA-03, WA-04]
gap_closure: true

must_haves:
  truths:
    - "Zero bare or tuple-embedded except Exception: in production src/ code"
    - "All catch handlers specify only the exception types actually expected"
    - "WA-04 sys.exit compliance confirmed per D-08 rationale"
  artifacts:
    - path: "flext-api/src/flext_api/serializers.py"
      provides: "Specific exception catch without Exception"
    - path: "flext-core/src/flext_core/context.py"
      provides: "Two TypeError-only catches (no Exception)"
    - path: "flext-ldif/src/flext_ldif/services/conversion.py"
      provides: "Specific exception tuple without Exception"
    - path: "flext-target-ldif/src/flext_target_ldif/writer.py"
      provides: "Specific exception tuple without Exception"
    - path: "flext-tap-oracle-wms/src/flext_tap_oracle_wms/tap.py"
      provides: "Two specific exception catches replacing bare Exception"
  key_links: []
---

<objective>
Remove all 7 remaining `except Exception` violations from production src/ code. Also verify WA-04 (sys.exit) compliance.

Purpose: Close WA-03 gap — research missed tuple-embedded Exception catches and 2 bare catches in tap-oracle-wms. Confirm WA-04 compliance per D-08.
Output: 5 files with specific exception types only, zero bare Exception in production src/. WA-04 verification documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-workaround-residual-cleanup/08-CONTEXT.md
@.planning/phases/08-workaround-residual-cleanup/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Exception from catch tuples in 4 files</name>
  <files>
    flext-api/src/flext_api/serializers.py
    flext-core/src/flext_core/context.py
    flext-ldif/src/flext_ldif/services/conversion.py
    flext-target-ldif/src/flext_target_ldif/writer.py
  </files>
  <read_first>
    flext-api/src/flext_api/serializers.py
    flext-core/src/flext_core/context.py
    flext-ldif/src/flext_ldif/services/conversion.py
    flext-target-ldif/src/flext_target_ldif/writer.py
  </read_first>
  <action>
For each file, remove `Exception` from the catch tuple — the specific types already cover the intent:

1. **flext-api/src/flext_api/serializers.py:103** — Change `except (ValidationError, Exception) as e:` to `except (ValidationError,) as e:` (or just `except c.ValidationError as e:` if only one type remains). If ValidationError alone is insufficient, analyze the try block to determine what other specific types could be raised and add those instead.

2. **flext-core/src/flext_core/context.py:951** — Change `except (TypeError, Exception) as e:` to `except TypeError as e:`. The TypeError is the specific type; Exception was a catch-all safety net. If the try block can raise other specific types, add those.

3. **flext-core/src/flext_core/context.py:1003** — Same as above: `except (TypeError, Exception) as e:` to `except TypeError as e:`.

4. **flext-ldif/src/flext_ldif/services/conversion.py:758** — Change `except (ValueError, TypeError, AttributeError, RuntimeError, Exception) as e:` to `except (ValueError, TypeError, AttributeError, RuntimeError) as e:`. The 4 specific types already cover the intent.

5. **flext-target-ldif/src/flext_target_ldif/writer.py:100** — Change `except (RuntimeError, ValueError, TypeError, OSError, Exception) as e:` to `except (RuntimeError, ValueError, TypeError, OSError) as e:`. The 4 specific types already cover the intent.

After edits, run ruff check on each file to ensure no lint errors introduced.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && sg --pattern 'except ($$$Exception$$$) as $E' --lang py flext-api/src/flext_api/serializers.py flext-core/src/flext_core/context.py flext-ldif/src/flext_ldif/services/conversion.py flext-target-ldif/src/flext_target_ldif/writer.py | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of the word "Exception" in any except clause in these 4 files
    - ruff check passes on all 4 files
  </acceptance_criteria>
  <done>All 5 catch sites in 4 files use only specific exception types — no Exception in any tuple</done>
</task>

<task type="auto">
  <name>Task 2: Replace bare except Exception in flext-tap-oracle-wms/tap.py</name>
  <files>flext-tap-oracle-wms/src/flext_tap_oracle_wms/tap.py</files>
  <read_first>flext-tap-oracle-wms/src/flext_tap_oracle_wms/tap.py</read_first>
  <action>
Two bare `except Exception as exc:` catches at lines 120 and 251. For each:

1. Read the try block to understand what operations are performed.
2. Identify the specific exception types those operations can raise. For a Singer tap (oracle-wms), likely candidates:
   - Database operations: `oracledb.Error`, `oracledb.DatabaseError`
   - Network/connection: `ConnectionError`, `TimeoutError`
   - Data processing: `ValueError`, `TypeError`, `KeyError`
   - Singer SDK: `RuntimeError`
3. Replace `except Exception as exc:` with `except (SpecificType1, SpecificType2, ...) as exc:` using only the types that match the try block's operations.

If analysis shows the handler is a top-level fault barrier (e.g., wraps an entire sync/discover call), use the broadest specific types for that domain: `except (oracledb.Error, RuntimeError, OSError, ValueError) as exc:` — covering DB, runtime, network, and data errors without the catch-all Exception.

Run ruff check after edits.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && sg --pattern 'except Exception as $E' --lang py flext-tap-oracle-wms/src/flext_tap_oracle_wms/tap.py | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of "except Exception" in tap.py
    - Each catch uses specific exception types appropriate to the try block's operations
    - ruff check passes
  </acceptance_criteria>
  <done>Both catch sites in tap.py use specific exception types — zero bare Exception</done>
</task>

<task type="auto">
  <name>Task 3: Verify WA-04 sys.exit compliance per D-08</name>
  <files></files>
  <action>
Verify WA-04 compliance per D-06/D-07/D-08 decisions. This is a verification-only task — no code changes expected.

1. Run `sg --pattern 'sys.exit($$$)' --lang py` across all production src/ directories to find any sys.exit() calls.
2. For each match, confirm it is inside a `__main__.py` file (compliant per D-06/D-08) or inside an `if __name__ == "__main__":` guard (compliant per D-08).
3. If any non-compliant sys.exit() is found outside **main**.py without a guard, flag it. Per Phase 08-01, these should already be resolved.
4. Document the verification result in the plan summary: list all sys.exit() locations and their compliance status.

Per D-08: files that ARE **main**.py with sys.exit() in any position are compliant — that is the intended pattern.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && sg --pattern 'sys.exit($$$)' --lang py $(find . -path '*/src/*' -name '*.py' ! -name '**main**.py' ! -path './.venv/*' ! -path './docs/*' ! -path './.claude/*' 2>/dev/null) | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - All sys.exit() calls in production src/ are either in **main**.py files or inside `if __name__ == "__main__":` guards
    - Verification documented in summary
  </acceptance_criteria>
  <done>WA-04 compliance confirmed — all sys.exit() in production src/ follows D-06/D-07/D-08 pattern</done>
</task>

</tasks>

<verification>
```bash
# Zero Exception catches in all 5 modified files
cd /home/marlonsc/flext
sg --pattern 'except ($$$Exception$$$) as $E' --lang py \
  flext-api/src/flext_api/serializers.py \
  flext-core/src/flext_core/context.py \
  flext-ldif/src/flext_ldif/services/conversion.py \
  flext-target-ldif/src/flext_target_ldif/writer.py \
  flext-tap-oracle-wms/src/flext_tap_oracle_wms/tap.py
# Expected: 0 matches
```
</verification>

<success_criteria>
- Zero `except Exception` (bare or in tuple) across all production src/ in the 5 modified files
- ruff check clean on all modified files
- WA-03 requirement fully satisfied
- WA-04 compliance verified and documented
</success_criteria>

<output>
After completion, create `.planning/phases/08-workaround-residual-cleanup/08-02-SUMMARY.md`
</output>
