---
phase: 04-python313-modernization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - "flext-core/src/flext_core/_utilities/deprecation.py"
  - "flext-auth/src/flext_auth/models.py"
autonomous: true
requirements: [MOD-02, MOD-06]

must_haves:
  truths:
    - "FlextUtilitiesDeprecation wrapper is removed or reduced to non-stdlib-equivalent methods only"
    - "ProviderConfiguration in flext-auth uses Pydantic BaseModel instead of UserDict"
  artifacts:
    - path: "flext-core/src/flext_core/_utilities/deprecation.py"
      provides: "Simplified or removed deprecation framework"
    - path: "flext-auth/src/flext_auth/models.py"
      provides: "ProviderConfiguration as BaseModel"
      contains: "class ProviderConfiguration"
  key_links:
    - from: "caller sites"
      to: "warnings.deprecated"
      via: "direct stdlib import"
      pattern: "from warnings import deprecated"
---

<objective>
Remove FlextUtilitiesDeprecation wrapper where stdlib replaces it, and replace the single production UserDict with Pydantic BaseModel.

Purpose: Delete custom code that duplicates stdlib; enforce Pydantic-native data modeling.
Output: Deprecation framework simplified; UserDict eliminated from production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-python313-modernization/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify FlextUtilitiesDeprecation — remove stdlib-equivalent methods</name>
  <read_first>
    flext-core/src/flext_core/_utilities/deprecation.py
  </read_first>
  <files>
    flext-core/src/flext_core/_utilities/deprecation.py
    Any files calling u.deprecated(), u.deprecated_class()
  </files>
  <action>
    1. Read `flext-core/src/flext_core/_utilities/deprecation.py` (193 lines) fully.

    2. Research found 0 callers of `u.deprecated` in src/. Verify with Grep:
       Search for `u.deprecated`, `deprecated(`, `deprecated_class(`, `deprecated_parameter(`,
       `warn_once(`, `warn_polymorphic_input(` across all `*/src/**/*.py`.

    3. If the framework is ENTIRELY unused (0 callers for any method):
       - NOTE: `_utilities/*` is FROZEN per AGENTS.md §10.2.
       - If frozen: leave deprecation.py as-is but document it as dead code.
         Add a `# DEAD CODE: no callers found 2026-03-24` comment at module level.
       - If operator unfreezes: delete the file entirely and update __init__.py generators.

    4. If some methods HAVE callers:
       - Replace `u.deprecated()` callers with `from warnings import deprecated` + `@deprecated("msg")`
       - Replace `u.deprecated_class()` callers with `@deprecated("msg")` on the class
       - Keep `deprecated_parameter()`, `warn_once()`, `warn_polymorphic_input()` as standalone
         utilities (no stdlib equivalent exists)

    5. Run `make lint` and `.venv/bin/pyrefly check` on flext-core after changes.

    NOTE: Due to freeze policy on `_utilities/*`, the primary action may be caller-site changes only.
    If the entire framework is unused, document as dead code for future cleanup.

    Per MOD-02: warnings.deprecated (PEP 702) replaces custom FlextUtilitiesDeprecation framework.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && .venv/bin/python -c "
import subprocess, sys
# Check for callers of u.deprecated (should be 0 or all converted)
patterns = ['u.deprecated(', 'u.deprecated_class(']
for pat in patterns:
    r = subprocess.run(['grep', '-rn', pat, '--include=*.py', '.'], capture_output=True, text=True)
    callers = [l for l in r.stdout.strip().split('\n') if l and '/src/' in l and 'deprecation.py' not in l]
    if callers:
        print(f'FAIL: {pat} still called from: {callers}')
        sys.exit(1)
print('PASS: No u.deprecated/u.deprecated_class callers outside deprecation.py')
"</automated>
  </verify>
  <acceptance_criteria>
    - Zero callers of `u.deprecated()` or `u.deprecated_class()` in src/ (outside deprecation.py itself)
    - Any remaining callers use `from warnings import deprecated` directly
    - If framework is dead code, module-level comment documents this
    - `make lint` passes on flext-core
  </acceptance_criteria>
  <done>Deprecation framework either removed (if unfrozen) or documented as dead code (if frozen); all callers use stdlib.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ProviderConfiguration UserDict with Pydantic BaseModel</name>
  <read_first>
    flext-auth/src/flext_auth/models.py
  </read_first>
  <files>
    flext-auth/src/flext_auth/models.py
    Any files that reference ProviderConfiguration
  </files>
  <action>
    1. Read `flext-auth/src/flext_auth/models.py` — find `ProviderConfiguration(UserDict[str, t.ContainerValue])` around line 481.

    2. Find all references to `ProviderConfiguration` across the workspace using Grep.

    3. Convert from UserDict to Pydantic BaseModel:
       ```python
       # Before
       class ProviderConfiguration(UserDict[str, t.ContainerValue]): ...


       # After
       class ProviderConfiguration(m.BaseModel):
           model_config = ConfigDict(extra="allow")
           # Explicit fields for known keys (if any discovered from usage)
           # extra="allow" preserves dict-like flexibility for unknown keys
       ```

    4. Update all callers:
       - `settings["key"]` -> `settings.key` (for known fields) or `settings.model_extra["key"]` (for dynamic keys)
       - `settings.data` -> `settings.model_dump()` (UserDict's underlying dict)
       - `dict(settings)` -> `settings.model_dump()`
       - Constructor: `ProviderConfiguration({"k": "v"})` -> `ProviderConfiguration.model_validate({"k": "v"})`

    5. Alternatively, if ProviderConfiguration is used purely as a dict wrapper with no added behavior:
       Replace with `type ProviderConfiguration = Mapping[str, t.ContainerValue]` (a type alias).
       This is simpler if no methods are defined on the class.

    6. Run `.venv/bin/pytest flext-auth/tests/ -x --no-header -q` to verify.
    7. Run `make lint` on flext-auth.

    Per MOD-06: UserDict/UserString usages replaced with Pydantic BaseModel.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && .venv/bin/python -c "
import subprocess, sys
r = subprocess.run(['grep', '-rn', 'UserDict', '--include=*.py', '-l', '.'], capture_output=True, text=True)
prod_files = [l for l in r.stdout.strip().split('\n') if l and '/src/' in l and 'test' not in l.lower()]
if prod_files:
    print(f'FAIL: UserDict still in production code: {prod_files}')
    sys.exit(1)
print('PASS: No UserDict in production code')
"</automated>
  </verify>
  <acceptance_criteria>
    - Zero `UserDict` imports or usages in `*/src/**/*.py` files
    - `ProviderConfiguration` is now a Pydantic BaseModel (or type alias)
    - All callers updated to use BaseModel API
    - `.venv/bin/pytest flext-auth/tests/ -x` passes
    - `make lint` passes on flext-auth
  </acceptance_criteria>
  <done>ProviderConfiguration converted from UserDict to Pydantic BaseModel; tests and linters pass.</done>
</task>

</tasks>

<verification>
- `make lint` passes workspace-wide
- Zero UserDict in production src/ files
- Zero u.deprecated() callers outside deprecation.py
- `.venv/bin/pytest flext-auth/tests/ -x` passes
</verification>

<success_criteria>
MOD-02 and MOD-06 satisfied: deprecation framework simplified/removed, UserDict eliminated from production code.
</success_criteria>

<output>
After completion, create `.planning/phases/04-python313-modernization/04-03-SUMMARY.md`
</output>
