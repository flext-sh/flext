---
phase: 04-python313-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "*/src/**/*.py (callers of u.chunk)"
  - "*/src/**/*.py (setdefault grouping patterns)"
autonomous: true
requirements: [MOD-01, MOD-05]

must_haves:
  truths:
    - "Zero u.chunk() calls remain — all replaced with itertools.batched()"
    - "All hand-rolled grouping patterns use defaultdict instead of setdefault"
  artifacts:
    - path: "affected caller files"
      provides: "Direct itertools.batched() usage"
      contains: "itertools.batched"
    - path: "affected grouping files"
      provides: "defaultdict grouping"
      contains: "defaultdict"
  key_links:
    - from: "caller files"
      to: "itertools.batched"
      via: "direct import"
      pattern: "from itertools import batched"
---

<objective>
Replace custom chunking (u.chunk) with itertools.batched and hand-rolled grouping patterns with defaultdict across all projects.

Purpose: Eliminate custom code where Python 3.13 stdlib provides identical functionality.
Output: All callers use stdlib directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-python313-modernization/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace u.chunk() callers with itertools.batched</name>
  <read_first>
    flext-core/src/flext_core/_utilities/collection.py
  </read_first>
  <files>All files calling u.chunk() across the workspace</files>
  <action>
    1. Find all callers of u.chunk() or chunk() across src/ directories:
       Use Grep to search for `u.chunk(` and `chunk(` patterns in all `*/src/**/*.py` files.

    2. For each caller, replace:
       ```python
       # Before
       batches = u.chunk(records, 100)
       # After
       from itertools import batched

       batches = batched(records, 100)
       ```
       Note: `itertools.batched()` returns an iterator of tuples, while `u.chunk()` may return lists.
       If callers iterate (for loop), the tuple vs list difference is transparent.
       If callers index or expect list, wrap with `list()`: `list(batched(records, 100))`.

    3. The `chunk()` method in `_utilities/collection.py` is in the FROZEN `_utilities/*` directory.
       Do NOT modify collection.py itself. Only change caller sites to bypass the wrapper.

    4. Run `make lint` and `.venv/bin/pyrefly check` on each affected project after changes.

    Per MOD-01: itertools.batched replaces all custom chunking/batching utility code.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && .venv/bin/python -c "
import subprocess, sys
result = subprocess.run(['grep', '-r', 'u.chunk(', '--include=*.py', '-l'] + [f'{p}/src/' for p in ['flext-core','flext-infra','flext-tests','flext-cli','flext-api','flext-auth','flext-web','flext-grpc','flext-ldap','flext-ldif','flext-meltano','flext-observability','flext-oracle-oic','flext-oracle-wms','flext-quality','flext-plugin']], capture_output=True, text=True)
if result.stdout.strip():
    print(f'FAIL: u.chunk() still found in: {result.stdout.strip()}')
    sys.exit(1)
print('PASS: No u.chunk() callers remain')
"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `u.chunk(` in any `*/src/**/*.py` file
    - All replacements use `from itertools import batched` directly
    - `make lint` passes on affected projects
  </acceptance_criteria>
  <done>All u.chunk() callers replaced with itertools.batched(); linters pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace setdefault grouping patterns with defaultdict</name>
  <read_first>
    flext-infra/src/flext_infra/
    flext-ldif/src/flext_ldif/
  </read_first>
  <files>Files containing .setdefault(key, []).append() patterns across src/</files>
  <action>
    1. Find all `.setdefault(key, []).append(value)` patterns in `*/src/**/*.py`:
       Use Grep for pattern `setdefault\(.+,\s*\[\]\)\.append` across all src/ directories.

    2. For each GENUINE GROUPING pattern (dict initialized as empty, then setdefault+append in loop):
       ```python
       # Before
       result: dict[str, list[X]] = {}
       for item in items:
           result.setdefault(item.key, []).append(item.value)

       # After
       from collections import defaultdict

       result: defaultdict[str, list[X]] = defaultdict(list)
       for item in items:
           result[item.key].append(item.value)
       ```

    3. Do NOT convert these patterns:
       - `kwargs.setdefault("key", scalar_value)` — settings defaults, not grouping
       - `.setdefault(key, set()).add(value)` — use `defaultdict(set)` for these
       - Any setdefault in frozen `_utilities/*` directory

    4. Research found ~23 setdefault occurrences, ~10-12 genuine grouping patterns.
       Triage each one. Convert only grouping patterns.

    5. Run `.venv/bin/pyrefly check` and `make lint` on each affected project.

    Per MOD-05: defaultdict replaces hand-rolled grouping patterns.
  </action>
  <verify>
    <automated>cd /home/marlonsc/flext && .venv/bin/python -c "
import subprocess, sys
# Check that no setdefault([]).append patterns remain in src
result = subprocess.run(['grep', '-rn', 'setdefault.*\[\].*append', '--include=*.py'] + [f'{p}/src/' for p in ['flext-core','flext-infra','flext-tests','flext-cli','flext-api','flext-auth','flext-web','flext-grpc','flext-ldap','flext-ldif','flext-meltano','flext-observability','flext-oracle-oic','flext-oracle-wms','flext-quality','flext-plugin']], capture_output=True, text=True)
lines = [l for l in result.stdout.strip().split('\n') if l and '_utilities/' not in l]
if lines:
    print(f'FAIL: grouping setdefault patterns remain:\n' + '\n'.join(lines))
    sys.exit(1)
print('PASS: No setdefault grouping patterns remain outside _utilities/')
"</automated>
  </verify>
  <acceptance_criteria>
    - Zero `.setdefault(key, []).append()` patterns in `*/src/**/*.py` (outside frozen _utilities)
    - All conversions use `from collections import defaultdict`
    - Type annotations updated from `dict[K, list[V]]` to `defaultdict[K, list[V]]`
    - `make lint` passes on affected projects
  </acceptance_criteria>
  <done>All hand-rolled grouping patterns converted to defaultdict; linters pass.</done>
</task>

</tasks>

<verification>
- `make lint` passes workspace-wide
- `.venv/bin/pyrefly check` passes on affected projects
- Zero `u.chunk(` calls in src/
- Zero `setdefault(k, []).append` grouping patterns in src/ (outside frozen dirs)
</verification>

<success_criteria>
MOD-01 and MOD-05 satisfied: all custom chunking replaced with itertools.batched, all grouping patterns use defaultdict.
</success_criteria>

<output>
After completion, create `.planning/phases/04-python313-modernization/04-01-SUMMARY.md`
</output>
