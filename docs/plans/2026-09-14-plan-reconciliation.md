---
title: Plan reconciliation and fixed Make contracts
source_updated_at: 2026-09-14T21:55:00Z
source: Operator-approved conversation
status: Approved
work_item: flext-ro6mj
---

# Plan reconciliation and fixed Make contracts

## Audit and handoff amendment

Operator amendment, 2026-09-14T21:55:00Z: pause implementation to investigate
the requested outcome, critique actual execution, preserve all current work in
published WIP branches and Draft PRs, and produce a durable handoff. Read the
[handoff index](2026-09-14-plan-reconciliation/00-index.md) and
[critical audit](2026-09-14-plan-reconciliation/handoff.md) before resuming.
This changes the immediate activity, not the approved product acceptance below.
The automation increment and full reconciliation remain incomplete; Beads owns
their live state. No historical pass or merged earlier PR validates later WIP.

## Approved outcome

Publish one reusable workflow, invoked by “Reconcilie os planos do FLEXT até
terminar”, through canonical agents governance and AI Hub distribution. Collect
FLEXT plans and their research into versioned `docs/plans/`, project them into
the configured `~/docs/plans/` destination, then reconcile documentation,
architecture decisions, and every open and closed FLEXT Bead against evidence.

Beads owns execution state and closure. This plan records the approved contract;
it does not duplicate the tracker. Functional implementation discovered during
reconciliation remains accurately tracked without expanding this documentary
cycle. Implementing the collector and workflow is explicitly in scope.

## Sequential delivery

1. Deliver the usable automation increment: fixed Make effects, the governance
   workflow distributed through AI Hub, and the incremental collector integrated
   with the workspace `make docs` lifecycle.
2. Prove collection, projection, reconciliation, and interrupted-cycle resumption
   with one real plan and its annexes through reviewed integration.
3. Reconcile the complete source inventory, newest plan first, finishing the
   integration of each plan before starting the next.

Operator amendment, 2026-09-14T20:05:00Z: automation has priority over the
previously expanded serialization repairs. Preserve their work without turning
this documentary cycle into a general code migration. The operator selected
versioned FLEXT plans plus a home-directory projection, not a new repository.

The active first increment is `flext-ro6mj.1`. Related prior work includes
`flext-uqji`, `flext-im2my`, `flext-5mgye`, and `flext-xeg9x`; historical claims
must be revalidated rather than inherited as proof.

## Fixed Make contract

Each public verb performs its fixed operation without an effect token, mode
selector, or validation of retired parameters. Remove the former `APPLY`
mechanism, derived check-mode branches, schemas, templates, consumers, tests,
and operational instructions. Do not replace it with another toggle.

`make docs` collects and updates documentation; `make gen` generates;
verification verbs retain their own validation responsibilities. Preserve
content, environment, and integrity validation. Change sources and regenerate
projections instead of editing generated Makefiles.

## Governance ownership and distribution

`agents` owns generic rules, skills, commands, their supporting resources, and
semantic evaluations. Reconcile the existing documentation, architecture,
Beads, Make, handoff, and plan-ownership procedures before adding the workflow
composition `plan-reconciliation` and command `/reconcile-plans`.

AI Hub consumes the published governance bundle, selects capabilities, and
reconciles managed provider projections. Creation, renaming, and removal must
reach FLEXT consumers through that owner. Preserve independent local material;
remove obsolete managed identities with ownership evidence. Prove a second
generation has no further changes.

FLEXT owns its source association and destination configuration. A thin script
connected through `pre-docs` in `custom.mk` invokes reusable implementation
owned by `flext-infra`. Existing session extractors remain with their owners.
Correct the existing document generator's publication path through its
transaction owner; do not replace the builtin docs lifecycle or introduce a
second publisher. Declare the external projection root explicitly without
weakening destination-boundary checks. Import existing home plans before
publishing, and treat later home edits as incoming revisions rather than
silently overwriting them.

## Collection contract

Inventory workspace and member documents, earlier home-directory plans, OMO,
Sisyphus, OpenCode, Codex, VS Code, Claude, Poolside, and Kimi. Include relevant
historical sessions and workspaces. Read every plan and its associated research,
addenda, and handoffs in full; never publish entire private transcripts.

Use a stable plan filename and a companion directory with the same basename.
Preserve provider and source identity, provenance, references, and revisions.
Order the index by the source's substantive update, oldest first, and process
it bottom-up, newest first. Keep source,
collection, and reconciliation timestamps separate. Use ISO 8601 UTC when a
complete timestamp exists; preserve incomplete precision without invented time.

Identity and content digests include attachments. Collection is atomic,
preserves reconciled content, and exposes changed source revisions for review.
An unchanged second collection produces no changes. Automated collection does
not decide implementation status, semantic supersession, deletion, or Bead
state: the invoked agent workflow performs those evidence-backed decisions.
Missing source access prevents claiming complete coverage. Provider extraction
is automatic through its declared owner, never a manual-export prerequisite.
Collection provenance is not a second execution tracker.

## Reconciliation and ADR consolidation

For every plan, compare its orientations with newer decisions, registered
records, current integrated code, measured behavior, documentation, ADRs,
commits, PRs, and Beads. Classify each orientation as implemented, partial,
pending, superseded, or unproven, with evidence and the successor where relevant.

Revalidate all linked Beads, including closed records, and correct dependencies,
scope, references, and incorrect closures. Examine unmatched Beads without
inventing relationships. Apply documentation and governance changes at their
canonical owners, validate, review, integrate, and record post-merge proof before
advancing to the next plan.

For duplicate ADR decisions in the same scope, preserve the oldest identity as
established by history. Incorporate unique content and newer approved decisions
there. Immediately below changed passages, record the dated change, rationale,
source ADR, plan, and Beads. Replace duplicate passages in newer ADRs with
consultation references to the canonical ADR. Preserve distinct decisions in
partially overlapping records and repair indexes and consumers.

Discard a wholly superseded plan only after proving no unique contribution
remains, preserving useful evidence and repairing references. Record its
successor and disposition in Beads so collection cannot recreate that revision.
Do not delete complete sessions or rewrite Git history.

## Execution boundary and acceptance

Use `direnv exec` in the appropriate checkout. The operator authorized manual
tracker and integration work while rigs remain suspended, then required a
separate FLEXT worktree outside the primary checkout. The primary root and its
31 declared members remain on `0.12.0-dev`; preserve all existing work.

Use bounded workers for research, disjoint simple edits, and independent review.
The main agent owns coordination, validation, review disposition, and merges.
Each increment requires native gates, independent approval, merge-commit
integration, and post-merge evidence. Publish member commits before gitlinks.

Necessary structural code movements use the existing Rope owner, semantic
identity and reference propagation across all affected consumers. Compose
facades in the generator, never by hand. Regenerate through callbacks in the
existing loop, refresh its indices, and require convergence without ambiguous
owner guesses or no-progress retries. This requirement does not turn document
copying into a code refactor or authorize an unrelated migration campaign.

Acceptance covers fixed Make behavior, managed identity creation/rename/removal,
bundle and projection convergence, collection revisions and attachments,
temporal ambiguity, source failures, semantic ADR deduplication, intact links,
Bead evidence, prompt activation, and interrupted-cycle resumption. Completion
requires full source coverage, reconciled plans and Beads, integrated changes,
and a final collection with no pending new revisions.
