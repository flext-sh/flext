# Release v0.12.0rc2

## Status

- Quality: Alpha
- Usage: Non-production

## Scope

- Workspace release version: 0.12.0rc2
- Follows 0.12.0rc1 with CI matrix default-off and release Make wiring

## Highlights

- `ci-matrix` projected for workspace-root/standalone defaults to `workflow_dispatch` only
- Opt into push-to-`main` auto-run via `repository_policy_overlays.ci_matrix_auto_run: true`
- Workspace Release invokes `make release WHAT=rel APPLY=Y` (no bare `make rel`)
- uv dependency cooldown projected as `exclude-newer = "14 days"`

## Projects impacted

- root and all flext-sh workspace members (same set as v0.12.0rc1)
