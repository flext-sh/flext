# Generated by `flext-infra codegen conform`.
# Project identity is derived from the typed repository owner.
issue-prefix: flext
# Dolt shared-server connection (Gas Town town server on :3307).
# The `flext` database lives there; a repository-local server is never used.
dolt:
    mode: server
    shared-server: true
    host: 127.0.0.1
    port: 3307
    user: root
    database: flext
    auto-commit: on
sync:
    remote: "git+https://github.com/flext-sh/flext.git"
prefix: flext
dolt.idle-timeout: "0"
export.auto: "false"
types.custom: agent,role,rig,convoy,slot,queue,event,message,molecule,gate,merge-request
types.infra: agent,role,message
