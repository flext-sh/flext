# Generated by `flext-infra codegen conform`.
# Project identity is derived from the typed repository owner.
issue-prefix: "mro"

# Dolt shared-server connection (machine-wide shared server on :3308).
# The `mro` database lives there; a repository-local server is never used.
dolt:
    mode: server
    shared-server: true
    host: 127.0.0.1
    port: 3308
    user: root
    database: mro
    auto-commit: on
