dbt_ldap:
  target: test
  outputs:
    test:
      type: postgres
      host: postgres
      user: flx_user
      password: flx_password
      port: 5432
      dbname: flx_ldap_test
      schema: analytics_ldap
      threads: 4
      keepalives_idle: 0
      search_path: "analytics_ldap,staging_ldap,raw_ldap"
