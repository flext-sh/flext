id: pydantic-model-rebuild-critical
language: python
rule:
  pattern: $OBJ.model_rebuild($$$ARGS)
message: "model_rebuild() is prohibited. Resolve type references at definition time or use protocols."
severity: error
