id: ban-direct-structlog-getlogger
language: python
rule:
  pattern: structlog.get_logger($$$ARGS)
message: "Direct structlog.get_logger() is banned — use FlextLogger.create_module_logger()"
severity: error
