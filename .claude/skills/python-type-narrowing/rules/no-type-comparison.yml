id: no-type-comparison
language: python
rule:
  kind: comparison_operator
  has:
    kind: call
    has:
      kind: identifier
      regex: "^type$"
      field: function
    stopBy: end
message: "type() comparison - use isinstance() instead"
severity: warning
