id: no-type-in-condition
language: python
rule:
  kind: if_statement
  has:
    kind: call
    has:
      kind: identifier
      regex: "^type$"
      field: function
    stopBy: neighbor
message: "type() in condition - use isinstance()"
severity: warning
