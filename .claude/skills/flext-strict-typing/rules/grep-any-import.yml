id: grep-any-import
language: python
rule:
  kind: import_from_statement
  all:
    - has:
        kind: dotted_name
        field: module_name
        regex: "^typing$"
    - has:
        stopBy: end
        kind: identifier
        regex: "^Any$"
message: "Importing Any from typing is banned"
severity: error
