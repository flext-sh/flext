id: ban-mapping-usage
language: python
rule:
  kind: identifier
  regex: "^__FLEXT_MAPPING_RULE_RETIRED__$"
  not:
    inside:
      kind: import_from_statement
      stopBy: end
message: "Rule retired: Mapping is preferred for read-only contracts in FLEXT."
severity: warning
