id: ban-direct-beartype-decorator
language: python
rule:
  kind: decorator
  has:
    kind: identifier
    regex: "^beartype$"
    stopBy: end
message: "Direct @beartype decorator banned - use package-level conf"
severity: error
