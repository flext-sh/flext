id: fix-legacy-config-class
language: python
rule:
  pattern: |
    class Config:
      $$$BODY
fix: model_config = SettingsConfigDict()
message: "Rewrite class Config -> model_config = SettingsConfigDict()"
severity: error
